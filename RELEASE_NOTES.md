# Release Notes

## v0.1.1

Scaffold-only version bump. No functional changes.

The repository currently contains the initial Tauri v2 project scaffold and the migration specification (`MIGRATION_SPEC.md`). Implementation of the features described in `CLAUDE.md` is pending.

## v0.1.0

- Initial Tauri v2 project scaffold with migration spec.
