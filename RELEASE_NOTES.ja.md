# リリースノート

## v2.0.1

v2.0.0 へのフォローアップリリースです。目玉の変更は **Google Cloud IAP** をトップレベルのプロトコルに昇格させたことで、IAP 接続に SSH 認証情報が一切不要になりました。残りはセキュリティが中心で、ファイルシステム周辺のレンダラー側列挙ギャップを 1 件塞ぎ、Windows でこれまで静かに既存の機微パスチェックを無効化していたマッチャーのバグを 1 件修正しました。あわせて v2.0.0 で取りこぼしていた小さな UI フォント統一も入っています。

### 新機能

- **Google Cloud IAP がトップレベルのプロトコルになりました。** 従来は SSH フォーム内の "Connect via Google Cloud IAP" チェックボックスでしたが、Protocol ドロップダウンに独立した項目として並ぶようになりました (新規セッションダイアログとホストツリーの追加/編集モーダルの両方)。これを選ぶとユーザー名 / パスワード / 秘密鍵フィールドは消え、HoTTY は接続を <code>gcloud compute ssh --tunnel-through-iap</code> に委譲します。IAP トンネルの確立、OS Login のマッピング、SSH キーの自動生成 (<code>~/.ssh/google_compute_engine</code>)、プロジェクトへのキー登録、認証はすべて gcloud 側で処理されます。Google Cloud SDK をインストールし `gcloud auth login` を一度完了させておくだけで使えます。ホストツリー上では `<project>:<instance> (IAP)` のように表示されるので一目で識別できます。

### 改善

- **UI フォントの統一。** **Confirm**、**Paste Confirmation**、**SSH Host Key**、**Ask AI**、**Custom Theme Creator** モーダルのアクションボタン、および **Log Viewer**、**Ping Monitor**、**Text Editor** ペインのツールバーボタンが UI クローム用フォント (`--ui-font-family`) を使うようになりました。いくつかは等幅フォント (`--font-family`) で描画されており、周囲のクロームに対して微妙に浮いて見える状態でした。内部の等幅コンテンツ (ペースト時のプレビュー、ホストキーのフィンガープリント等) は従来どおりです。Confirm、Ask AI、Custom Theme Creator モーダルのコンテナにも欠落していたクロームフォント宣言を補いました。

### セキュリティ

- **ファイルエクスプローラーが機微なディレクトリの列挙を拒否するようになりました。** ファイルブラウザペイン (とその裏側の `file_explorer_list_directory` Tauri コマンド) は、認証情報の保管場所 — `~/.ssh`、`~/.aws`、`~/.azure`、`~/.gnupg`、`%APPDATA%\Roaming\gcloud`、`%APPDATA%\Local\Microsoft\Vault`、HoTTY 自身の `%APPDATA%\{Roaming,Local}\com.hotty.terminal`、その他既存のブロックリスト — に解決されるパスの列挙を拒否します。従来はレンダラーが任意のディレクトリを列挙でき、ファイルの中身は出ないものの、どの認証ファイルが存在しいつ最後に更新されたかが漏れていました。Text Editor や Log Viewer の読み取り経路は既に同等のガードで守られており、ファイルエクスプローラーも揃えるべき多層防御として実装しています (悪用にはそもそもレンダラー側の侵害が前提です)。
- **Windows で機微パスマッチャーが機能していなかった問題の修正。** `is_sensitive_path()` — Text Editor の読み書き / ドロップファイル承認 / Vertex AI サービスアカウントキーファイル選択フローのガード — は正規化済みパスに対して `starts_with(home + dir)` で前方一致比較を行っていました。Windows では `canonicalize()` が `\\?\` のベルバティム接頭辞付きパスを返しますが、マッチャー側でこれを除去していなかったため一致せず、ガードは正規化済みパスに対し事実上ノーオペでした。マッチャーは比較前に解決パスとホームディレクトリ双方から接頭辞を除去するようになり、リグレッションテストも追加しました。悪用報告はありませんが、これらのフローで本来効くべきだったオンディスク保護が実際に機能するようになりました。

## v2.0.0

v2.0.0 の正式版リリースです。最終ハードニング回として、認証情報の保存および Tauri コマンドサーフェスまわりに 5 件の多層防御を追加し、モーダルファミリー全体の細かなビジュアル統一を行いました。

### 改善

- **モーダルの一貫性の磨き込み。** モーダルファミリー全体でいくつかの細かなビジュアルの不整合を揃えました: **Ask AI** はスクロール時もコンテンツが角丸内に正しくクリップされ、**System Prompt** のオーバーレイは余計なインセットを持たなくなり、**Custom Theme Creator** は標準のモーダル z-index を使用するようになりました。**Paste Confirmation** と **SSH Host Key** ダイアログのコンテナは UI 用フォントを使うようになりました (内部の等幅プレビューとフィンガープリントは従来どおり)。Text Editor のメニュー・検索バー・検索クローズの各ボタンは標準の 4px 角丸を使うようになりました。

### セキュリティ

- **AI のサービスアカウントキーファイルにダイアログ承認を必須化しました。** Vertex AI の `service_account` 認証経路は、`keyFilePath` がネイティブのファイル選択ダイアログ経由で選ばれていない限り拒否するようになりました。レンダラーが侵害されても、AI 認証を任意のオンディスク JSON キーへ向けることはできません。`auto_auth` の再開経路は影響を受けません — DPAPI 暗号化済みのオンディスク設定から `client_email` と `private_key` を読み込み、ユーザーのキーファイル自体を再読込することはないためです。
- **DPAPI 暗号文を HoTTY に紐づけるようになりました。** 新規に保存される認証情報は、HoTTY 固有のエントロピーと、平文先頭に付与する内部「HoTTY」マーカーを併用して `CryptProtectData` で暗号化されます。レンダラーから呼び出せる `dpapi_decrypt` / `dpapi_decrypt_batch` コマンドはこれにより、他アプリケーションの DPAPI 暗号文 (例: 他のアプリが保存した暗号化キーブロブ) を復号しなくなります — 従来はユーザー単位の汎用復号オラクルとして機能していました。エントロピー導入前の旧 `[SAFE]` 暗号文は引き続き透過的に復号され、次回保存時に新形式へ自動アップグレードされます。
- **機微なパスのブロックリストを拡張しました。** Text Editor とドロップファイル承認フローは、`~/.aws`、`~/.azure`、`%APPDATA%\Roaming\gcloud`、`%APPDATA%\Local\Microsoft\Vault`、`~/.config/gcloud`、および HoTTY 自身の `%APPDATA%\{Roaming,Local}\com.hotty.terminal` 配下のパスを拒否するようになりました。最後の項目が重要で、エディター経由の書き込みが HoTTY の `approved_log_dirs.json` や `vertexai_config.json` などを改ざんできなくなります — これらが書き換え可能だと、他所で利用しているダイアログ承認の不変条件が崩れていました。
- **Telnet 自動ログインがパスワードをユーザー名として流出させなくなりました。** ログイン前バナーが `Username:` ではなく `...Password:` で終わっている telnet サーバーに接続すると、自動ログインのステートマシンがユーザー名送信前にパスワードフェーズへフォールスルーし、設定されたパスワードがユーザー名フィールドに送信される問題がありました。ステートマシンはユーザー名待ちの間、本物のユーザー名プロンプトにのみ反応するようになりました。
- **バックエンドのセッション設定の Debug 出力で認証情報を伏字化するようになりました。** `SshConfig`、`TelnetConfig`、`JumpboxConfig` は、Rust の `{:?}` デバッグフォーマッタを通して `password` / `private_key_passphrase` の値を露出しなくなりました。現状はどのログ呼び出し箇所にも到達していませんが、将来ログフォーマットのリグレッションによって誤って出力されるのを防ぐ多層防御です。

## v2.0.0-beta13

正式版リリース前の 2 回目のハードニング回です。ターミナルのフォントとスクロールバック設定が起動中のセッションにも即時反映されるようになり、AI チャットタブが連携ターミナルのクローズに追従するようになりました。あわせて、フロントエンドのログ出力・ログフォルダーへのアクセス・外部 URL のオープン周りで 3 つのセキュリティ強化を行っています。

### 改善

- **ターミナルのフォントとスクロールバック変更が起動中のセッションに反映されます。** **Settings &rarr; Appearance &rarr; Font Size** / **Font Family**、**Settings &rarr; General &rarr; Scrollback Buffer** の変更が、すでに開いているターミナルにも即時反映されるようになりました。従来は新規接続時のみ適用されていました。
- **AI チャットタブがセッションのライフサイクルに追従します。** ターミナルセッションが閉じられた場合 (切断時の自動クローズ、または手動クローズ) に、そのセッションにリンクされていた AI チャットペインのタブが連動してクローズされます。ペイン内の最後のタブはペインが使用不能にならないようリンクのみ解除されます。
- **モーダルのクローズ X ボタンの統一。** **Ask AI** および **System Prompt** ダイアログのヘッダークローズ X のフォントサイズが、他のモーダルと一致するように調整されました。

### セキュリティ

- **ログフォルダーが明示的な承認を必要とするようになりました。** ログ出力先・ログビューアからの読み込み先には、ユーザーが承認したフォルダーしか使用できません。**Browse...** ボタンでフォルダーを選択した場合は自動的に承認されます。手入力されたパスは、最初に使用される時点で OS ネイティブの確認ダイアログが表示されます。承認内容は `%APPDATA%\com.hotty.terminal\approved_log_dirs.json` に永続化されるため (ユーザー単位、レンダラーから書き込み不可)、フォルダーごとに 1 度承認すれば次回以降の起動でダイアログは出ません。レンダラーが侵害されてもこの承認を偽造できないため、Tauri コマンドの呼び出しだけで攻撃者指定のパスをログ書き込み・読み取り対象に追加することはできません。
- **キュレーション済みリスト外の外部 URL は確認が必要になりました。** HoTTY リポジトリ・gcloud インストールドキュメント・GPL ライセンス・Google OAuth 同意フローへのリンクは従来どおり即座に開かれます。それ以外の URL — ターミナル出力や AI チャットでユーザーがクリックしたリンクを含む — は、システムブラウザに渡される前に URL 全文を表示する OS ネイティブの確認ダイアログが表示されます。Tauri capability のスコープも対応してキュレーション済みホストのみに絞られました。
- **フロントエンドのログ転送が認証情報らしきフィールドを伏字化するようになりました。** `logDebug` (レンダラーから `%APPDATA%/com.hotty.terminal/logs/` 配下の永続デバッグログへ転送される経路) は、`password`, `apikey` / `api_key` / `api-key`, `secret`, `token`, `clientSecret`, `privateKey`, `refreshToken`, `accessToken`、および `Bearer` HTTP ヘッダーの値を伏字化します。メッセージ長は両端で 4 KB に制限されます。これは多層防御で、現状の既知のリーク修正ではなく、将来的に誤って認証情報をログ出力してしまうリグレッションが入った場合の被害範囲を限定することが目的です。

## v2.0.0-beta12

正式版リリース前のハードニング回です。SSH アルゴリズム既定値の現代化、AI チャットのストリームアイドル監視、ホストツリー・AI チャット履歴・ペルソナ設定まわりの静かなデータ整合性バグの修正、モーダル・フォーカス・入力バリデーション周辺の UX 改善を含みます。

### 新機能

- **AI チャットタブ → リンク先ターミナルのフラッシュ表示** — リンクされたターミナルセッションを持つ AI チャットタブをクリックすると、対応するターミナルペインが一瞬ハイライトされ、どのセッションに紐付いているかが一目でわかります。
- **AI ストリームのアイドル監視** — AI プロバイダがストリーム途中で応答を止めた場合 (ネットワーク断、バックエンドのハングなど)、3 分間の無音で自動的にリクエストをキャンセルし、ストリーミング状態のまま固まらずチャットにエラーが表示されます。
- **`diffie-hellman-group-exchange-sha1` 有効化時の警告ダイアログ** — **Settings → Protocols → SSH Algorithms** でこの非推奨 KEX を有効化しようとすると、SHA-1 が破られていることを説明し、より安全な代替を提示する警告ダイアログが表示されます。
- **AI チャット言語の OS ロケール自動検出** — 日本語ロケールの環境で初回起動した場合、AI チャットの言語が既定で「日本語」になります。既存ユーザーの選択は維持されます。
- **接続ダイアログの入力バリデーション** — 接続フォームが、空ホスト・1〜65535 範囲外のポート・不正な GCP プロジェクト ID・CRLF/空白の混入などを接続前にチェックするようになりました。

### 改善

- **モダンな SSH KEX 既定値** — 同梱のアルゴリズムリストで `diffie-hellman-group14-sha256` を有効、`diffie-hellman-group1-sha1` を無効にしました。既存ユーザーの選択はそのまま保持され、リリースで新規追加されたアルゴリズムは読み込み時にユーザー設定にマージされるため、手動リセットなしでセキュリティ改善が行き渡ります。
- **ジャンプボックス Host-Key プロンプトのタイムアウト** — バスチオンの host-key プロンプトを未応答のまま放置すると接続全体が無期限にハングしていましたが、5 分で接続を切断するようになりました。
- **モーダルの Escape スタック** — 複数モーダルが同時にマウントされている状態で Escape を押すと、最前面のモーダルだけが閉じます。従来は全モーダルのリスナーが並列発火し、見えていない背景モーダルまで閉じられる場合がありました。
- **モーダルクローズ後のフォーカス復元** — モーダルを閉じたとき、開く前にフォーカスを持っていた要素 (入力欄・ターミナル・ボタン) にフォーカスが戻ります。削除されたボタン上にフォーカスが取り残されることがありません。
- **マルチバイトプロンプトの検出** — ターミナルテキストをユーザー定義のプロンプトパターンに照合する前に NFC 正規化するようになりました。結合文字や全角文字 (日本語、アクセント付きラテン) を含むプロンプトが、機器の送出形式に依らず安定して検出されます。
- **静かに失敗していた処理の通知化** — クリップボードコピー失敗とセッションログ切替失敗がトースト通知として表示されるようになりました。AI チャットの invoke 失敗もハンドリングされ、UI が「streaming」状態で固まることがなくなりました。
- **到達不能な AI プロバイダで素早く失敗** — AI HTTP リクエストに 30 秒の接続タイムアウトを設定しました。設定ミスのエンドポイントでチャットがハングしません。

### バグ修正

- **セッション切断時にバックグラウンドタスクがリークしていました。** SSH/Serial/WSL/Local セッションは reader/writer/keepalive タスクハンドルに対し `tokio::time::timeout` を使っていましたが、タイムアウトしても JoinHandle が drop (デタッチ) されるだけでタスクは abort されていませんでした。長時間運用でゾンビタスクが少しずつ蓄積していました。猶予を 1.5 秒に延長したうえで、超過時は明示的に abort するように修正しました。
- **AI チャットでのキャンセル後にチャットが壊れることがありました。** ストリームをキャンセルすると、ユーザーメッセージのみが履歴に残り assistant 応答が無い状態になり、Anthropic が要求する user/assistant 交互ルールに違反していました。次回リクエストが API に拒否されます。キャンセルされたターンには `[cancelled]` プレースホルダーが記録され、以降のメッセージが正常に送信されるようになりました。
- **`aiCommandIdleTimeoutSecs`・カスタム AI ペルソナなどの設定がアップグレード時に消える可能性がありました。** 設定マイグレーションがバージョンバンプのたびに `aiPersonas` を同梱既定値で無条件上書きしていました。カスタマイズ済みプロンプトとユーザー追加ペルソナはアップグレードを跨いで保持され、最新の標準プロンプトを取り込みたい場合は Settings の **Reset All Personas** を使用してください。
- **アップデート通知の `beta9` と `beta10` の順序が逆転していました。** 文字列の辞書順比較で `beta10 < beta9` になるため、`beta10` のユーザーに `beta9` が新しいと通知される (またはアップデートが提示されない) ことがありました。同じアルファベット接頭辞を持つ pre-release タグは数値比較されるよう修正しました。
- **ホストツリーの編集が連続操作で失われる可能性がありました。** 暗号化が非同期のため、素早い 2 回の編集が逆順で完了し、古い暗号化結果が新しいものを上書きすることがありました。書き込みを単調増加カウンタで保護し、最新の暗号化結果のみ永続化されるようにしました。
- **セッションステータスがエラー遷移で一瞬ちらつくことがありました。** connect-promise の catch と `onSessionError` リスナーが同じ失敗で state 更新とログを二重に行っていました。冗長な経路を抑止しました。
- **設定を再オープンした際に古い「SHA-1 を有効化?」警告ダイアログが残ることがありました。** 警告ダイアログ表示中に Settings モーダルを閉じると保留状態が残存し、次回オープン時にダイアログが再出現していました。タブのアンマウント時に状態をリセットするようにしました。
- **AI チャットペインを連携先ターミナルより先に閉じると watch バッファがリークしていました。** 連携セッションのバッファがそのセッション自身が削除されるまで残存していました。セッション削除時には watch していたペインに関わらずバッファを必ず破棄するようにしました。

### セキュリティ

- **テキストエディタが symlink 直接ドロップを拒否します。** テキストエディタにドロップされたファイルがシンボリックリンクの場合、機密領域を指しているリンクを誤って承認させられないように直接拒否されます。
- **`known_hosts` 書き込み失敗がユーザーに通知されます。** SSH がエントリの保存・削除に失敗した場合 (権限不足、ディスクフル等) にログ出力に加え `ssh-known-hosts-warning` イベントを emit するようになりました。次回接続でホストキープロンプトが予期せず再出現してから気付くのではなく、発生時点で把握できます。
- **DPAPI のパススルーが疑わしい入力で警告するようになりました。** クレデンシャル復号器が `[` で始まる (が未知のタグの) 入力でプレーンテキストパススルーにフォールバックした場合、暗号化タグの破損が判別できるようログに警告が出力されます。

## v2.0.0-beta11

正確性とセキュリティに関するリリースです。これまで見た目だけだった Settings の SSH アルゴリズム設定が実際にハンドシェイクへ反映されるようになり、SHA-1 KEX・3DES・DSA ホストキーを要求するレガシー機器との接続が可能になりました。あわせて、ジャンプボックス側 SSH の `known_hosts` I/O ハードニング (直接接続側と同等) も追加しています。

### バグ修正

- **Settings の SSH アルゴリズム設定が接続に反映されるようになりました。** 従来は **Settings &rarr; Protocols &rarr; SSH Algorithms** の kex / cipher / MAC / ホストキーのトグルが保存されるだけで SSH クライアント側に渡されておらず、UI 上の選択に関わらず常に russh の組み込み既定リストが提示されていました。今回の修正で直接 SSH 接続にもジャンプボックス経由にも、UI で有効化したアルゴリズムが実際にハンドシェイクで使われます。これにより、SHA-1 KEX (`diffie-hellman-group14-sha1`, `diffie-hellman-group1-sha1`, `diffie-hellman-group-exchange-sha1`)、`3des-cbc`、`ssh-dss` ホストキーしか受け付けないレガシー機器 (Cisco Catalyst 3650・旧 Cisco IOS など) にも接続できるようになりました。これらは選択可能かつ実効性のある状態になっています。
- いずれかのカテゴリ内で全アルゴリズムを無効化した場合、ライブラリ既定値へ無言でフォールバックせず、明確なエラーで接続を拒否するようになりました。設定に未知のアルゴリズム名が含まれていた場合は警告ログを出してスキップされます。

### セキュリティ

- **ジャンプボックス側 `known_hosts` の I/O エラー時に接続を拒否するようになりました。** 直接 SSH 接続側と同じハードニングをジャンプボックス経路にも適用しています。バスチオン側の `known_hosts` を破壊・読み取り不能にできる攻撃者が、ユーザーを「新しいホスト」プロンプトへ戻して攻撃者制御下のキーを承認させるシナリオを防ぎます。

## v2.0.0-beta10

AI チャット UX の大幅刷新を中心としたリリースです。AI チャットペイン内のタブ化とターミナルセッションへのスマート連携、入力欄に統合した実行モードバー (一時停止／再開対応)、デバイス応答アイドルタイムアウト設定の追加、Network Expert ペルソナのプロンプト改善などを含みます。

### 新機能

- **AI チャットタブ** — AI チャットペイン上部にタブストリップを追加し、複数の会話を並行管理できるようになりました。ターミナルで **AI モニター** をオンにするとそのセッション専用タブが自動でリンクされ、別のターミナルでオンにすると新しいタブが作られるため、複数の監視ストリームが混ざりません。ターミナルを選択するとリンクされた AI タブに自動で切り替わり、現在リンクしているターミナルは入力欄の隣にチップとして表示されます。**+ New chat** で新しいタブを開けます。
- **インライン実行モードバー (一時停止／再開)** — AI チャットペインの入力カード下部に Execution Mode チップを配置し、自動実行ループ専用の一時停止／再開コントロールを追加しました。これらの操作は従来 Settings ダイアログ側にありました。
- **AI コマンドのアイドルタイムアウト** — `aiCommandIdleTimeoutSecs` 設定 (既定 10 秒、`0` で無効、上限 30 分) を新設し、AI 実行ポーリングの暗黙の 30 秒タイムアウトを置き換えました。タイムアウト発火時は、それまでに取得した出力と `[no response from device for N seconds]` の注記を AI に送るため、会話が無音で止まらず継続されます。

### 改善

- **AI チャット UX の再設計** — チップ状のモード選択、リンク済みターミナルチップ、空状態のオンボーディング、送信無効時のヒント、ストリーミング中のトークン進捗表示を追加。
- **AI チャット入力欄の統合** — 入力欄、添付物、実行モードチップを単一の角丸カードに統合し、チップを右寄せに配置して見た目を整理しました。
- **AI チャットヘッダー設定をポップオーバーへ集約** — 従来 AI チャットヘッダーに散らばっていた設定項目を、入力ツールバーから開くポップオーバーに集約しました。スタンドアロンの System Prompt ボタンは削除し、ポップオーバー内へ移しています。
- **折りたたみ可能なターミナル出力ブロック** — AI チャットに取り込まれたターミナル出力を、先頭コマンド・行数・文字数をヘッダーに表示する折りたたみブロックとして描画します。クリックまたは <kbd>Enter</kbd> / <kbd>Space</kbd> で展開／折りたたみ。
- **Network Expert ペルソナのプロンプト** — 先頭に必須のセッション開始プロトコル (REPLY 1 で `show version` 相当、REPLY 2 で `terminal length 0` 相当、REPLY 3 以降でユーザー応答) を強制する形で書き直し、ページャ付きデバイスで応答ループが止まらないようにしました。
- **SSH/Telnet 接続タイムアウト既定値** — 3 秒から 5 秒に引き上げ、ジャンプボックス／IAP トンネル経由などの遅い経路でも待ちやすくしました。

### バグ修正

- **AI チャットのターゲットチップが残る問題** — リンク先セッションが削除された場合や **AI モニター** が無効化された場合に、リンク済みターミナルチップが正しくクリアされるようになりました。従来は存在しないセッションを指したまま残っていました。
- **AI 実行時の出力切り詰め** — AI 実行ループが取得した長いターミナル出力がモデルに渡る前に切り詰められていた経路を修正しました。

## v2.0.0-beta9

ホストツリーの v1→v2 認証情報自動マイグレーション、ペースト周りの不具合修正、Asset プロトコルおよび SSH known_hosts のセキュリティ強化を含むリリースです。

### 改善

- **ホストツリーの v1→v2 認証情報自動マイグレーション** — 旧 Electron 版 HoTTY (v1) で書き出されたホストツリーは `username` / `password` を `[SAFE]` + base64(`v10` + DPAPI ブロブ) 形式で保持していました。読み込み時に v2 形式 (`[SAFE]` + base64(DPAPI ブロブ)) へインプレースで自動アップグレードします。マイグレーションは Rust バックエンドで実行され、冪等 (v2 エントリはバイト一致でそのまま通過) で、平文の認証情報は IPC 境界を越えません。

### バグ修正

- **Ctrl+V でクリップボード内容が二重にペーストされる問題** — Ctrl+V を押すと、ペースト確認ダイアログが開く前に 1 回ペーストされ、ダイアログで「Paste」を押下するともう 1 回ペーストされていました。xterm.js 内部の `paste` DOM リスナーが、私たちの keydown インターセプターとは独立に発火していたためです。ターミナルホスト側でネイティブの paste イベントを抑止するようにし、確認ダイアログだけがペースト経路になるよう修正しました。右クリックペーストは影響を受けません。
- **ペースト確認ダイアログ閉鎖後にターミナルがフォーカスを失う問題** — ダイアログの Confirm / Cancel 後、削除済みの「Paste」ボタンにフォーカスが取り残され、キーボード入力に戻るには追加のクリックが必要でした。ダイアログのアンマウント後、ペーストを起動したターミナルペインへ自動的にフォーカスを戻すようにしました。

### セキュリティ

- **Tauri Asset プロトコルのスコープを狭窄** — v2 ラインの初期に `tauri.conf.json` の `assetProtocol.scope` が `**` (任意パス) に拡げられていましたが、今回画像拡張子のみ (`.png`, `.jpg`, `.jpeg`, `.gif`, `.bmp`, `.webp`, `.ico`, `.svg`) に制限しました。ペイン背景画像機能は引き続き動作します。レンダラ侵害時の多層防御の強化です。
- **SSH known_hosts の I/O エラー時に接続を拒否** — 従来は `known_hosts` の読み取りで *何らかの* エラー (権限拒否、ディスク障害、部分読み出し) が発生すると、すべて「新規ホスト」として黙って扱い、ユーザーに再プロンプトしていました。攻撃者が当該ファイルを破損 / chmod 0 できる場合、信頼済みホストの再プロンプトを誘発し、差し替えたホスト鍵を承認させる経路になり得ました。本物の I/O エラーはログ出力のうえ接続を拒否し、「ファイル未存在」(初回ユーザー) のみ従来どおり新規ホストプロンプトを返します。

## v2.0.0-beta8

接続ライフサイクル UI、Line Wrap オフ時の水平スクロール、マーカーとスクロールバーをペイン右端に固定する 3 レイヤー構成のターミナルレイアウトを追加したリリースです。

### 新機能

- **接続ライフサイクルオーバーレイ** — トランスポート確立中に Connecting オーバーレイを表示し、接続失敗を黙って終わらせるのではなく閉じられるトースト通知で通知するようになりました。セッションステータスに明示的な `connecting` / `error` を追加し、タブとペイン枠線の色をテーマで切り替え可能にしました。
- **接続タイムアウトを設定可能に** — SSH / Telnet 接続が無限に待機せず、ユーザーが設定可能な時間 (既定 3 秒) でタイムアウトするようになりました。
- **Line Wrap オフ時の水平スクロール** — Line Wrap を無効にするとターミナルペインに水平スクロールバーが復活し、カーソルが右端を越えると伸長します。Enter でスクロール位置がカラム 0 に戻り、入力中はカーソルが見える位置までホストが自動スクロールします。

### 改善

- **3 レイヤーのターミナルレイアウト** — プロンプトマーカーとバーティカルスクロールバーを xterm ホスト外側の専用 DOM レール上にレンダリングするように変更。ホストの水平スクロール位置に関係なく常にペイン右端に固定されるため、スクロールバー / マーカー / テキストが重ならなくなりました。
- **ターミナルのカスタムバーティカルスクロールバー** — xterm v6 既定の Monaco 風スクロールバーをアプリ全体のクロームと統一されたスタイル (グローバルなスクロールバースタイル) に置換。
- **Connecting 状態のテーマカラー** — 新しい接続中状態用のタブ／ペイン色を Dark / Medium / Light の各テーマに既定値として追加。

### バグ修正

- **プロンプトマーカーの色** — プロンプトは prompt-default テーマカラー (赤) を、prompt-active (青) は非プロンプト行 (出力) のみに使用するよう修正。
- **プロンプトマーカーの検出漏れ** — `onRender` で参照していた古いバッファ位置を `buffer.baseY` に置き換えて検出漏れを修正。末尾の未使用行にマーカーが付かないようにしました。
- **プロンプトマーカーの位置ズレ** — マーカーを `right` 基準で配置するよう変更し、親要素の左端や水平スクロール位置に依存して位置がズレる問題を解消。オーバーレイスクロールバー向けの CSS フォールバックと、起動時のカーソル遷移を吸収するコンテンツ基準のチェックを追加。
- **スクロールバー隅のアーティファクト** — 一部ペインの右下に出ていたスクロールバー隅 / リサイザー / ボタンの描画残りを非表示化。
- **出力直後にターミナルビューポートが 1 行巻き戻る** — 本リリースで導入した 3 レイヤーレイアウトの不具合で、`dir` 出力など高頻度な書き込み時にビューポートが 1 行戻り、末尾のプロンプト行がキー入力まで表示されないことがありました。カスタムスクロールバーのスペーサー高さをスクロールイベントと同期して書き込むように修正し、常に最新の出力に追従するようにしました。

## v2.0.0-beta7

上書きインストールの安全性向上、依存パッケージのセキュリティ更新、モーダルの重なり順に関する修正を含むリリースです。

### バグ修正

- **インストーラーが上書きインストール時に「Uninstall before installing」を既定にしない** — 既存の HoTTY インストールを検出した際、アップグレード／ダウングレードのシナリオでは **「Don't uninstall (設定を保持)」** ラジオボタンが事前選択・フォーカスされるようになりました。従来は破壊的な「Uninstall」が既定であり、確認せず進むと WebView2 のローカルストレージに保存されている HostTree や AI プロバイダー認証情報が消える恐れがありました。同一バージョンの再インストール時の挙動は変更ありません。
- **ヘルプモーダルの z-index** — 規約に合わせて `10001` から `10000` に修正。従来値ではネストされた他のオーバーレイより上に表示されるおそれがありました。
- **保存確認モーダルの z-index** — 同様の理由で `10001` から `10000` に修正。このダイアログが他のモーダルの上に表示されることはありません。

### セキュリティ

- **DOMPurify を 3.4.0 にアップグレード** — [GHSA-39q2-94rc-95cp](https://github.com/advisories/GHSA-39q2-94rc-95cp) に対応。`ADD_TAGS` の短絡評価により `FORBID_TAGS` が回避される問題を修正。AI 生成マークダウンは DOMPurify で無害化されるため、当該経路を強化します。

## v2.0.0-beta6

UI 仕上げ、近未来的なテーマ効果、空きペインの背景カスタマイズ、セキュリティ強化を中心とした6番目のベータリリースです。

### 新機能

- **空きペインの背景** — **設定 → 外観** で、空のグリッドペインに表示する単色または任意の画像を選択可能
- **近未来的テーマ効果** — カスタムテーマエディタに **Futuristic Effects** セクションを追加。アクティブペイン/サイドバーアイコンのネオングロー、モーダルのグラスモーフィズム (背景ぼかし)、アイコン線幅・グローぼかしの調整に対応
- **ファイルエクスプローラのサイドバー優先配置** — File Explorer を開くと、グリッドセルを埋める前に空きサイドバースロットへ割り当てる `preferSidebar` 戦略を追加
- **空きペインのドロップヒント** — 空のグリッドセルにペイン番号と「Drop Tab Here」の案内を表示し、タブの配置先を分かりやすく

### 改善

- **設定 UI のリニューアル** — Appearance / Features / General / Protocols の各タブを「カード」単位にグループ化し、Layout / Theme / Font / Terminal Display などのセクションタイトルで視認性を改善
- **テーマ刷新** — ダーク系の `accent-color` を明るい `#00b4ff` に変更、`prompt-highlight-default` / `glow-*` / `glass-*` 変数を Dark / Light / Medium 全テーマに追加
- **アイコンの線を細く** — AI Chat、File Explorer、Log Viewer、Ping Monitor、Help、AppSidebar、TabBar、Sidebar の SVG 線幅を `2` から `1.5` に変更し、洗練された見た目に統一
- **モーダルの背景ぼかし** — すべてのモーダルオーバーレイに控えめな 6px のぼかしを追加
- **プロンプトハイライトのデフォルトがテーマ追従** — 未設定時は `--prompt-highlight-default` を参照するようになり、テーマ切替に追従
- **Ask AI モーダルのスタイル修正** — 回帰していたプライマリボタンの背景・余白・ホバー状態を復元
- **依存整理** — 未使用の `@tauri-apps/plugin-shell` npm 依存を削除

### バグ修正

- **v1 htree インポートの資格情報破損修正** — 旧 v1 ホストツリーからインポートした際に、ユーザー名とパスワードが誤ったフィールドにマッピングされていた問題を修正
- **About タブの GitHub リンク修正** — リンク先および `horryworks/HoTTY-Rust-Tauri` リポジトリ URL を更新
- **セッション重複レース修正** — `connect_session` が接続完了後に ID の重複を再チェックし、衝突時に新しいサービスを安全に切断するように変更

### セキュリティ

- **テキストエディタの TOCTOU 対策** — `text_editor_read_file` / `write_file` が I/O 実行時に解決済みパスとファイルサイズを再検証。ダイアログ承認後のシンボリックリンク差し替えを防止
- **ログビューアの TOCTOU 修正** — `read_log_file` が元の解決パスではなく、再 canonicalize 後のパスから読み取るように修正
- **HTML サニタイザ強化** — AI 出力マークダウンで DOMPurify が `svg` / `iframe` / `object` / `embed` / `script` / `link` / `base` 要素と広範な `on*` イベントハンドラ属性を禁止
- **WSL ディストリビューション名の検証** — 正規表現チェックの前にシェルメタ文字 (`$`, バッククォート, `;`, `&`, `|`, リダイレクト, 引用符, 空白) を拒否 (多層防御)
- **GCP IAP トンネルの argv 強化** — Windows 上で gcloud 呼び出しをシェル文字列組み立てではなく argv 配列 (`cmd /C gcloud.cmd <args>`) で渡すよう変更し、クォート由来のインジェクションリスクを排除
- **アセットプロトコルのスコープ明示** — Tauri `protocol-asset` を有効化し、CSP の `img-src` で `http://asset.localhost` を明示的に許可することで、ユーザー指定のペイン背景画像を安全に配信

## v2.0.0-beta5

セキュリティ強化、AI プロバイダーアイコンのテーマ対応、UI 改善に焦点を当てた5番目のベータリリースです。

### 改善

- **AI プロバイダーアイコンのテーマ対応** — Gemini のグラデーション、OpenAI、Anthropic、Vertex AI の各アイコン色をテーマ変数 (`provider-gemini-1/2/3`, `provider-openai`, `provider-anthropic`, `provider-vertex-ai`) から取得するように変更。カスタムテーマエディタに新セクション **AI Providers** を追加
- **シェルプラグインの置き換え** — 外部 URL 起動用プラグインを `tauri-plugin-shell` から軽量な `tauri-plugin-opener` に移行し、許可する機能範囲を縮小
- **AI プロバイダーのストリーミング整合性** — Anthropic / Gemini / OpenAI / Vertex AI の各プロバイダーで、キャンセル時や応答が空の場合でも chat-done イベントを発行し、ローディング状態が残らないように修正
- **モーダルの一貫性** — ConfirmModal / PasteConfirmationModal / AskAiModal のアクションボタン余白 (`6px 16px`) とフッター間隔 (`8px`) を UI 規約に揃えて統一

### セキュリティ

- **SSH 認証失敗時の資格情報消去** — パスワードおよび秘密鍵パスフレーズを認証試行直後 (成功・失敗を問わず) にメモリから消去し、認証失敗時に平文の機密情報が残る隙をなくしました
- **IAP トンネルのゾーンフィルタ強化** — GCE インスタンス一覧取得で `--filter=zone:(…)` ではなく専用の `--zones=` フラグを使用することで、gcloud フィルタ構文のエッジケース影響を排除
- **DPAPI unsafe ブロックへの安全性コメント追加** — `CryptProtectData` / `CryptUnprotectData` 呼び出しにバッファ初期化・寿命・`LocalFree` 所有権に関する SAFETY 不変条件を明示

## v2.0.0-beta4

ジャンプボックス経由の接続、自動アップデート通知、編集ワークフローの安全性向上、セキュリティ強化に焦点を当てた4番目のベータリリースです。

### 新機能

- **SSH/Telnet ジャンプボックス (踏み台) トンネリング** — SSH 踏み台ホスト経由で目的の SSH / Telnet サーバーへ `direct-tcpip` チャネルで接続。踏み台側もホストキー検証とキーボードインタラクティブ認証に対応
- **自動アップデート通知** — 起動時に GitHub リリース API で新バージョンを確認し、閉じられる通知からリリースページへ直接遷移
- **未保存変更の確認プロンプト** — テキストエディタでタブを閉じる / 終了する際に未保存があれば保存 / 破棄 / キャンセルを問うモーダルを表示、複数ペインを跨ぐ未保存状態トラッカーと連動
- **AI システムプロンプトビューア** — 現在の AI ペルソナに送信される実効的なシステム指示をクリップボードコピー付きで閲覧可能
- **React Error Boundary** — 最上位のエラーバウンダリがレンダラのクラッシュを捕捉し、空白画面の代わりに復帰可能なフォールバック UI を表示

### 改善

- **Telnet サービス** — 接続処理を SSH と共通のジャンプボックストンネル抽象に統一、トランスポート処理を一元化
- **tauriService** — 新しい updater / jumpbox コマンド向けの型付きラッパーを追加
- **useResize フック** — ペインのドラッグリサイズに関する使用感を小改善
- **アプリシェル** — UpdateNotification、SaveConfirmModal、SystemPromptModal、ErrorBoundary を最上位レイアウトに統合

### セキュリティ

- **SSH 認証情報のバリデーション** — `SshConfig::validate` にホスト / ユーザー名 / パスワード / パスフレーズの長さ上限を追加、SSH スタックに到達する前に不正・過大な入力を拒否
- **ログビューアの TOCTOU 対策** — 読み取り直前に解決済みパスを再 canonicalize し、許可ディレクトリチェックを再実行。チェック〜読み取り間のシンボリックリンク差し替え攻撃を防止
- **フォント列挙の unsafe 強化** — Windows フォント列挙コールバックで、OS 供給ポインタを参照する前に null / アラインメントを検証

### メンテナンス

- **未使用アセットの削除** — アプリ内で参照されていない `public/HoTTY_logo.png` を削除
- **テスト追加** — 未保存エディタトラッカーユーティリティのユニットテストを追加

## v2.0.0-beta3

テーマカスタマイズ、UI の調整、テストカバレッジ拡張に焦点を当てた3番目のベータリリースです。

### 新機能

- **カスタムテーマクリエイター** — 外観タブから任意の CSS 変数を編集してユーザー定義テーマを作成・保存・編集・削除できるアプリ内エディタ
- **ヘルプツールチップコンポーネント** — 設定項目やコントロールの横に表示される文脈ヘルプヒント
- **バージョン入りウィンドウタイトル** — メインウィンドウのタイトルに現在のアプリバージョンを表示

### 改善

- **設定モーダル** — 全タブ (外観・全般・機能・プロトコル・AI・バージョン情報) のスタイルを統一し視覚的整合性を向上
- **設定ストア** — 追加の機能トグルと設定オプションを拡張
- **サイドバーのアイコン間隔** — アプリサイドバーのアイコン配置を調整し均等化
- **非推奨の ConnectForm を削除** — セッションダイアログに完全置き換え、レガシーコンポーネントとスタイルを削除
- **テストカバレッジの拡張** — AI チャットパネル、Ask AI モーダル、認証パネル、テーマユーティリティ、カラー/HTML ヘルパー関数のテストを追加
- **ヘルプモーダル** — 新しいカスタムテーマクリエイターのワークフローに対応したドキュメントに更新

### バグ修正

- **モーダル CSS の整合性** — PasteConfirmationModal、SettingsModal、HelpModal のパディング、ボーダー半径、アニメーションタイミングを統一
- **ペインツールバーの整合性** — TextEditorPane と PingMonitorPane のツールバーを標準 36px ツールバー仕様に整合

## v2.0.0-beta2

AI統合、接続管理UI、ユーティリティペインの強化を含む2番目のベータリリースです。

### 新機能

- **AI チャットペイン** — マルチプロバイダー対応AIチャット (ストリーミングレスポンス、ペルソナ、トークンコスト追跡)
- **AI プロバイダー** — Google AI Studio (Gemini)、Vertex AI、Anthropic (Claude)、OpenAI (GPT) をプロバイダー別認証で対応
- **AI バックエンドサービス** — SSEストリーミング対応の Rust ベース AI プロバイダー基盤
- **Ask AI モーダル** — ターミナルテキストを右クリックして、組み込みまたはカスタムコマンドで AI に問い合わせ
- **AI 設定タブ** — AI プロバイダー、モデル、ペルソナ、Ask AI コマンド、コマンド実行モード、モニターバッファ制限の設定
- **AI インタラクティブモード** — AI がターミナルコマンドを提案・実行 (安全性分類: 安全/破壊的/不明)
- **AI ウォッチモード** — ターミナル出力を監視し、キャプチャしたログを AI に送信して分析
- **ホストツリー** — フォルダ、ドラッグ＆ドロップ並べ替え、ホストツリーのエクスポート/インポート対応の接続管理UI
- **セッションダイアログ** — SSH、Telnet、シリアル、WSL、ローカル、Git Bash セッションの作成・編集ダイアログ (ジャンプボックス・IAPトンネル対応)
- **ヘルプモーダル** — 全機能、ショートカット、AIセットアップガイドを網羅するアプリ内ドキュメント
- **確認モーダル** — 破壊的操作用の再利用可能な確認ダイアログ
- **コマンド分類器** — AI自動実行判定のためターミナルコマンドを安全/破壊的/不明に分類
- **AI トークン価格** — 全対応プロバイダーのモデル別コスト見積もり用価格データ

### 改善

- **テキストエディタ** — 検索＆置換、行移動、サブタブによる複数ファイル対応、エンコーディング/改行コード選択、行折り返し、改行コード可視化、ファイル関連付けを追加した大幅強化
- **ファイルエクスプローラ** — パンくずパス、隠しファイル切替、ドライブ参照、ダブルクリックでエディタを開く機能による改善
- **Pingモニター** — 設定可能なインターバル、ログ出力、レイアウト改善による強化
- **ログビューア** — 検索/フィルター、正規表現切替、ファイル参照の改善
- **タブバー** — フィーチャーペインタブ対応とドラッグ＆ドロップの改善
- **設定ストア** — AI設定、有効フィーチャー、追加設定オプションの拡張
- **セッションマネージャー** — AIチャットターミナル統合のサポートを更新
- **アプリアイコン** — 全プラットフォーム (Windows, macOS, iOS, Android) のアイコンを刷新
- **新ユーティリティフック** — useFocusTrap、useModalState、useResize による UI インタラクションの改善
- **ANSI ユーティリティ** — ターミナル出力処理用の ANSI コード処理関数を追加
- **カラー・HTMLユーティリティ** — 色操作・HTML処理のヘルパー関数を追加

## v2.0.0-beta1

Rust/Tauri 版リライトの初回ベータリリースです。Electron ベースの HoTTY を Tauri v2 バックエンドに置き換え、メモリ効率とパフォーマンスを改善しました。

### 新機能

- **マルチプロトコル接続** — SSH、Telnet、シリアル、WSL、ローカルシェル (cmd, PowerShell, Git Bash)
- **SSH ホスト鍵検証** — 新規・変更されたホスト鍵のフィンガープリント表示と承認/拒否プロンプト
- **SSH 秘密鍵認証** — 鍵ファイルとパスフレーズ対応
- **SSH アルゴリズム設定** — KEX、暗号、MAC、ホスト鍵アルゴリズムの設定
- **シリアルポート対応** — ボーレート、データビット、パリティ、ストップビット、フロー制御の設定
- **WSL ディストリビューション選択** — インストール済みの任意の WSL ディストリビューションに接続
- **マルチペインレイアウト** — グリッドレイアウト (1x1, 1x2, 2x1, 2x2, 2x3, 3x2) と四辺の折りたたみサイドバー
- **タブバー** — ドラッグ＆ドロップによる並べ替え、セッション・フィーチャーペインタブ
- **ログビューアペイン** — セッションログファイルの閲覧・表示
- **テキストエディタペイン** — ファイルの表示・編集・保存 (改行コード検出対応)
- **ファイルエクスプローラペイン** — ディレクトリ・ドライブの参照、テキストエディタでファイルを開く
- **Pingモニターペイン** — 複数ターゲットの監視 (間隔設定・ログ出力対応)
- **テーマ機能** — 組み込み Dark / Medium / Light テーマとカスタムテーマ対応
- **設定モーダル** — 外観、全般、機能、プロトコル、バージョン情報のタブ構成
- **Windows DPAPI 暗号化** — 保存された接続の認証情報を安全に保管
- **貼り付け確認** — ターミナルへの貼り付け前にクリップボード内容を確認するモーダル
- **セッションログ** — セッションごとのログファイル出力
- **ホストツリー エクスポート/インポート** — 暗号化 .htree 形式による接続設定のバックアップ
- **GCE IAP トンネル** — Identity-Aware Proxy 経由で Google Cloud インスタンスに接続
- **エンコーディング対応** — セッションごとに UTF-8、Shift_JIS、EUC-JP を選択可能
- **キープアライブ** — SSH / Telnet 接続のキープアライブ間隔設定
- **システムフォント検出** — インストール済みフォントの一覧表示・選択
- **コンテキストメニュー** — 右クリックメニュー対応
- **デバッグログ管理** — デバッグログフォルダの表示・オープン

### 改善

- **Rust/Tauri v2 バックエンド** — Electron からの完全リライトによりメモリ使用量削減・起動高速化
- **Zustand 状態管理** — localStorage ミドルウェアによる設定の永続化
- **型付き IPC レイヤー** — 全 Tauri コマンドを tauriService.ts で TypeScript 型付きラッパーとして実装
- **包括的なテストカバレッジ** — 全フロントエンドコンポーネント・フック・サービスの Vitest テスト

## v0.1.1

スキャフォールドのみのバージョン更新。機能変更なし。

## v0.1.0

- Tauri v2 プロジェクトの初期スキャフォールドとマイグレーション仕様書。
