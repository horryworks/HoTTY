# リリースノート - HoTTY

## [v1.0.5-beta1] - 2026-04-05

### 改善
- **設定画面の UX 改善**: General タブを Storage、Input、Diagnostics の3セクションに再編成し、見出しを追加。設定項目名をわかりやすく変更: "Watch Buffer Limit" → "AI Monitor Buffer Limit"、"Interactive Flow Stabilization Timeout" → "Command Output Wait Time"、"Local Log Buffer" → "Terminal Scrollback Buffer"、"Empty Pane Background" → "Unused Pane Background"。AI 設定の説明文も全体的に改善。
- **セッションダイアログのヘルプテキスト**: Google Cloud IAP、ジャンプボックス（踏み台）、シリアルポート設定にインラインヘルプの説明を追加。"Jumpbox" ラベルを "Jumpbox (Bastion)" に変更し、より明確に。
- **SSH アルゴリズムの折りたたみ表示**: Protocols タブの SSH アルゴリズムセクションが折りたたみ可能になり、不要時の視覚的なノイズを軽減。
- **ドラッグ＆ドロップの視覚フィードバック**: Ask AI コマンドリストのドラッグ時に、ドロップ先にアクセントカラーのボーダーハイライトを表示。
- **アップデート通知のテーマ変数**: アップデート通知バナーの外観を完全にカスタマイズできる 8 つの新しい `update-notification-*` テーマ変数を追加。

### バグ修正
- **ドラッグ＆ドロップのちらつき修正**: Ask AI コマンドのドラッグ＆ドロップ時に、子要素の境界で `onDragLeave` が発火することによるハイライトのちらつきを修正。
- **CSS フォールバックカラーの不一致修正**: TabBar のグラデーションフォールバックカラーと LayoutSelector のボーダーフォールバックカラーを、実際のダークテーマの値に一致するよう修正。

---

## [v1.0.4] - 2026-04-05

### 改善
- **接続エラーの詳細表示**: セッションダイアログの接続エラーが、Connect ボタン上部の専用スクロール可能パネルに表示されるようになりました。ユーザーフレンドリーなメッセージと生のエラー詳細の両方が表示され、レガシーデバイス（Cisco ASA のアルゴリズムネゴシエーション失敗など）での SSH ハンドシェイク問題の診断が容易になりました。
- **プロンプトパターン設定の簡素化**: プロンプトパターン設定の UI を簡素化しました。パターンごとの有効/無効チェックボックスを廃止し、よりすっきりした1行レイアウトに変更。組み込みパターンをすぐに復元できる「Reset to Default」ボタンを追加しました。
- **Huawei プロンプト検出の改善**: Huawei/Yamaha のプロンプトパターンを更新し、HA クラスター構成で使用される HRP（High Reliability Platform）プレフィックスを認識できるようになりました。

### バグ修正
- **ジャンプボックスチェックボックス**: ホストツリーからホストを選択せずに手動でセッション情報を入力した場合に、「Use as Jumpbox」チェックボックスが誤って表示される問題を修正しました。

### セキュリティ
- **依存パッケージの脆弱性修正**: `@xmldom/xmldom` の高深刻度 XML インジェクション、`lodash` の高深刻度コードインジェクションおよびプロトタイプ汚染、`electron` の中程度 HTTP レスポンスヘッダーインジェクションおよび use-after-free を `npm audit fix` で解消しました。

---

## [v1.0.3] - 2026-03-28

### 新機能
- **GCE IAP トンネル**: Google Compute Engine VM に Identity-Aware Proxy (IAP) 経由で SSH 接続が可能になりました。VM をパブリックインターネットに公開する��要がありません。セッションダイアログが gcloud CLI と連携し、GCP プロジェクト・ゾーン・インスタンスのオートコンプリートを提供します。SSH 接続フォームの「Connect via Google Cloud IAP」チェックボックスから有効化できます。
- **ファイルエクスプローラー**: 内蔵ファイルブラウザペインを追加しました（**⊞** Features メニュー → **File Explorer** からアクセス）。ドライブとディレクトリをツリー構造で閲覧でき、遅延読み込みによるフォルダ展開、隠しファイルの表示切替、ダブルクリックでテキストエディターに直接ファイルを開く機能を搭載。仮想スクロールにより大量ファイルでもスムーズなパフォーマンスを実現。
- **Git Bash 接続**: Git Bash を新しい接続タイプとして追加。HoTTY が Git Bash のインストールを自動検出し、インタラクティブなログインシェルセッションを提供します。セッションダイアログのプロトコルドロップダウンから選択可能。

### 改善
- **設定画面の再構成**: 設定モーダルを専用タブに再編成しました — **General**（ログ、キーボード、バッファ）、**Protocols**（プロトコル切替、SSH/Telnet KeepAlive、SSH アルゴリズム）、**Features**（AI Chat、Log Viewer、Ping Monitor、Text Editor、File Explorer の有効/無効）、**Appearance**、**AI** — よりナビゲーションしやすく、設定しやすくなりました。

### バグ修正
- **テキストエディタータブの同期**: クロージャバグにより、テキストエディターのサブタブの状態が古くなり、タブが古いコンテンツを表示する問題を修正しました。

---

## [v1.0.2] - 2026-03-26

### 改善
- **セッションダイアログの自動センタリング**: アプリケーションウィンドウのリサイズ時に「新規セッション」ダイアログが自動的に中央に再配置されるようになり、常にアクセスしやすい位置を維持します。
- **セッションダイアログのフォームロック**: 接続フォーム（Save・Connect ボタンを含む）が送信直後に即座に無効化されるようになり、誤ったダブルクリックを防止します。
- **テーマ準拠の強化**: 10 コンポーネントにわたる 19 箇所のハードコードされたインラインカラー、フォントファミリー、フォントサイズを CSS テーマ変数（`--bg-primary`、`--font-family`、`--font-size-base`）に置き換え、すべての UI 要素がアクティブテーマに従うようにしました。
- **UI の一貫性向上**: 全モーダルコンポーネント（SettingsModal、CustomThemeCreator、PasteConfirmationModal、AskAiModal）のヘッダー/フッターパディング、ボタンのトランジション速度、背景色、ヘッダーの border-radius を統一しました。

### セキュリティ
- **依存パッケージの脆弱性修正**: `picomatch` の高深刻度 ReDoS 脆弱性および `yaml` の中程度のスタックオーバーフロー脆弱性を `npm audit fix` で解消しました。

---

## [v1.0.1] - 2026-03-25

### 改善
- **テキストエディター: 改行コードの可視化**: エディター上で改行コード（改行文字）が視覚的なシンボルとして表示されるようになりました。新しい **View** メニューから表示の切り替えが可能です。
- **テキストエディター: 行選択の改善**: 行をダブルクリックすると、改行コードを含まずに行全体の内容が選択されるようになりました。
- **UI の一貫性向上**: モーダルの z-index 階層、ヘッダーパディング、ペインツールバーのボタンパディング、無効化ボタンのカーソル、フォントウェイトパターンを全コンポーネントで統一し、より均一な外観を実現しました。

### セキュリティ
- **テキストエディターのファイルアクセス制限**: テキストエディターの IPC ファイル読み書きハンドラーが、ネイティブファイルダイアログまたはドラッグ＆ドロップで事前に承認されたファイルパスのみを受け付けるようになりました。レンダラープロセスが侵害された場合の不正ファイルアクセスを防止します。

### バグ修正
- **About タブアイコン**: 設定画面の About タブにおけるアイコンの背景色を修正しました。

---

## [v1.0.0] - 2026-03-22

### 初の安定版リリース

HoTTY v1.0.0 は初の安定版リリースです。SSH、Telnet、Serial、WSL、ローカルシェル接続に対応した、AI 統合型の高機能 Windows ターミナルエミュレーターです。

> ⚠️ **注意:** OpenAI プロバイダー連携は十分にテストされていません。Google AI Studio (Gemini)、Vertex AI、Anthropic の使用を推奨します。

### 新機能（v1.0.0-beta10 以降）
- **パスワード表示**: ホストツリーにパスワード表示トグルを追加。保存済みパスワードを必要に応じて確認可能に。

### バグ修正（v1.0.0-beta10 以降）
- **ホストツリーのインポート/エクスポート**: ホストツリーのインポート/エクスポート機能のバグを修正。
- **タブツールチップ**: ツールチップ表示中にタブを閉じるとツールチップが消えない問題を修正。

---

## [v1.0.0-beta10] - 2026-03-22

> ⚠️ **プレビューリリース** — Vertex AI、Anthropic、OpenAI の各プロバイダー連携は十分にテストされていません。本番環境では引き続き Gemini の使用を推奨します。

### 新機能
- **ジャンプボックス（踏み台ホスト）トンネリング**: SSH および Telnet 接続を中間の SSH ジャンプボックスホスト経由でルーティングできるようになりました。主な機能：
    - ホストツリーで任意の SSH ホストを **ジャンプボックス** として指定可能（🔗 リンクアイコンで表示）。
    - ターゲットホストにジャンプボックスを割り当て — 接続フォームに「via [ジャンプボックス名]」のメタデータを表示。
    - ssh2 の `forwardOut()` を使用してジャンプボックス経由のセキュアな TCP トンネルを作成。
    - 既存の known_hosts インフラによるジャンプボックスのホストキー検証。
    - トンネル接続のセッションタブタイトルに「via [ジャンプボックスホスト]」を自動付加。
- **タブツールチップ**: タブタイトルがオーバーフローした場合にツールチップを表示し、狭いタブでも接続名の全文を確認可能に。

### 改善
- **Telnet 認証**: Telnet 接続でユーザー名/パスワードの自動認証をサポート。認証パースを telnet-client ライブラリに委譲。
- **ターミナルマーカー**: マーカーの位置を垂直スクロールバーの2px左に調整し、重なりを防止。
- **ターミナルスクロールバー**: 垂直スクロールバーの外観とスタイルの一貫性を改善。
- **UI の統一性**: 重複していた `@keyframes` アニメーションを共有 `animations.css` ファイルに集約。すべてのダイアログコンポーネントのモーダルシャドウ、border-radius、入力フィールドのスタイルを統一。
- **テーマの整理**: 廃止されたテーマ変数（`error-color`、`chat-msg-model-bg`、`panel-bg`、`active-pane-color`、`select-arrow`）を削除し、タブの Watch 状態やターミナルプロンプトインジケーター用の新しい変数を追加。

---

## [v1.0.0-beta9] - 2026-03-21

> ⚠️ **プレビューリリース** — Vertex AI、Anthropic、OpenAI の各プロバイダー連携は十分にテストされていません。本番環境では引き続き Gemini の使用を推奨します。

### 新機能
- **テキストエディター**: 新しいペインタイプとして内蔵テキストエディターを追加しました（**⊞** Features メニュー → **Text Editor** から開けます）。主な機能：
    - マルチタブ編集 — 1つのペイン内の独立したサブタブで複数ファイルを編集可能。
    - エンコーディング（UTF-8、ASCII、Latin-1）と改行コード（LF / CRLF）表示に対応したファイルの開く・保存。
    - **検索 & 置換** ツールバー（大文字小文字区別あり・一致数表示）。
    - 大きなファイルを素早く移動するための **行ジャンプ** ダイアログ。
    - 折り返し行を正確に反映したビジュアル行番号表示。
    - サブタブのタイトルに未保存変更インジケーター（`•`）を表示。
    - **ファイル関連付け** — コマンドライン引数として渡されたファイルをテキストエディターで直接開きます。

### 改善
- **テーマの完全性**: UI 内に残っていたハードコードされた色をすべて CSS 変数に置き換えました。すべての組み込みテーマに新しい CSS 変数を追加：
    - `text-tertiary` — ドラッグハンドルやヒントなどの薄いテキスト用。
    - `status-success` / `status-error` — 認証ステータスのインジケータードット用。
    - `link-color` — About パネルのハイパーリンク用。
    - `pane-color-1` ～ `pane-color-6` — タブ-ペイン接続線の色用。
    - `resize-grip-shadow` — リサイズグリップハンドルのストライプパターン用。
- **UI の統一性**: すべてのダイアログコンポーネントのモーダルパディング、フォントサイズ、スペーシングを統一しました。
- **行折り返しのパフォーマンス改善**: ビジュアル行番号の計算を、行ごとのリフローではなく1回の DOM レイアウトパスで処理するよう書き直し、行折り返し有効時の大きなファイルでの遅延を解消しました。
- **コード品質**: 重複していたターミナルカラー適用ロジックを抽出し、文字列型のタブタイプ判定を型付き Set 定数に置き換えました。

### セキュリティ
- **DPAPI バッチバリデーション**: `dpapi-encrypt-batch` および `dpapi-decrypt-batch` IPC ハンドラーに配列長のガード（最大1,000件）を追加し、無制限の入力によるメモリ枯渇を防止します。

---

## [v1.0.0-beta8] - 2026-03-21

> ⚠️ **プレビューリリース** — Vertex AI、Anthropic、OpenAI の各プロバイダー連携は十分にテストされていません。本番環境では引き続き Gemini の使用を推奨します。

### 新機能
- **Ping Monitor（Ping監視）**: ICMP Ping による監視ペインを新しいセッションタイプとして追加しました。複数ホストの同時監視が可能です：
    - Ping 間隔の設定（1秒、3秒、5秒、10秒、30秒、60秒）。
    - ターゲットごとの RTT（往復時間）と TTL のリアルタイム表示。
    - ステータスインジケーター（OK、Fail、DNS エラー）による視覚的な状態表示。
    - 設定済みのログフォルダへの CSV ログ出力（オプション）。
    - ホスト名を通じたシェルインジェクションを防止する入力バリデーション。

### 改善
- **ペルソナ別 Ask AI コマンド**: AI ペルソナごとに独立した Ask AI コマンドセットを設定できるようになりました。ペルソナを切り替えると対応するコマンドセットが自動的にロードされ、役割に応じたクイックアクション（例：ネットワークエキスパート vs セキュリティアナリスト）が可能です。AI 設定はスタンドアロンのモーダルとして分離し、アクセスしやすくなりました。
- **グラフィックパフォーマンスの向上**: UI 全体の描画パフォーマンスを改善しました：
    - `requestAnimationFrame` を活用したグリッドレイアウトの再計算とリサイズ処理の最適化。
    - ターミナルペイン、AI チャット、ログビューアーコンポーネントの不要な再レンダリングを削減。
    - スロットリングによるサイドバーリサイズのスムーズ化。

---

## [v1.0.0-beta7] - 2026-03-20

> ⚠️ **プレビューリリース** — Vertex AI、Anthropic、OpenAI の各プロバイダー連携は十分にテストされていません。本番環境では引き続き Gemini の使用を推奨します。

### 改善
- **Vertex AI 実装の強化**: Vertex AI プロバイダーの大幅な改善を実施しました：
    - **リージョン対応モデル検出**: `ai-list-locations` および `ai-set-location` IPC チャネルを追加し、Vertex AI のモデル取得時に動的なリージョン選択を可能にしました。
    - **グローバルエンドポイント対応**: `global` ロケーション指定時に、リージョンプレフィックス付きエンドポイントではなく `aiplatform.googleapis.com` に正しくルーティングされるようになりました。
    - **パブリッシャー別ルーティング**: Vertex AI のチャットリクエストが、Google とサードパーティ Model Garden パブリッシャー間の API フォーマットの違いに対応するため、パブリッシャー固有の実装を通じてルーティングされるようになりました。
- **マルチプロバイダーコスト表示**: Gemini 専用だった `calcGeminiCost` を、Gemini・OpenAI・Anthropic の料金テーブルに対応した統合的な `calcAICost` 関数に置き換えました。AI チャットペインにレスポンスごとのコストが累積表示されるようになりました。
- **ヘルプモーダルの刷新**: ヘルプモーダルの AI セクションを「AI クイックスタートガイド」として書き直し、3ステップのオンボーディングフローと新しい「AI プロバイダー比較」セクションを追加しました。
- **プロバイダー切替時のクリーンアップ**: AI プロバイダーを切り替えた際にチャット履歴、ストリーミング状態、トークンカウンターがクリアされるようになり、古いデータが残留する問題を解消しました。

### 変更
- **レガシー Gemini IPC の削除**: 下位互換性のために維持していた `gemini-*` IPC エイリアス（`gemini-auth-start`、`gemini-auth-auto`、`gemini-auth-status`、`gemini-auth-logout`、`gemini-chat-send`、`gemini-list-models`、`gemini-chat-cancel`、`gemini-chat-clear`）およびプリロードのバインディングをすべて削除しました。AI 通信は統一された `ai-*` チャネルのみを使用します。
- **直接 API アクセスの整理**: `HostTree`、`SessionDialog`、`AppearanceTab` に残っていた `window.electronAPI` の直接呼び出しを、型付きの `electronService` ラッパーに置き換え、コードベース全体で一貫した API アクセスパターンを確保しました。

### セキュリティ
- **ログフォルダアクセスの強化**: `list-log-files` IPC ハンドラーが、`update-logging` で事前に登録されたディレクトリからのみ読み取りを許可するようになり、任意のディレクトリのリスティングを防止します。

### バグ修正
- **AI ペインの再ログイン時フォーカス**: AI プロバイダーからログアウトして再ログインした後、AI チャットペインがフォーカスを失う問題を修正しました。
- **AI タブ名**: 選択されたプロバイダーを反映していなかったハードコードされた AI タブ名を修正しました。

---

## [v1.0.0-beta6] - 2026-03-18

### 新機能
- **マルチプロバイダー AI 対応**: AI バックエンドを完全にプロバイダー非依存のアーキテクチャに刷新しました。設定 > AI から使用するプロバイダーを選択可能になりました：
    - **Google Gemini** (Google AI Studio) — 既存の Gemini OAuth フローをそのまま使用。変更なし。
    - **Vertex AI** (Google Cloud) — Application Default Credentials (ADC) またはサービスアカウントキーファイルによる認証をサポート。
    - **OpenAI** — OpenAI API キーで接続。
    - **Anthropic** — Anthropic API キーで接続。
- **プロバイダー専用認証パネル**: 各プロバイダーに対応した専用認証パネルを追加しました。

### 変更
- **AI アーキテクチャの刷新**: 内部 AI 通信をプロバイダー非依存の `AIService` / `IAIProvider` 抽象化で統一しました。IPC チャネルは新しい `ai-*` プレフィックスに移行し、下位互換性のために `gemini-*` エイリアスを維持しています。
- **`useGeminiChat` を `useAiChat` に置き換え**: フロントエンドの AI チャットフックをプロバイダー非依存の `useAiChat` として書き直し、旧 `AskGeminiModal` を `AskAiModal` に置き換えました。

---

## [v1.0.0-beta5] - 2026-03-17

### 改善
- **アップデート通知の機能強化**: アップデート通知バナーに2つのアクションボタンを追加しました。
    - **Skip this version（このバージョンをスキップ）**: バナーを閉じ、現在のバージョンに対する通知を抑制します。さらに新しいバージョンがリリースされた場合は再度通知されます。
    - **Never Notify（通知しない）**: アップデート通知を永続的に無効化します。この設定は localStorage に保存されます。
- **設定画面からのアップデート確認**: アップデートが利用可能な場合、**設定 > About** タブにも新バージョンへのダウンロードボタンが表示されるようになりました。設定ダイアログ内からすばやく最新リリースにアクセスできます。

---

## [v1.0.0-beta4] - 2026-03-17

### バグ修正
- **アップデート通知**: プレリリース形式のバージョン文字列（例: `1.0.0-beta3` vs `1.0.0-beta2`）を持つ新バージョンがリリースされた際に、アップデート通知が表示されなかったバグを修正しました。バージョン比較ロジックを改善し、`-beta3` のようなプレリリースサフィックスを正しく比較できるようになりました。

### セキュリティ
- **デシリアライゼーションの強化**: `GeminiService.loadToken()` における入力検証を強化しました。復号したトークンファイルの各フィールドを個別に型チェックして取り出すことで、予期しないプロパティが代入されないよう対策しました。

---

## [v1.0.0-beta3] - 2026-03-16

### 改善
- **テストカバレッジ**: 全ての機能コードに対するテストカバレッジを100%達成しました。これまでテストが存在しなかった全モジュール（Electronメインプロセスのサービス群: `Logger`、`LogManager`、`dpapi`、`knownHosts`、`GeminiService`、`SshService`、`TelnetService`、`SerialService`、`WslService`、`LocalService`）、`geminiPricing`ユーティリティ、およびルート `App` コンポーネントにユニットテストを追加しました。合計: 58ファイル / 701テスト。
- **ESLint 設定**: テストファイルで TypeScript の private メンバーにアクセスするために必要な `@typescript-eslint/no-explicit-any` を、テストファイルに限定して許可するオーバーライドを追加しました。

---

## [v1.0.0-beta2] - 2026-03-16

### 新機能
- **アップデート通知**: 起動時に GitHub 上の最新リリースを自動チェックするようになりました。新しいバージョンが公開されている場合、ウィンドウ上部に非ブロッキングなバナーが表示され、ダウンロードリンクを案内します。Pre-release バージョンはチェック対象外です。

### セキュリティ
- **XSS 修正**: AI チャットの出力レンダリングで使用していたカスタムHTMLサニタイズ関数を [DOMPurify](https://github.com/cure53/DOMPurify) に置き換えました。SVG `onload`、Data URI、その他の従来実装では対応できなかったXSSバイパス手法を排除します。

### バグ修正
- **Watch タブ切り替えの不具合**: 監視対象のタブを切り替えた際に AI セッションの状態が正しく更新されないバグを修正しました。

### 変更
- **AI 機能名称の汎用化**: AI 関連の機能名・UIラベルを特定のプロバイダー名に依存しない汎用的な名称に変更しました（例: "Gemini Chat" → "AI Chat"、"Gemini Monitor" → "AI Monitor"）。今後の複数AIプロバイダー対応を見据えた変更です。

---

## [v1.0.0-beta1] - 2026-03-14
### 正式リリース
HoTTY v1.0.0 を正式にリリースしました！このマイルストーンは、高度な機能とGemini AIの深い統合を備えた、安定したターミナル体験の完成を意味します。

### アーキテクチャと安定性
- **大規模リファクタリング**: 核心となる `App.tsx` の解体を完了。コードの複雑さを50%以上削減し、長期的な保守性を確保しました。
- **フックベースの状態管理**: 主要なロジック（設定、Gemini、ターミナルフロー）を専用のカスタムフックに移行し、パフォーマンスとデバッグ性を向上させました。
- **テーマエンジン**: CSS変数に完全準拠した堅牢なテーマシステムを構築。ビジュアルテーマエディタも搭載しています。

### AI 機能
- **Gemini 2.0 統合**: 最新の Gemini モデルをネイティブサポートし、利用可能なモデルを自動取得します。
- **インタラクティブ調査**: AIが自発的に調査コマンドを提案し、リアルタイムで結果を解析する高度な連携を実現しました。
- **出力監視 (Watch)**: ターミナル出力を監視し、蓄積されたデータに対して即座にデバッグや解説を依頼できます。

### ユーザーエクスペリエンス
- **高度なグリッドレイアウト**: 直感的な2Dリサイズ機能と、再起動後もレイアウトを維持する永続化機能を搭載。
- **幅広いプロトコル対応**: SSH, Telnet, シリアル, WSL, Windows ローカルシェルを網羅。
- **ログ管理**: 内蔵のログビューアーと、ミリ秒精度のタイムスタンプログ機能を実装しました。

### セキュリティ
- **IPC入力バリデーション**: `update-session-encoding` IPCハンドラーに許可リスト検証を追加し、不正なエンコーディング値がセッションサービスに渡される前にリジェクトするようにしました。

---

## [v0.1.15] - 2026-03-14
### 追加
- **カスタムテーマクリエーター**: 設定 > 外観 に、ビジュアルなテーマエディタを追加しました。JSONファイルを手動で編集することなく、アプリ内でカスタムカラースキームを直感的に作成・調整できるようになりました。
- **Log Viewer（ログビューア）**: ログファイルをHoTTY内で直接閲覧できる新しいセッションタイプを追加しました。New Connection ダイアログのプロトコル選択で「Log Viewer」を選択すると、保存済みセッションログファイルをブラウズ・閲覧する専用ペインを開けます。ログフォルダは設定で指定されたパスを参照します。
- **タイムスタンプログ（`.tslog`）**: 各ログセッションで、通常の `.txt` ログと同時に `.tslog` ファイルを生成するようになりました。`.tslog` ファイルは各行の出力に対応する正確なタイムスタンプ（ミリ秒精度）を記録しており、セッション後の時系列分析が可能になります。

### 変更
- **テーマの完全適用**: すべてのUIコンポーネントにおいて、色・フォントが CSS 変数（`var(--xxx)`）を使用するように統一されました。コードベース全体からハードコードされた色値・フォントサイズを完全に排除し、テーマへの完全準拠を実現しました。
- **Geminiコマンド応答の挙動改善**: GeminiがAIコマンド実行の終了を検出・処理する方法を改善し、より信頼性が高く予測可能な動作を実現しました。
- **Geminiシステムプロンプトの調整**: ターミナル統合ワークフローでのAI動作がより明確で一貫性を持つよう、組み込みのシステムプロンプトを改善しました。
- **New Sessionアイコンの変更**: 「New Session」ボタンのアイコンを、視覚的な分かりやすさを向上させるために更新しました。
- **タブバーの改善**: タブバーのスタイルとインタラクションを洗練し、テーマとの一貫性を向上させました。

### 修正
- **サイドバー/バーのトグル不具合**: 左サイドバー・右サイドバー・トップバー・ボトムバーのトグルアイコンを2回クリックしてもパネルが閉じない不具合を修正しました。
- **Tab-Pane Mapping の矢印が残る不具合**: バーを非表示にした後も「Show Tab-Pane Mapping」オーバーレイの矢印が残り続ける不具合を修正しました。

## [v0.1.15-beta9] - 2026-03-13
### 追加
- **Log Viewer（ログビューア）**: ログファイルをHoTTY内で直接閲覧できる新しいセッションタイプを追加しました。New Connection ダイアログのプロトコル選択で「Log Viewer」を選択すると、保存済みセッションログファイルをブラウズ・閲覧する専用ペインを開けます。ログフォルダは設定で指定されたパスを参照します。
- **タイムスタンプログ（`.tslog`）**: 各ログセッションで、通常の `.txt` ログと同時に `.tslog` ファイルを生成するようになりました。`.tslog` ファイルは各行の出力に対応する正確なタイムスタンプ（ミリ秒精度）を記録しており、セッション後の時系列分析が可能になります。

## [v0.1.15-beta8] - 2026-03-13
### 修正
- **サイドバー/バーのトグル不具合**: 左サイドバー・右サイドバー・トップバー・ボトムバーのトグルアイコンを2回クリックしてもパネルが閉じない不具合を修正しました。原因は、boolean 値のみを受け付ける Zustand のセッターに、関数 (`prev => !prev`) を渡していたことでした。
- **Tab-Pane Mapping の矢印が残る不具合**: バーを非表示にした後も「Show Tab-Pane Mapping」オーバーレイの矢印が残り続ける不具合を修正しました。`computeLines` 関数の依存関係に4つのバー表示フラグ（`showLeftSidebar`、`showRightSidebar`、`showTopBar`、`showBottomBar`）を追加し、いずれかが変更されると自動的に再計算されるようにしました。

### 変更
- **非表示 AI タブのインジケーター**: レイアウトの縮小（ペイン数またはバー数の減少）によって Gemini AI ペインが非表示になった場合、該当タブが通常のレインボーグラデーションではなく赤系グラデーション（濃赤 → クリムゾン）で表示されるようになりました。AI セッションが現在表示されていないことを一目で把握できます。

## [v0.1.15-beta7] - 2026-03-11
### 変更
- **大規模リファクタリングとアーキテクチャの単純化**:
    - **App.tsx の解体**: メインの `App.tsx` を 1953 行から約 900 行（54% 削減）へ大幅に削減しました。「巨大なコンポーネント」を解消し、保守性を劇的に向上させました。
    - **カスタムフックの抽出**:
        - **`useInteractiveFlow`**: ターミナル出力の監視、プロンプト検知、AIフィードバックの安定化ロジックを一元化しました。
        - **`useGeminiChat`**: Gemini API との通信、右クリックメニュー（Ask AI）の解決、コンテキスト管理を統合しました。
        - **`useSettings`**: 40 を超えるアプリケーション設定を 1 つのフックに集約し、localStorage との自動同期を実現しました。
    - **`PaneContent` コンポーネント**: ワークスペース内の 5 箇所（左右サイドバー、上下バー、メイングリッド）で重複していた Terminal/AI Chat の描画ロジックを共通化しました。

### 修正
- **テーマ同期の修正**: 設定フックへの移行後に、テーマ変更が一部のコンポーネントに反映されない不具合を修正しました。
- **コードのクリーンアップ**: 大規模リファクタリングに伴い、多数の未使用変数、lint 警告、型定義の不整合を解消しました。

## [v0.1.15-beta6] - 2026-03-09
### 追加
- **インタラクティブAIの改善**:
    - **多重送信の防止**: ターミナル出力が連続する場合にAIへのリクエストが重複して送られないよう、タイマーの上書き制御ロジックを実装しました。
    - **タイムアウトのカスタマイズ**: 設定 > AI タブに、実行完了判定の待機時間（静寂待ちタイマー）をミリ秒単位で設定できる項目を追加しました（デフォルト：10,000ms）。
    - **キャンセルの完全連動**: AIチャット側で「待機中」をキャンセルした際、バックグラウンドのターミナル監視も即座に停止するように改善しました。これにより `top` 等のコマンドを途中で切り上げた際の挙動が安定しました。
- **UIの洗練**:
    - **設定画面のレイアウト調整**: 設定 > AI タブにおいて、「積極的調査の指示 (Proactive Investigation Instruction)」を「AIペルソナ」の直下に配置し、設定の流れをより直感的にしました。
- **プロンプト検知の精度向上**:
    - プロンプト検知を「取得行全体との完全一致」に厳格化し、コマンド出力内容に含まれる文字列による誤検知を解消しました。

## [v0.1.14-beta5] - 2026-03-08
### 追加
- **インタラクティブ AI 調査機能**:
    - AI（Network Expert 等）が「`show version` を試してみていい？」のように、自発的に調査コマンドを提案するようになりました。
    - **インタラクティブAI調査**: AIが自ら調査コマンドを提案し、実行結果を自動キャプチャして分析を継続できるようになりました。
- **安定性の向上**: コマンド出力の取り込みに300msの「静寂待ち」を導入し、分割して届く出力も漏らさず分析に回せるようになりました。
- **プロンプト検知の改善**: ANSI/OSCコードの除去強化と末尾一致の厳格化により、実行完了の検知精度が大幅に向上しました。
- **待機プロセスの可視化**: 待機インジケータに経過時間と受信データ量をリアルタイム表示する機能を追加しました。
- **手動コントロール**: 待機状態に「Cancel」および「Send Now」ボタンを追加し、手動での介入が可能になりました。
- **チャットのスマートスクロール**: 長いAI回答が届いた際、メッセージの先頭が自動的に表示されるようスクロールを最適化しました。スクロールして戻る手間が省けます。

### 修正
- **プロンプト検知の堅牢化**: CMD や PowerShell において、行の途中にプロンプトが現れるケース（他出力との混在）を含め、検知精度を大幅に向上させました。
- **データリスナーの安定化**: ターミナルデータ受信時のステート管理を刷新し、高速な出力時でもデータの欠落や古いセッション情報への参照が発生しないように修正しました。

## [v0.1.14-beta4] - 2026-03-08
### 追加
- **AI Monitor (出力監視機能)**:
    - ターミナルセッションの出力を監視・記録する「Watch」（👁️）機能を追加しました。
    - **タブAskボタン**: 監視中のタブに「✨ Ask」ボタンが表示されます。クリックすると「Analyze Watched Output」やユーザー定義のAIコマンドを選択できるメニューが表示されます。
    - **バッファ上限設定**: Settings > AI タブに「Watch Buffer Limit」項目を追加。記録する出力の上限文字数を調整可能です（デフォルト50万文字）。
- **AI連携時のタブ自動切り替え**: 「Ask AI」実行時に、解析対象のターミナルタブを自動的にアクティブにするよう改善し、コンテキストの整合性を高めました。
- **UIデザインの刷新**:
    - Watch状態を示すアイコンを、絵文字からクリーンなSVGアイコンに変更しました。
    - 監視中であることを示すアイコンが緩やかに点滅し、一目で状態がわかるようになりました。
- **Askボタンの無効化制御**: バッファにデータが溜まっていない状態では「✨ Ask」ボタンを無効化し、不要な操作を防止します。

### 修正
- **コンテキスト解決の修正**: バックグラウンドにあるタブからAIに質問を投げた際、誤ったターミナルの内容が読み取られる不具合を修正しました。
- **メニュー表示のクリーンアップ**: タブの✨ボタンからメニューを開く際、不要な「Paste」項目などを排除し、AI関連の操作に特化した表示に改善しました。

### 追加
- **Geminiログアウト機能**: Settings > AI タブに「Logout from Gemini」ボタンを追加しました。Googleアカウントの認証トークンを安全に破棄できます。
- **AIタブの自動閉鎖**: Geminiからログアウトした際、開いているすべてのAIチャットタブを自動的に閉じるようにし、セキュリティと利便性を向上させました。

### セキュリティ強化
- **Content Security Policy (CSP) の導入**: 厳格なCSPを導入し、XSSのリスク軽減と不正なスクリプト実行の制限を強化しました。
- **トークンベースのメディアアクセス制御**: `media://` プロトコルをトークンベースの参照方式（Capability-based Security）へ刷新しました。これにより、任意のパスへのアクセスを許可する脆弱な `authorizeMediaPath` IPC を廃止し、ローカルファイルへの不正アクセスを防止します。

## [v0.1.14-beta2] - 2026-03-08
### 修正
- **コンテキストメニューの挙動改善**: 「Export」選択後にコンテキストメニューが消えずに残ってしまう問題を修正しました。エクスポート用のパスワード入力画面が表示される際に、自動的にメニューが閉じるようになります。
- **UIのクリーンアップ**: ホスト単体を右クリックした際に、メニューの一番上に表示されていた不要な区切り線を削除しました。

## [v0.1.14-beta1] - 2026-03-04
### 追加
- **Telnet KeepAlive**:
    - Telnet接続のアイドルタイムアウトを防止する2層KeepAlive機能を実装しました。
    - **TCP Keepalive**: `socket.setKeepAlive()` によるOSレベルの接続死活監視。
    - **Telnet NOP**: IAC NOP (`0xFF 0xF1`) を定期送信し、サーバー側のアイドルタイマーをリセットします。
    - デフォルト間隔: 30秒（設定で変更可能）。
- **Telnet設定タブ**: Settingsに専用の「Telnet」タブを新設。KeepAliveの有効/無効チェックボックスと間隔入力を配置しました。
- **ツールバー位置の切り替え**: Settings > Appearance に「Toolbar Position」オプションを追加。サイドバーを左右に切り替え可能になりました。

### 修正
- **閉じるボタンの統一**: SettingsモーダルとNew Connectionダイアログの閉じるボタンのスタイルを統一しました（フォントサイズ、色、フォーカスリングの非表示を統一）。

## [v0.1.13] - 2026-02-23
### 修正
- **ターミナルマーカーの表示改善**:
    - 長い出力が折り返された行においても、マーカー（オレンジ/青）が途切れずに表示されるように修正しました。
    - コマンド出力内の空行に対しても一貫してマーカーが表示されるように改善しました。
    - WSLやPowerShellなどで、カーソル位置より下に不要なマーカーが残る問題を解消しました。マーカーの管理ロジックを簡素化し、物理的な行の状態に正確に追従するように再設計しました。

### 変更
- **クリーンアップ**:
    - プロジェクトルートに残っていた一時的なテストスクリプト (`test.js`) およびバックアップファイル (`README.ja.md.bak`) を削除し、プロジェクトをクリーンな状態にしました。


## [v0.1.12] - 2026-02-22
### 修正
- **ホストツリーのインデント**:
    - 深くネストされたホストエントリのインデントが大きくなりすぎるバグを修正しました。外側のラッパー `div` に `paddingLeft` を設定していたため、ネストが深くなるほどインデントが累積して膨れ上がる問題が発生していました。
    - `paddingLeft` の設定先を外側の `host-tree-node` ラッパーから、行要素 `host-tree-row` 本体に変更しました。これにより、ネストの深さに関係なく各レベルのインデントが正確に 14px ずつ加算されます。
    - 深いフォルダ以下のホスト（例: Global → APAC → Japan → WSL local）が、浅いフォルダのホスト（例: Local → WSL local）と同じ相対インデントで表示されるようになりました。

### 変更
- **コードメンテナンス**:
    - `electron/main/index.ts` に残っていた日本語コメントを英語に翻訳しました。

## [v0.1.11] - 2026-02-20
### 追加
- **トップバー / ボトムバー**:
    - ワークスペースの上下に表示できる **Top Bar** と **Bottom Bar** を追加しました。
    - 通常のペインと同様にタブをドロップして利用できますが、中央グリッドの全幅にまたがって表示されます。
    - ステータス監視や常駐ツールの配置に最適です。
- **レイアウトの保存機能 (Persistence)**:
    - アプリ再起動時に、前回のレイアウト状態を自動的に復元するようになりました！
    - **保存される設定**:
        - サイドバーおよびトップ/ボトムバーの表示状態とサイズ（パーセント）。
        - グリッドのレイアウトモード（1x1, 2x2 など）。
        - グリッド内のペインリサイズ比率。
    - **スマートリサイズ**: 各バーのサイズ指定を **パーセントベース** に変更しました。ウィンドウサイズを変更しても、レイアウトの比率が崩れずに維持されます。
- **ホストマネージャー**:
    - **New Connection** ダイアログが左右2パネル構成に変わりました。
    - **左パネル**: ツリー型ホスト管理。フォルダを何段階でもネストできます（例: Global → APAC → Japan）。
    - ホストにプロトコル・ホスト名/IP・ポート・ユーザー名・パスワードを保存できます（localStorage に永続化）。
    - 右クリックでフォルダ追加・ホスト追加・編集・削除が可能。
    - ホストをクリックすると右側フォームに自動入力。Connect 時に認証情報が自動更新されます。

### 修正
- **タブ・ペインマッピング**:
    - サイドバー、トップバー、ボトムバーにタブを配置した際に、「Show Tab-Pane Mapping」の矢印が表示されない問題を修正しました。
- **未読データインジケーター**:
    - サイドバー、トップバー、ボトムバーに配置されたターミナルタブにおいて、該当のバーが非表示にされた際に、バックグラウンドでの出力があってもタブが赤く（未読状態）ならない問題を修正しました。

### 変更
- **UIの改善**:
    - **コーナーの優先順位**: 左右のサイドバーが上下のバーよりも優先して縦いっぱいに表示される「サンドイッチ型」レイアウトに変更しました。
    - **ギャップの統一**: 全てのペイン、サイドバー、バーの間の隙間を 2px に統一し、見た目をすっきりさせました。
    - **レイアウトセレクター**: トップ/ボトムバーの表示切り替えアイコンを追加しました。
- **ターミナルの割り当て**:
    - **フォールバック割り当て**: メインのグリッドが一杯になった場合、新しいターミナルセッションは自動的に空いている（かつ表示されている）外側のバーに以下の優先順位で開くようになりました: 左サイドバー -> 右サイドバー -> トップバー -> ボトムバー。



## [v0.1.11-beta2] - 2026-02-19
### 修正
- **インストーラー版の修正**:
    - 設定ファイル (`themes.json`, `ssh_algorithms.json`) がインストーラーに含まれない問題を修正。
    - 設定の保存先をユーザーデータフォルダ（`%APPDATA%` 等）に変更し、設定変更が確実に保存されるように修正。
- **起動時の不具合修正**:
    - アプリ起動時に真っ黒な画面のまま停止する問題を修正（IPC通信定義の欠落を補完）。
- **ネットワーク機器互換性**:
    - **SSH, Telnet, Serial** 接続において、Backspace時に送信される文字コードのデフォルトを `0x7f` (DEL) から `0x08` (BS) に変更。
    - WSLやローカルシェル（CMD, PowerShell）では従来の `0x7f` (DEL) を維持し、互換性を確保。
    - 必要に応じて `0x7f` を送信するための設定項目「Backspace sends 0x7F (DEL)」を設定 > システムに追加。

### 追加
- **SSHアルゴリズムのカスタマイズ機能**: 設定 > SSHに、SSHの鍵交換(Kex)、暗号方式(Cipher)、サーバーホストキー(HostKey)、HMACアルゴリズムを個別に有効/無効化できるメニューを追加。
- **設定ファイルの外出し**: テーマ定義 (`themes.json`) と SSHアルゴリズム定義 (`ssh_algorithms.json`) を外部リソースとして分離。ユーザーデータフォルダ内のファイルを編集することで、高度なカスタマイズが可能になりました。
- **設定画面のUI改善**: タブの並び順を変更（System, Appearance, SSH, AI, About）し、Network設定を専用の「SSH」タブに統合。


## [v0.1.10] - 2026-02-19
### 追加
- **マルチウィンドウ対応**: 複数のHoTTYウィンドウを同時に開いて利用できるようになりました。アプリのアイコンをクリックしたり、2つ目のインスタンスを起動すると、新しい独立したウィンドウが開きます。
- **IPCアーキテクチャの改善**: 各ウィンドウが独立して自身のセッションやAIチャットを管理できるように、内部通信のリファクタリングを行いました。

## [v0.1.9] - 2026-02-18
### 追加機能
- **Windows ローカルシェル対応**: コマンドプロンプト (CMD) と PowerShell への直接接続を追加しました。
- **PTY 実装の統合**: `node-pty` を導入し、バックスペース、矢印キーによる履歴、Ctrl+C による中断などの完璧なターミナル操作を実現しました。
- **WSL 接続の強化**: WSL 接続も `node-pty` 方式に刷新し、安定性とウィンドウリサイズへの追従性を向上させました。
- **SSH**: サポートされるアルゴリズムを外部JSONファイル (`ssh_algorithms.json`) に分離し、マルチティアのフォールバックに対応。
- **SSH**: 設定ファイル内の各アルゴリズムに有効/無効フラグを追加し、きめ細やかな制御が可能に。

## [0.1.8] - 2026-02-18

### 追加
- **WSL 接続サポート**: Windows Subsystem for Linux (WSL) へのダイレクト接続に対応しました。
  - インストール済みのディストリビューションを自動でリストアップします。
  - `script` コマンドを用いた TTY エミュレーションにより、複雑な依存関係なしでインタラクティブなシェル操作を可能にしました。

### 変更
- **SSH UX の改善**:
  - 「認証に失敗しました」というテクニカルなエラーメッセージを、「ユーザー名またはパスワードが正しくない可能性があります」という分かりやすい表現に変更しました。
  - サーバーによる切断など、通常の終了時に表示されていた不要なエラーポップアップを抑制しました。
- **ターミナル操作の改善**:
  - **Ctrl+C 挙動のカスタマイズ**: テキストが選択されている状態で `Ctrl+C` を押すと、信号（SIGINT）を送信せずに選択を解除するだけにしました。コピーしようとして誤ってプロセスを止めてしまうミスを防ぎます。
- **ドキュメント**:
  - README および リリースノートを最新の機能に合わせて更新しました。

## [0.1.7] - 2026-02-17

### 新機能
- **テーマガイドの追加**: `themes.json` の各プロパティの意味を解説する [THEMES_GUIDE.md](THEMES_GUIDE.md) および [THEMES_GUIDE.ja.md](THEMES_GUIDE.ja.md) を追加しました。
- **デフォルト背景モード**: 空きペインの背景設定に「Default」オプションを追加しました。ファイルを手動で選択しなくても、標準の HoTTY ロゴ背景を簡単に復元できるようになりました。
- **SSH 互換性のさらなる向上**: 特定の機器で発生していた「Unknown DH group」エラーを解消するため、すべての Key Exchange アルゴリズムを明示的に許可するように設定を拡張しました。これにより、古いネットワーク機器や産業用ハードウェアへの接続性がさらに向上しました。

### 変更・改善
- **ビジュアルの改善**:
  - **Darkテーマ**: アクティブなターミナルの背景色を完全な黒（`#000000`）に設定し、デザイン性を向上させました。
  - **Mediumテーマ**: アクティブ・非アクティブの配色を入れ替えて最適化しました。また、不要な白いオーバーレイを削除し、意図した通りの深みのある色合いを再現しました。
  - **Lightテーマ**: `paneBackground` のタイポを修正しました。
- **コードメンテナンス**:
  - 開発の継続性を考慮し、ソースコード内のコメントをすべて日本語から英語に翻訳しました。
  - コードベースから非ASCIIの区切り記号を削除しました。
- **信頼性の向上**:
  - テーマ適用ロジックを改善し、アプリ起動時に `themes.json` から最新の値を読み込み、`localStorage` の古いデータを正しく上書きするように修正しました。

## [0.1.6] - 2026-02-15

### 変更・改善
- **ペルソナシステムの改善**:
  - "Ask AI" 機能が、選択されたペルソナではなくデフォルトの "General Helper" を使用してしまう問題を修正しました。
  - 手動でチャットメッセージを送信する際にも System Prompt が正しく送信されるように修正し、会話中のペルソナ維持を確実にしました。
  - 手動入力エリアにあった重複する Persona 変更プルダウンを削除し、UIを整理しました。
- **UI/UX の改善**:
  - "Settings -> AI" タブの項目順序を "Ask AI Commands", "Personas", "Debugging" に変更しました。
  - 設定画面の "Show System Prompt" および "Enable Logging" ラベルの文字折り返しを防止しました。
- **AIコンテンツの強化**:
  - **新しいコマンド**: "Explain code", "Interpret log", "Root cause analysis", "Fix this" コマンドを追加し、プロンプトテンプレートを改善しました。
  - **新しいペルソナ**: "Security Analyst" ペルソナを追加しました。
  - **ペルソナの洗練**: "Network/Server/Cloud/Coding Expert" のシステムプロンプトをより専門的で具体的な内容に更新しました。
- **インストーラーの改善**:
  - **サイレント更新**: サイレントインストール時（自動更新など）にユーザーデータ削除の確認メッセージを表示しないように修正しました。手動アンインストール時のみ確認が表示されます。

## [0.1.5] - 2026-02-15

### 変更・改善
- **ビジュアル改善**:
  - Setting -> About ページのアイコンをアプリアイコン（icon.png）に差し替えました。
  - アイコンが透過属性の場合でも黒背景で見やすくなるよう、About ページのロゴに白い角丸背景を追加しました。
  - ウィンドウアイコンとブラウザタブ用アイコンを最新のロゴに統一しました。

## [0.1.4] - 2026-02-14

### 新機能
- **セキュリティ強化**:
  - `media://` プロトコルのパス検証を実装し、パス・トラバーサル脆弱性を修正しました。
  - 全てのウィンドウで Electron サンドボックスを有効化しました。
  - 内部 IPC ハンドラーに入力バリデーションを追加しました。
- **アンインストーラーの改善**:
  - アンインストール時にユーザーデータ（設定、認証情報、履歴）を完全に削除するオプションを追加しました。
- **AI 統合の改善**:
  - 起動時のデフォルト AI モデルを「未指定 (Unspecified)」に変更しました。
  - モデルが選択されるまで送信ボタンを無効化し、英語でのエラーメッセージを表示するガード機能を追加しました。

### 変更・改善
- **ビジュアル改善**:
  - アプリアイコンを新しい黒い「H」ロゴに更新しました。
  - タブの閉じるボタンを改善: 赤い丸背景に、SVGで描画された中央揃えの×印に変更しました。
  - サイドバーのレイアウト調整: 「Show Tab-Pane Mapping」アイコンを上部に移動し、「Line Wrap」アイコンを下部に配置しました。また、絵文字アイコンをテーマカラーに対応したSVGアイコンに変更しました。
- **カスタマイズ**:
  - カスタムテーマ設定に「非アクティブ時のターミナル背景色」を追加しました。アクティブ・非アクティブで異なる背景色を設定可能です。

## [0.1.3] - 2026-02-14

### 変更・改善
- **デフォルトテーマ**: 初期設定のテーマを、視認性のバランスが良い「Medium」に変更しました。
- **Mediumテーマの改善**:
  - 全てのペイン（Geminiチャット含む）での配色を統一し、見やすさを向上させました。
  - コントラスト比を調整し、可読性を高めました。
- **アプリアイコン**: Windowsのタスクバーやインストーラーでアプリアイコンが正しく表示されるように修正しました。
- **設定画面 (UI)**:
  - **Ask AI コマンド**:
    - テキスト入力エリアが枠からはみ出す問題を修正しました。
    - コマンドリストのドラッグ＆ドロップによる並び替え機能を追加しました。

## [0.1.2] - 2026-02-14

### 新機能
- **ログ保存機能**:
  - ターミナルセッションのローカルテキストファイルへの自動ログ保存機能を追加。
  - 保存先フォルダはOS標準のダイアログで選択可能。
  - ファイル名はタイムスタンプ、プロトコル、ホスト名を含みます（例: `YYYYMMDDHHMMSS-SSH-192.168.1.1.txt`）。
- **ローカルログバッファ制御**:
  - メモリ使用量を管理するため、スクロールバックの上限行数（デフォルト 10,000行）を設定可能に。
  - 上限を超えた場合、古いログから自動的に削除されます。
- **行折り返し（Line Wrap）切替**:
  - サイドバーに追加されたボタンで、行の折り返し設定をON/OFF切り替え可能に。
  - **水平スクロール**: 折り返しOFF時は、長い行に対して水平スクロールバーが表示されます。

### 変更・改善
- **設定画面 (UI)**: ログおよびバッファ設定用に、新たに「System」タブを追加しました。

## [0.1.1] - 2026-02-13

### 新機能
- **AIモデルの動的読み込み**: Googleアカウントに紐付いた利用可能なGeminiモデル（例: Gemini 1.5 Pro, 2.0 Flash）を自動取得・表示するように改善。
- **テーマシステム**: ライト/ダーク/カスタムの3つのテーマモードを正式サポート。配色設定の保存・復元に対応。
- **設定画面の整理**: 設定ダイアログを「外観（Appearance）」と「ネットワーク（Network）」のタブに分割し、使いやすさを向上。

### 変更・改善
- **UIの洗練**:
  - ドロップダウンメニューの矢印デザインを統一。
  - モーダル背景の不透明度を調整し、視認性を向上。
  - 設定および接続ダイアログをスクロール可能にし、小画面での操作性を改善。
- **AIチャット体験**:
  - AIペインを移動させてもチャット履歴や入力内容が保持されるように変更。
  - モデル選択のフォールバックロジックを強化。

## [0.1.0] - 2026-02-13

### 新機能
- **マルチプロトコル対応**: SSH、Telnet、およびシリアル（COM）ポートへの接続を完全サポート。
- **高度なグリッドレイアウト**: ドラッグ＆ドロップによるタブ操作に対応した、柔軟なマルチペイン・インターフェース。
- **2Dインターセクション・リサイズ**: 4つのペインが交わる十字部分をドラッグすることで、縦横同時にサイズ変更が可能。
- **ビジュアル機能の強化**:
  - ターミナルの文字色・背景色のカスタマイズ。
  - 空きペインへの背景画像適用（タイル表示・繰り返し対応）。
  - 設定画面からの画像ファイル選択機能（エクスプローラー連携）。
- **操作性の向上**: リサイザーの反応領域を 8px に拡大し、状況に応じたカーソル表示（↔︎/↕︎/移動アイコン）を導入。
- **セッション管理**:
  - フォント、文字コード、配色設定の自動保存・復元。
  - セキュアなインメモリ・パスワードキャッシュ機能。
  - 接続維持のための SSH KeepAlive 設定。
- **インストーラー**: Windows 用 NSIS インストーラー (v0.1.0) を提供。

### 変更・改善
- **SSH**: レガシーデバイス（Cisco, Allied Telesis等）との互換性向上のため、幅広いアルゴリズム（KEX, Cipher, HostKey, HMAC）を一挙追加。
- コードの保守性向上のため、`ISessionService` インターフェースによる抽象化を実施。
- `App.tsx` をリファクタリングし、ロジックをカスタムフック (`useSessionManager`, `usePaneManager`) へ分離。
- 操作性を考慮し、境界線のリサイズ幅を 4px から 8px へ拡大。

### 技術的改善
- React 19 + Vite 7 スタックへの移行。
- メインプロセスでのカスタム `media://` プロトコルによる、ローカル画像読み込みの安定化。
- リポジトリ公開に向けた `.gitignore` の最適化。
